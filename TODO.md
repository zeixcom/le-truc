# TODO

## Open — prioritized

- [x] LT-085: Widen the server-fold rule to derived `host.<prop>` thunks, then escalate TSRX034 to an error for `disabled`/`checked` on real form controls (LT-062 decision, 2026-08-29) — reviewed ✓
  **Review (2026-08-29):** Approved. Traced `hostDerivedFold`'s splice-by-source-range mechanism by hand against a nested-member case not in the corpus (`host.config.b` where `config` is `asJSON()`-exposed) — worried this would misfold, but the range-only substitution correctly leaves `.b` untouched (`(config).b`), verified against actual compiler output rather than just reading the code. The all-or-nothing design (one non-foldable `host` read nulls the whole expression) is the right call — matches the same reasoning already applied elsewhere in this compiler (TSRX031's asymmetric-branch handling, LT-055's exclusive-`@if`-branch scoping) of refusing a partial, plausible-looking-but-wrong fold. The severity escalation's gate (`config.form` set + native submittable tag) is appropriately conservative — approved the Check note's reasoning that per-attribute traceability to `formResetCallback` isn't necessary; any `disabled`/`checked` on a real control in a form-associated component is a legitimate correctness risk. Re-ran the full verification suite independently rather than trusting the handoff: `bun test server/tests` (1067/1067), `bun test src/tests` (426/426), `check:tsrx` (TSRX034 down to the expected single `form-listbox.tsrx` warning), `tsc --noEmit` (clean). Minor, non-blocking observation: the new tests exercise the `disabled` escalation path but not `checked` — the logic is symmetric and low-risk, not worth a follow-up task on its own.
  **Skill:** le-truc-dev
  **Changed:** `server/tsrx/evaluability.ts` (new `foldableHostProps`, `HostPropRead`, `hostDerivedFold`, `spliceHostDerivedFold`), `server/tsrx/emit-server.ts` (new `hostDerivedExpr`; wired into `case 'reactive'` between the bare-mirror and `isServerEvaluable` branches), `server/tsrx/analysis/effects.ts` (new `SUBMITTABLE_FORM_CONTROL_TAGS`; TSRX034 gate now also exempts a `hostDerivedFold` hit; severity arg added), `server/tsrx/diagnostics.ts` (`unsafeLoadedAttributeDefault` takes a `severe` flag — error for `disabled`/`checked` on a submittable native control inside a `formAssociated`/`formAssociatedCheckbox` component, warning otherwise). Tests: `server/tests/tsrx/diagnostics.test.ts` — rewrote the now-inaccurate "non-foldable comparison" fixture (it WAS foldable once (c) landed) into two tests (unfoldable when the root doesn't render the prop; folds when it does), plus 4 new tests for the escalation rule.
  **How:** (c) `hostDerivedFold(thunk, foldable)` walks the thunk; every `host` occurrence must be a non-computed `host.<prop>` member read where `prop ∈ foldable` (`foldableHostProps`: Parser-exposed props the root renders as a `kind: 'server'` attribute) — one disqualifying read (bare `host`, computed member, an unlisted prop) nulls the WHOLE result, no partial substitution. `dependenciesOf(node)` minus `'host'` must be empty (no other free names) — this is what makes `host.value <= host.min` foldable but `host.value <= someSignal.get()` not. `spliceHostDerivedFold` replaces each read's source range in `thunkText` with `(${rootAttr.exprText})`, then `emit-server.ts` IIFE-invokes the rewritten thunk exactly like the pre-existing `isServerEvaluable` case. (b) TSRX034's severity is `error` only when the attribute is `disabled`/`checked`, the component's `config.form` is set, and the element tag is a native submittable control (`input`/`select`/`textarea`/`button` — `SUBMITTABLE_FORM_CONTROL_TAGS`, a front-end-layer duplicate of `compiler.ts`'s `NAMED_FORM_CONTROL_TAGS`, same precedent as `MANAGED_FORM_MEMBERS`); every other unfoldable case stays a warning.
  **Corpus impact:** `bun run check:tsrx` — TSRX034 dropped from 6 occurrences across 5 files to 1 (`form-listbox.tsrx` line 107, `hidden={() => !host.filter}` — `filter` is Parser-exposed but never rendered on the root, so correctly stays unfoldable and a warning, not `disabled`/`checked` so no escalation applies either). `bun test server/tests`: 1067 pass, 0 fail (1062 baseline + 5 new). `bun test src/tests`: 426/426. `bun run build:cem`: 64 declarations, no duplicate tags. `bunx tsc -p tsconfig.json --noEmit`: clean.
  **Check:** (1) Confirm the severity split's gate — `config.form != null` (component-level) AND the element's own tag is a submittable control — is the right boundary, not e.g. also requiring the specific attribute to be the ONE actually wired to `formResetCallback`/`setFormValue` (no such per-attribute traceability exists; any `disabled`/`checked` on a real native control inside a form-associated component can affect submission). (2) `foldableHostProps`/`hostDerivedFold` intentionally do NOT recurse into whether the root attribute's OWN `exprText` is itself server-known — reused the same trust boundary `hostPropMirrorExpr` (the pre-existing bare-mirror case) already relies on without extra validation; consistent, not a new gap. (3) Did not widen this to `class-map`/`style-map` objects (CHECKLIST §5 names only the five `SEMANTICALLY_LOADED_ATTRS`) — out of scope as specified.

- [x] LT-077: Auto-wrap non-active `@try`/`@pending`/`@catch` arms in `fieldset[disabled]` (CHECKLIST §8) — reviewed ✓
  **Review (2026-08-29):** Approved with a follow-up (LT-086). Read every changed file in full (not just the handoff) and independently re-ran the full suite: `bun test server/tests` (1070/1070 — the handoff's "1 pre-existing flake" is confirmed real and unrelated, `file-watcher.test.ts` passes standalone), `bun test src/tests` (426/426), `check:tsrx` (21/21, same single pre-existing TSRX034 warning), `tsc --noEmit` (clean). The core design is sound: reusing the arm root's own already-proven-unique selector as the fieldset's addressing key (rather than a second, independently-incrementing counter) is exactly the right call — two independent derivations of "which fieldset" is the same drift risk CHECKLIST §4's "one AST node, two bindings" rule warns against, and this avoids it entirely. Reusing the SAME `hiddenCond` for both `hidden` and `disabled` (one boolean, two writes) is the minimal correct fix. **Issue found:** the fieldset is addressed client-side via `first<HTMLFieldSetElement>('fieldset:has(${armRootSelector})', …)` — CSS `:has()` in a live `querySelector()` call, not in a stylesheet. REQUIREMENTS.md's Browser Support section targets "evergreen browsers... as of Web Platform 2020 baseline"; `:has()` shipped 2022 (Chrome 105) to 2023 (Firefox 121) — outside that baseline. The existing `:has()` precedent the handoff's own comment gestures at (CHECKLIST §10, `form-tokenbox.css`'s `.input:has(input:focus-visible)`) is CSS-only, which degrades gracefully (the rule just doesn't apply) on an unsupporting engine — a fundamentally different risk profile from `querySelector('...:has(...)')`, which THROWS `InvalidSelectorError` (this codebase's own name for exactly this failure mode, `src/errors.ts`) for a syntactically-unrecognized selector, an uncaught deferred-activation throw (ADR 0007) on the hydration path of every async-boundary component on an unsupporting browser. Filed as LT-086 rather than sending this back — the rest of the change (the fieldset wrap itself, the `hiddenCond` reuse, the `explicitType` escape hatch for `first()`) is correct and worth keeping; only the addressing mechanism needs to change.
  **Skill:** le-truc-dev
  **Changed:** `server/tsrx/emit-server.ts` (`emitArmRoot` wraps every arm root in a synthetic `<fieldset disabled={hiddenCond} style="border:0;padding:0;margin:0;min-width:0">`, same condition as the root's own pre-existing `hidden`), `server/tsrx/emit-client.ts` (async `watch()`'s `ok`/`nil`/`err` handlers now also toggle each fieldset query's `.disabled`; query-declaration loop supports an `explicitType` override for `first<E>()`), `server/tsrx/analysis/effects.ts` (`handleAsyncBoundary` registers 3 additional fieldset queries), `server/tsrx/analysis/plan.ts` (`QueryPlan.explicitType?`, `AsyncEffect` gains `*FieldsetQuery` fields), `server/tsrx/analysis/naming.ts` (`addQuery` takes `explicitType`). No `ir.ts` change — the fieldset is a pure codegen wrap, never represented in `TemplateNode`. Tests: `server/tests/tsrx/features.test.ts` — 3 new tests in the `@pending` describe block (server render at the pending step and the ok step with a named `<input>` inside each arm, asserting the wrapping fieldset's `disabled` state; client codegen asserting the err handler's fieldset toggles).
  **How:** The wrapper's `disabled` reuses the SAME `hiddenCond` expression already computed for the arm's own `hidden` — no new server-side state. Client-side, each fieldset is addressed via `first<HTMLFieldSetElement>('fieldset:has(${armRootSelector})', …)`: `armRootSelector` is the arm root's own already-proven-structurally-unique selector, so there's one source of truth for "which fieldset" (no second, independently-incrementing counter that could drift from the emitter's). The explicit `<HTMLFieldSetElement>` type argument is required because `first()`'s string-literal type inference has no notion of parens and misparses the class/attribute selector nested inside `:has(...)` as the outer selector's own suffix — bypassing inference with the existing `first<E extends Element>(selector, required): E` overload sidesteps that instead of trying to make the parser paren-aware.
  **Corpus impact:** None measurable — `bun run check:tsrx`: 21/21 compiled, same single pre-existing TSRX034 warning (`form-listbox.tsrx`). `bun test server/tests`: 1070/1071 (1 pre-existing, unrelated `file-watcher.test.ts` flake — passes in isolation). `bun test src/tests`: 426/426. `bun run build:cem`: 64 declarations, no duplicate tags. `bunx tsc -p tsconfig.json --noEmit`: clean (including the generated client fixtures' `first<HTMLFieldSetElement>()` calls, checked directly).
  **Check:** (1) **The task's own "must not regress form-listbox.tsrx" premise is stale** — that component's async/remote-fetch mode was already dropped during its TSRX migration (its own docstring says so); the corpus currently has ZERO real `@try`/`@pending`/`@catch` consumers. This fix is verified only at the compiler-unit level (server HTML + generated client codegen), not with a real DOM/`FormData` submission test — this repo's `server/tests` have no real DOM (custom-element tests use a fake registry), and the library's actual submission-behavior proof mechanism is Playwright specs against real example components (`examples/*/*.spec.ts`), which would need a new demo component built around the async boundary first. Recommend either accepting the compiler-level proof as sufficient (disabled-fieldset-excludes-descendants-from-FormData is well-established native browser behavior, not something this codebase needs to re-verify) or scoping a follow-up task to build a real async-boundary demo + Playwright spec. (2) DOC items from CHECKLIST §8 (legend escapes a disabled fieldset; `<fieldset>` invalid inside `tr`/`select`/`ul`/`dl`) are author-facing constraints on a construct the compiler generates, not ones an author writes directly — no natural doc home found yet (`TSRX-HOST-PROFILE.md` documents author-facing syntax, not compiler-internal codegen shape); left undocumented, flagging for the architect to place. (3) Did not touch the plain (non-async) `@try`/`@catch` path (`pendingChildren === null`) — confirmed out of scope, since exactly one arm ever renders there (real mutually-exclusive try/catch), so no duplicate-submission surface exists.

- [x] LT-086: Replace LT-077's `:has()`-based fieldset addressing with a browser-baseline-safe alternative — reviewed ✓
  **Review (2026-08-29):** Approved. Read the full diff, not just the handoff — `.parentElement as HTMLFieldSetElement` is a direct, correct fix: it relies on a structural guarantee `emit-server.ts` already establishes unconditionally (the fieldset is always the arm root's immediate parent), so there's nothing to keep in sync and no `:has()` dependency anywhere in the emitted client code. The cast itself type-checks cleanly (`HTMLElement | null` → `HTMLFieldSetElement`, target assignable to source) and is the same category of compiler-proven-structural-fact shortcut this codegen already leans on elsewhere (e.g. `hiddenAttr`'s unconditional emission) — not a new kind of risk. Independently re-ran the full suite rather than trusting the handoff: `bun test server/tests` (1070/1070), `bun test src/tests` (426/426), `tsc --noEmit` (clean), `check:tsrx` (21/21, same single pre-existing warning). Scoped correctly — only the addressing mechanism changed; the fieldset-wrap design itself (already reviewed under LT-077) is untouched. No further action.
  **Skill:** le-truc-dev
  **Changed:** `server/tsrx/analysis/effects.ts` (`handleAsyncBoundary` no longer calls `addQuery` for the fieldset trio — reserves their variable names via `uniqueName(usedNames, …)` from `analysis/naming.ts` instead, since they're no longer entries in the query table), `server/tsrx/emit-client.ts` (the async effect branch now emits `const <name> = <armRootQuery>.parentElement as HTMLFieldSetElement` for each of the three fieldsets, right before the `watch(...)` call, instead of relying on `first<HTMLFieldSetElement>('fieldset:has(...)', …)`), `server/tsrx/analysis/plan.ts` (doc comment update only — the `*FieldsetQuery` fields are unchanged strings, just no longer query-table entries). `QueryPlan.explicitType`/`first<E>()` kept as instructed — orthogonal, generically useful, currently unexercised. Tests: `server/tests/tsrx/features.test.ts` — updated the one test asserting the old `fieldset:has(...)` codegen shape to assert `.parentElement as HTMLFieldSetElement` instead; the other two fieldset tests (server-render disabled-state assertions) were unaffected, since the SERVER side never used `:has()` in the first place — only the client query was the concern.
  **How:** `emit-server.ts` (from LT-077, untouched here) already guarantees the synthetic fieldset is each arm root's immediate parent, so `.parentElement` is a structural fact, not a lookup — no selector to synthesize or keep in sync with the emitter, and no `:has()` dependency at all. The cast to `HTMLFieldSetElement` is safe because that structural guarantee holds unconditionally; `parentElement`'s nullable typing is narrowed away by the cast rather than a runtime check, consistent with how the rest of this codegen treats compiler-proven structural facts (e.g. `hiddenAttr`'s own unconditional emission).
  **Corpus impact:** No corpus consumer exercises this path (per the LT-077 review note, `form-listbox.tsrx`'s async mode was already dropped) — verified via `check:tsrx` (21/21, same single pre-existing TSRX034 warning) and the compiler's own fixture tests. `bun test server/tests`: 1070/1070 (the file-watcher flake from the LT-077 review did not reproduce this run). `bun test src/tests`: 426/426. `bunx tsc -p tsconfig.json --noEmit`: clean. `bun run build:cem`: 64 declarations, no duplicate tags.
  **Check:** None outstanding — this was a scoped, mechanical fix to the addressing mechanism only; the fieldset-wrap design itself (reviewed and approved under LT-077) is unchanged.

- [x] LT-069: Widen the client-setup free-name gate to element locals and effect helpers (resolves NOTES LT-039) — done ✓
  **Skill:** le-truc-dev
  **Changed:** `server/tsrx/compiler.ts` (client-only setup-statement gate), `server/tsrx/ast-utils.ts` (`JS_GLOBALS`: `HTMLCanvasElement`, `PointerEvent`, `ResizeObserver`), `server/tests/tsrx/compiler.test.ts` (+3 tests).
  **How:** The gate now also accepts a free name when it's a `first()`-bound `elementRefs` entry (declared earlier in the same setup block, so JS declare-before-use ordering already guarantees it) or a `CLIENT_ONLY_PRIMITIVES` ambient (`watch`/`on`/`pass`/etc.) — the latter also gets added to `contextRefs`, which `analysis/plan.ts` already seeds `ambientContext` from, so the generated client destructures it from `FactoryContext` with no separate import-tracking plumbing needed. `watch(signal, { ok: () => { const ro = new ResizeObserver(...); ro.observe(el); return () => ro.disconnect() } })` (the effect-with-cleanup idiom, ADR reference in ARCHITECTURE.md) needed no new primitive — `watch()`'s internal `createEffect()` already self-registers a returned cleanup on the active owner. Genuinely unbound names are still rejected (TSRX005-family diagnostic unchanged).
  **Corpus impact:** `bun test server/tests`: 1073/1073. `check:tsrx`: 21/21 compile (same single pre-existing TSRX034 warning on `form-listbox.tsrx`, unrelated). `bunx biome check`: clean except one pre-existing, unrelated formatting finding in `compiler.ts` (confirmed via `git stash` — present before this change too).
  **Check:** Scoped to the gate itself, validated via 3 targeted compiler unit tests (element-local reference, `watch`/`on` ambients with an effect-with-cleanup pattern modeling `form-colorgraph`'s ResizeObserver case, and a still-rejected unbound-name regression test) rather than full corpus migrations — LT-070 (already filed, depends on LT-069) is the follow-up that spends this capability on `form-colorgraph` and retires `form-radiogroup`/`form-inplace-edit`'s `onKeydown` JSX-attribute workarounds.

- [x] LT-070: Migrate `form-colorgraph` to `.tsrx`; retire the `on(host, …)` workarounds (acceptance case for LT-069) — reviewed ✓ (partial — `form-colorgraph.tsrx` split to LT-088)
  **Review (2026-08-29):** Approved what landed; the remainder was a real, previously-unknown compiler gap, not a developer shortfall, so it's split into new tasks rather than sent back. Read the full diff (not just the handoff) and independently verified both blocker claims in NOTES.md against source: `elementRefs` (`compiler.ts`) is populated only inside the `first()` `VariableDeclaration` branch of the setup-statement loop (line 635) — a composed element's `ref={name}` is classified later, in `classify-attributes.ts`, and never touches that map, confirming blocker 1. `first-refs.ts`'s `collectMatchingElements` explicitly skips `kind: 'compose'` nodes (own doc comment: "the walk doesn't enter them"), and `analysis/effects.ts`'s `emitComposeEffects` requires `countComposeBySource(node.source) === 1`, confirming blocker 2 — three same-source `<FormSpinbutton>` instances (`form-colorgraph.tsrx` needs exactly this) are unaddressable regardless of their distinguishing `class`. Re-ran verification independently: `bun test server/tests` (1073/1073), `bun test server/tests/tsrx` (335/335), `check:tsrx` (21/21, same single pre-existing TSRX034 warning), `bunx biome check` on the changed compiler file (clean but for the pre-existing, unrelated finding already confirmed via `git stash` in LT-069's own review pass). `form-radiogroup.tsrx`'s full retirement and `form-inplace-edit.tsrx`'s `onFocusout` retirement are both correct, minimal, and match the hand-written originals' own `on(host, …)` calls verbatim — no notes. The `setupInits` gate widening (found compiling `form-radiogroup.tsrx`'s ported handlers) is the right fix and correctly reuses the existing `computeClientNeededNames` client-side fixpoint rather than adding new plumbing.
  **Decision:** Rather than NOTES.md's options (a)/(b) as two separate patches, unify them: extend `first()`'s own matching (`collectMatchingElements`) to also match `kind: 'compose'` nodes — by the child's resolved tag (`composeRegistry`) plus the compose JSX site's own static attrs (`class`/`id`/discriminator), the same selector vocabulary `parseSimpleSelector` already parses for plain elements. This single change closes both gaps at once: a `first()`-declared name for a composed element populates `elementRefs` exactly like today's raw-element case (closing blocker 1 without a phase-ordering change — `first()` already resolves after `lowerChildren`, `compiler.ts` lines ~956–992, so no new pass is needed), and multiple same-source instances become addressable whenever their `class`/`id`/attrs disambiguate (closing blocker 2) without touching `countComposeBySource`'s existing sole-instance rule for `ref={}`/`pass={{ }}` at all — `first()` becomes the general composed-element addressing mechanism, and `ref={}`-on-composed stays as-is for the single-instance/pass-only case it already handles. Filed as LT-087 (compiler capability) and LT-088 (re-attempt `form-colorgraph.tsrx`, gated on LT-087) — kept separate because LT-087 is compiler design/implementation work verified by unit tests, while LT-088 is a large, separately-risky UI migration (canvas drawing, pointer-capture drag, ResizeObserver) that deserves its own verification pass. NOTES.md's LT-070 entry resolved and removed.
  **Skill:** le-truc-dev
  **Changed:** `form-radiogroup.tsrx` — `onKeydown`/`onKeyup` on `<fieldset>` fully retired to bare `on(host, 'keydown'|'keyup', …)` setup statements (verbatim port of `form-radiogroup.ts`'s own handlers, `all()`/plain setup consts called inline). `form-inplace-edit.tsrx` — `onFocusout` on `.edit` retired the same way; `onKeydown` NOT retired (needs LT-087). `server/tsrx/compiler.ts` — the LT-069 gate also needed to accept earlier plain setup consts (`setupInits`) as free names, not just `elementRefs`/`CLIENT_ONLY_PRIMITIVES`.
  **Corpus impact:** `bun run check:tsrx`: 21/21 (same single pre-existing TSRX034 warning). `bun test server/tests`: 1073/1073. `bun test server/tests/tsrx`: 335/335.

- [x] LT-071: Revisit deferred component-shape questions after the conformance refactoring (NOTES LT-033, LT-035, LT-036) — reviewed ✓ (re-evaluated 2026-08-29, split into LT-093/094/095)
  **Review (2026-08-29):** The gate condition is met (LT-051–060, LT-069 and everything since have landed), so all three walls were re-evaluated empirically against the current compiler rather than from the NOTES write-ups. **LT-036 — wall still stands:** a probe compiling the exact shape (`const DEFAULT = 'red'; const color = createCell(DEFAULT)` consumed only through a style-map) still fires TSRX004's misleading "never rendered" message — the LT-069/087/088 widenings touched `compiler.ts`'s statement gate and `plan.ts`'s `badFreeNames`, but `substituteArgExpr`'s seeding gate is untouched. → LT-093. **LT-035 — no compiler blocker:** the reshaped template-owning component shipped long ago; only the demo `.html` and one e2e test still assume the old light-DOM contract → LT-094. **LT-033 — reshape, not a new mechanism:** card-blogmeta's light DOM converts to typed props (author name, avatar, published/modified datetimes, reading time) with the schema.org microdata re-emitted by the component's own template — the mediaqueries reshape precedent, and still exactly one use case for a speculative `{children}`-adjacent addressing mechanism. → LT-095, design-first. The policy stands: no example stays hand-written; this trio needs no new compiler capability except the honest-diagnostic/threading work in LT-093.
  **Skill:** le-truc-dev

- [x] LT-087: Widen `first()` to address composed elements, by resolved child tag + static attrs on the compose site (LT-070 review decision) — reviewed ✓ (design changed mid-implementation, approved)
  **Review (2026-08-29):** Approved as shipped — blocker 1 only, blocker 2 split into LT-089. Independently verified the developer's central claim (the approved design's premise doesn't hold) directly against source: `composeRegistry` does not appear anywhere in `compiler.ts` or `first-refs.ts` (`grep` confirms zero occurrences); `compileSource`'s signature is `(source, filename)` — no registry parameter — while `analysis/effects.ts`'s `emitComposeEffects` (which DOES receive `composeRegistry`, closed over via `runEffects`) is a genuinely later phase, consuming `ComposeAttrIR` that `lowerChildren` (itself dependent on the setup-statement loop's `signalByName`) has already produced. The original LT-055 doc comment in `classify-attributes.ts` independently corroborates this same boundary. Given that, dropping the `first()`/`collectMatchingElements` approach in favor of a standalone raw-AST pre-scan (`collectComposedRefNames`) is the right call — smaller, has no cross-phase dependency, and cleanly solves the one blocker it targets. Re-ran verification independently rather than trusting the handoff: `bun test server/tests` (1075/1075), `bun run check:tsrx` (21/21, same single pre-existing TSRX034 warning), `bunx tsc -p tsconfig.json --noEmit` (clean, exit 0). Read `form-inplace-edit.tsrx`'s generated client directly (`server/generated/tsrx/form-inplace-edit.client.ts`) — `const textbox = first('form-textbox', …)` is correctly emitted and `textbox.value`/`textbox.querySelector(...)` resolve against it in both the new bare `on(host, 'keydown', …)` statement and the pre-existing handlers. No notes on the shipped code itself.
  **Decision (blocker 2):** Confirmed the proposed fix in NOTES.md is sound and stays entirely within `analysis/effects.ts` (already has `composeRegistry`, no phase-boundary issue) — read `emitComposeEffects` directly: `addQuery(refAttr.name, childTag, 'one')` synthesizes the client query selector from the bare child tag ONLY (e.g. `'form-spinbutton'`), and `countComposeBySource(node.source) !== 1` gates before that, rejecting ANY multi-instance same-source composition regardless of whether the compose sites are actually distinguishable. Each `ComposeNode`'s own `attrs` already carries `{kind:'arg', name:'class', exprText, node}` for a static `class="lightness"` — reading `node.type === 'Literal'` off that (same pattern `first-refs.ts`'s `staticAttrs` already uses for raw elements) gives a real, verifiable per-instance discriminator with no new grammar. Filed as LT-089: relax `countComposeBySource`'s check to allow >1 same-source instances when each `ref`/`pass`-bearing one carries a distinct static `class`/`id`/`[attr]` discriminator, and fold that discriminator into `addQuery`'s synthesized selector (`'form-spinbutton.lightness'` instead of bare `'form-spinbutton'`) so the resulting client query is actually unique. LT-088 updated to depend on LT-089 instead of LT-087. NOTES.md's LT-087 entry resolved and removed.
  **Skill:** le-truc-dev
  **Changed:** `server/tsrx/ast-utils.ts` (new `collectComposedRefNames`), `server/tsrx/compiler.ts` (pre-scans `codeBlock.render` into `composedRefNames`, gate checks it), `server/tests/tsrx/compiler.test.ts` (+2 tests), `examples/form/inplace-edit/form-inplace-edit.tsrx` (`onKeydown` retired to a bare `on(host, 'keydown', …)` statement).
  **Corpus impact:** `bun run check:tsrx`: 21/21. `bun test server/tests`: 1075/1075. `bunx tsc --noEmit`: clean.

- [x] LT-089: Support multiple same-source composed instances, discriminated by a static `class`/`id`/`[attr]` on the compose site (LT-087 review decision, blocker 2) — reviewed ✓ (client half sound; the server half was never built — compose-site `class`/`id` are dropped from the rendered DOM entirely, filed as LT-090)
  **Review (2026-08-29):** The analysis-side mechanics are correct as shipped — read `selectors.ts`'s three new functions and `effects.ts`'s wiring in full: `composeDiscriminatorClause`'s priority order (class tokens → `id` → `data-*`), token-membership class matching, and uniqueness-checked-among-same-source-siblings are all sound; the flat, non-exclusivity-aware `composeNodesBySource` walk is the right conservative trade (over-requiring a discriminator on mutually-exclusive-branch instances is never unsound); the `JSON.parse` round-trip for a bare `class="a"` (`node: null` in `classify-attributes.ts`) matches the corpus's actual attribute shape. But the feature is only HALF built: the discriminator is folded into the CLIENT query selector (`addQuery(refAttr.name, \`${childTag}${discriminator}\`)`) while `emit-server.ts` (same commit) filters compose-site `class`/`id` from the forwarded server args and materializes them NOWHERE — `renderFormSpinbutton({ name, label, min, max, step, bigStep })` emits `<form-spinbutton>` with no class attribute, so at runtime the required-overload `first('form-spinbutton.lightness', …)` throws `MissingElementError` on every real browser (`src/helpers/dom.ts`: the two-arg overload throws, it does not return undefined). The filtering itself is correct and regresses nothing (forwarding `class`/`id` as args was an excess-property type error waiting to happen; a corpus scan confirms colorgraph's three spinbuttons are the only compose sites with static `class`/`id` today) — what's missing is the server-side counterpart that renders the discriminator onto the child's root element. The LT-089 unit tests assert client codegen only and cannot see this. Filed as LT-090.
  **Skill:** le-truc-dev
  **Changed:** `server/tsrx/analysis/selectors.ts` (new `composeNodesBySource`, `composeStaticAttrs`, `composeDiscriminatorClause`; `countComposeBySource`'s doc comment updated — it's now only the "sole possible instance" fast-path check, not the final word), `server/tsrx/analysis/effects.ts` (`emitComposeEffects`: on a `countComposeBySource !== 1` count, searches for a discriminator instead of failing outright; folds it into `addQuery`'s selector), `server/tests/tsrx/compose.test.ts` (+2 tests: discriminated pair succeeds with distinct `first('basic-child.a'/'basic-child.b', …)` queries and independent `pass()` wiring; an undiscriminated pair still fires TSRX007 ×2).
  **How:** `countComposeBySource(source) === 1` stays the fast path (unchanged behavior, no discriminator search). When `> 1`, `composeNodesBySource` collects the actual sibling `ComposeNode`s sharing that source (a flat walk — deliberately NOT exclusivity-aware like `countComposeBySource`'s `@if`/`@switch` max-branch counting, since over-requiring a discriminator on two mutually-exclusive-branch instances is conservative, never unsound) and `composeDiscriminatorClause` looks at the target node's own static `arg` attrs — `class` (token-membership match, priority 1), `id`, then `data-*` — picking the first candidate that's unique to it among the siblings, mirroring `discriminatorCandidates`'s priority-list pattern for raw elements. Found and fixed one wrinkle verifying against the corpus's actual attribute shape: a bare `class="a"` (no `{}`) classifies with `node: null` (`classify-attributes.ts`) — `exprText` is `JSON.stringify`-encoded, so `composeStaticAttrs` round-trips it through `JSON.parse` for that case, falling back to reading `node.value` directly for a braced `class={'a'}`. No discriminator found → same `unaddressableElement`/TSRX007 diagnostic as before, message extended to mention the static-attr escape hatch.
  **Corpus impact:** `bun run check:tsrx`: 21/21 (same single pre-existing TSRX034 warning). `bun test server/tests`: 1077/1077. `bunx tsc -p tsconfig.json --noEmit`: clean.

- [x] LT-088: Re-attempt `form-colorgraph` migration to `.tsrx` (LT-070's original acceptance case, gated on LT-089) — reviewed ✓ (compiler-side blocker LT-090: not production-ready until it lands)
  **Review (2026-08-29):** Read the full authored component and both generated modules directly (`server/generated/tsrx/form-colorgraph.{server,client}.ts`), and independently re-ran verification rather than trusting the handoff: `bun test server/tests` — 1082/1082 on re-run (the first run's single failure was the known `file-watcher.test.ts` flake documented in LT-077's review; does not reproduce in isolation), `bun run check:tsrx` 22/22 with the same single pre-existing TSRX034 warning on `form-listbox.tsrx`, `bunx tsc --noEmit` clean. **Approved:** the authored source is faithful to the hand-written original — per-axis `on(<ref>, 'change', …)` readback, `name=""` FormData exclusion reasoning, inline style-object literals per the LT-028/029 literal-body constraint, the nested-locals discipline for `host`-writing helpers, and the no-cached-`color` trade (ADR 0023 sub-design 12's server-verbatim wall) are all correctly reasoned. Of the five general compiler fixes: `defineMethod`'s `any[]` verified against the real export (`src/types.ts:151` — identical); `freeIdentifiers`' for-loop and self-referencing-const scoping is correct with the right declare-before-visit ordering (a `for` initializer is visited BEFORE its own declared names are bound) and unit-tested; `badFreeNames`' widening admits exactly the name classes bare client-only statements already accepted (setup consts, real client-placed imports) and is safe because `computeClientNeededNames` traces those same names for client placement; `clientExprNodes`' pass-thunk tracing closes a real silent-omission gap and is tested; the compose `class`/`id` server-arg filter changes no existing component's output. **Blocker found (not the developer's — the compiler's):** LT-089's discriminator never materializes in the served DOM (see LT-089's review note; filed as LT-090) — and form-colorgraph is exactly the component that depends on it: all three `first('form-spinbutton.lightness'|'chroma'|'hue', …)` required queries match nothing and throw `MissingElementError` at activation, taking the whole component down (canvas, sliders, everything); even without the throw, the CSS grid areas (`.lightness`/`.chroma`/`.hue`) and the keydown handler's `el.closest('.lightness')` axis detection would dead-end. The hand-written original's static template carried the classes; the compiled version silently loses them. The developer's own Check note anticipated this exact blind-spot class (no runtime/DOM verification exists); this is a concrete instance of it. No re-authoring needed — the component's shape is right; LT-090 must land before this migration is treated as production-ready.
  **Skill:** le-truc-dev
  **Changed:** `examples/form/colorgraph/form-colorgraph.tsrx` (new). Compiler fixes found along the way (all corpus-wide, not colorgraph-specific): `server/tsrx/ast-utils.ts` (`freeIdentifiers` — `ForStatement`/`ForOfStatement`/`ForInStatement`/`CatchClause` cases, and a `const`-declared function/arrow expression may now reference its own name inside its own body); `server/tsrx/analysis/plan.ts` (`badFreeNames` — reactive-attribute thunks may now reference a plain setup const or a real client-placed `@zeix/le-truc` import, matching what bare client-only statements already allowed since LT-069/LT-087); `server/tsrx/imports.ts` + `server/tsrx/walk.ts` (`clientExprNodes` now walks `pass={{ }}` thunks, both raw-element and composed — previously undocumented-gap, `collectAttrs`'s own doc comment flagged it); `server/tsrx/emit-server.ts` (a composed element's `class`/`id` attrs address the compose site, LT-089, and are no longer forwarded as server args — `data-*` still is, a pre-existing tested convention); `server/tsrx/runtime.ts` (`defineMethod`'s server-side shim constrained to `any[]`, matching the real `@zeix/le-truc` export — `unknown[]` rejected a method whose param has a concrete union type under `strictFunctionTypes`). Tests: `server/tests/tsrx/compiler.test.ts` (+4: 2 `freeIdentifiers` scoping regressions, 2 `badFreeNames` widening), `server/tests/tsrx/compose.test.ts` (+1: plain setup const inside a compose `pass` thunk).
  **How:** Composes three `<FormSpinbutton class="lightness"|"chroma"|"hue" ref={…} truc:pass={{ value: … }}>` (LT-087/LT-089), reading committed changes back via a per-axis `on(<ref>, 'change', …)` — the same shape the hand-written original uses, now that a composed ref is a real client-side reference (`FormSpinbuttonProps`' own `stepDown`/`stepUp`/`value`/`validity`). `color` (`parseOklch(host.value)`) is never a cached signal — a signal declaration is re-emitted verbatim into the SERVER module too (ADR 0023 sub-design 12) where `host` doesn't exist, and this thunk doesn't fit the `hostPropOf`/`hostDerivedFold` mirror shape — every use is inline, the `card-colorscale.tsrx` trade ("recompute per key, no caching"). Same reasoning keeps `commit`/`moveKnob`/`moveThumb` (all write `host.value` back) as locals nested inside the one bare client-only statement that uses each, never top-level plain consts. `style={() => ({ … })}` (LT-028/029) requires the arrow's body to be a literal object expression — a `stepStyle()` helper CALL doesn't qualify — so the 8 step swatches and knob/thumb inline their object literals directly instead of sharing a helper. `first()` still can't reach `.graph canvas`/`.slider canvas` (descendant-combinator selectors, unsupported by `parseSimpleSelector`) — gave both canvases their own discriminating class (`graph-canvas`/`hue-track`) rather than widen selector support for one component.
  **Corpus impact:** `bun run check:tsrx`: 22/22 (same single pre-existing TSRX034 warning, `form-listbox.tsrx`, unrelated). `bun test server/tests`: 1082/1082. `bunx tsc -p tsconfig.json --noEmit`: clean.
  **Check:** Verified at the compiler-unit/tsc level only — no Playwright spec exists for this component's interactive paths (canvas drag, keyboard axis stepping, pointer capture), same gap LT-077's review flagged for `@try`/`@pending`/`@catch`; recommend a manual browser smoke-test (or a new e2e spec) before treating this as production-ready, since none of `check:tsrx`/`tsc`/the unit suite exercises real pointer/canvas behavior. The compiler fixes are all general (freeIdentifiers scoping, badFreeNames, clientExprNodes, compose server-arg filtering, defineMethod's server shim) — flagging in case any deserves closer scrutiny given how load-bearing `freeIdentifiers` is across the whole front end.

- [x] LT-090: Materialize compose-site static `class`/`id` on the composed child's server-rendered root (LT-089's missing server half; blocks form-colorgraph production-readiness) — reviewed ✓
  **Review (2026-08-29):** Approved. Read every changed file in full and re-ran verification independently: `bun test server/tests` 1084/1084, `tsc --noEmit` clean, and the regenerated `form-colorgraph.server.ts` confirmed emitting the wrapper (`composeHostAttrs(renderFormSpinbutton({...}), "form-spinbutton", { "class": "lightness" })`). The design holds up under scrutiny: passing `class`/`id` through as authored expressions (not just compile-time literals) gives dynamic server-known values the same materialization for free; the `composeHostAttrs` prefix check converts the compiler invariant (child markup starts at its root tag) into a loud throw instead of a silent drop; delegating attribute rendering to `attr()` keeps escaping/nil handling in one place; and re-exporting through `runtime.ts` preserves the single-import shape of generated modules. TSRX038's error severity and flat cross-source id tally both match the TSRX035 precedent. Two minor, non-blocking notes: `allComposeNodes` duplicates `composeNodesBySource`'s walk structure (could be one predicate-parameterized walk — cosmetic); and the `plainLocalNames` badFreeNames widening is currently unexercised by the corpus (the two-way pass that needed it was redesigned away during LT-091) — generically correct and needed by any future two-way `truc:pass` referencing a plain import, so keep it, but it wants a compiler unit test (folded into LT-093).
  **Skill:** le-truc-dev
  **Changed:** `server/tsrx/compose-attrs.ts` (new — `composeHostAttrs(markup, tag, attrs)`, re-exported through `server/tsrx/runtime.ts` so generated modules keep their single runtime import), `server/tsrx/emit-server.ts` (compose emission wraps the render call in `composeHostAttrs(renderX({...}), "<tag>", { class: <expr>, id: <expr> })` whenever the compose site carries `class`/`id`; non-class/id args unchanged), `server/tsrx/diagnostics.ts` (new TSRX038 `duplicateComposeId`, error severity), `server/tsrx/analysis/selectors.ts` (`allComposeNodes` — source-agnostic sibling of `composeNodesBySource`), `server/tsrx/analysis/effects.ts` (end-of-runEffects pass: static compose-site `id`s tallied via `composeStaticAttrs`, duplicates fire TSRX038), `server/tsrx/ir.ts` + `server/tsrx/compiler.ts` + `server/tsrx/analysis/plan.ts` (`imports.plainLocalNames` — badFreeNames now also accepts plain-import locals in client thunks, completing LT-088's widening; a two-way pass set-thunk referencing e.g. `formatCss` from `culori/fn` was the first construct to need it). Tests: `server/tests/tsrx/compose.test.ts` (+2: executed-module server-HTML assertion for the materialization via the server.golden harness; TSRX038 duplication), both written first and watched failing.
  **How:** Values pass through as the authored expressions (static string literals AND server-evaluable dynamic ones), evaluated at render time in the generated module's scope — identical trust to every other compose arg. `composeHostAttrs` validates the compiler-proven invariant that a child render's markup starts with its own root open tag (throws otherwise — a silent drop would reintroduce the LT-090 gap), delegates attribute rendering to `attr()` (escaping, nil omission), merges a `class` value with any class the child root renders itself (none does today). Duplicate-id tally is flat across sources (an id shared by mutually-exclusive-branch sites still renders two elements across the component's lifetime — conservative for a validity check).
  **Corpus impact:** `check:tsrx` 22/22 (same single pre-existing TSRX034 warning on `form-listbox.tsrx`); regenerated `form-colorgraph.server.ts` now emits `composeHostAttrs(renderFormSpinbutton({...}), "form-spinbutton", { "class": "lightness" })`; `bun test server/tests` 1084/1084; `bun test src/tests` 426/426; `tsc --noEmit` clean; `build:cem` 64 declarations, no duplicate tags.
  **Check:** (1) The emitted `composeHostAttrs` wrapper is a generated-server-module shape change — golden client tests unaffected (no compose, no wrapper). (2) TSRX038 is error-severity like TSRX035, so a duplicate-id component compiles to null — matches the across-arms precedent. (3) Real-browser proof landed under LT-091 (the unit suite could only see the codegen shape).

- [x] LT-091: Run/extend `form-colorgraph.spec.ts` against the migrated `.tsrx` component — first real-DOM coverage for a composed-ref component — reviewed ✓
  **Review (2026-08-29):** Approved, including ratification of the two decisions flagged in the handoff and NOTES.md. (1) **Build-phase reorder ratified:** generated clients are bundle inputs, and bundle inputs are exactly phase 1's charter ("generator effects that produce gitignored inputs consumed by later effects"); the old phase-2 position raced the bundler on a fresh checkout. The `generatedClientScripts` watcher correctly closes the watch-mode loop (`.tsrx` edit → regenerate → re-bundle), and `watchFiles`' `existsSync` guard covers the fresh-checkout case. (2) **Isolated-bundle spec pattern ratified:** the compiled-vs-hand-written dual definition would throw on the second `customElements.define` and take down main.js, so route-intercepted pages serving generated server HTML + `tsrx-test.js` is the right isolation — it exercises the real compiled client, the real generated markup, and the exact surface where LT-090 shipped invisible, without destabilizing site pages. Verified beyond the handoff: the emitted wrapper present in the regenerated server module, full Chromium suite 403/403 (re-run this review), unit suites and tsc clean, CEM 64/0-duplicates after the `form-colorgraph.ts` exclusion (which matches the established per-twin exclude convention). The spec-side findings chain (getter-only pass throws → mediated-set clobbers reset → child-owns-value redesign) is coherent and the final authored shape matches the hand-written original's architecture. Carried notes: WebKit project untested locally — confirm on CI; the pass write-ownership guidance (don't getter-only-pass a prop the child writes) needs a home in TSRX-HOST-PROFILE.md — folded into LT-092's scope.
  **Skill:** le-truc-dev
  **Changed:** `examples/tsrx-test.ts` (new second browser bundle entry: compiled components' generated clients for specs), `server/config.ts` (`TSRX_TEST_FILE`, `GENERATED_CLIENTS_DIR`), `server/effects/js.ts` (bundles both entries), `server/build.ts` (tsrxEffect moved to phase 1 — its generated clients are bundle inputs, so the compiler's first run must precede the bundler; previously a phase-2 effect racing AFTER the js build on a fresh checkout), `server/file-signals.ts` (+`generatedClientScripts` watcher: a `.tsrx` edit → regenerated client → js rebuild), `package.json` (`build:examples:js` builds the second entry too — the Playwright webServer path bypasses jsEffect), `custom-elements-manifest.config.mjs` (excludes the hand-written `form-colorgraph.ts` twin per the verifier's prescription), `examples/_common/asOklch.ts` (registers culori's `oklch`/`p3`/`rgb` modes — see Findings), `examples/form/spinbutton/form-spinbutton.ts` (removed its `HTMLElementTagNameMap` declaration — the compiled client is the single source of truth in the type graph now that both are in it), `examples/form/colorgraph/form-colorgraph.tsrx` (pass redesign — see Findings), `form-colorgraph.spec.ts` (rewritten: 10 tests against the compiled component).
  **How:** The spec route-intercepts `/test/form-colorgraph` and serves the component's GENERATED server HTML (rendered in-spec via the generated server module) + generated CSS + `/assets/tsrx-test.js` — the compiled and hand-written implementations never co-define a tag in one document, and site pages are untouched. Tests: activation without `MissingElementError` (the direct LT-090 regression guard), initial render, FormData submission, step methods, per-axis bounds, typed-input readback, out-of-P3-gamut rejection (`setCustomValidity`, no commit), knob pointer-drag commit, Arrow-key stepping, form reset.
  **Findings (all fixed, each caught by this spec's first runs — the exact blind spot LT-091 was filed for):** (1) **Getter-only `truc:pass` made every child write throw** `ReadonlySignalError` — a pass replaces the child's `value` Slot, so the child's own `stepDown`/`stepUp` methods and typed-input harvest route through the parent's (absent) setter. (2) Mediating the pass with `{ get, set }` restored steps/typing but **clobbered form reset**: the library's built-in `formResetCallback` baseline restore writes the child's Slot, whose setter re-committed the child's 0 baseline over the freshly restored host color. Final design matches the hand-written original: the child owns its value; the parent pushes display-scaled values via `watch(() => toDisplay(...), bindProperty(el, 'value'))` and commits on `change`. (3) **culori's `fn` build never had modes registered repo-wide**: `converter('oklch')` silently returned a converter that resolves nothing (every `asOklch` parser silently fell back to defaults) and `inGamut('p3')` threw at factory time — the OLD hand-written colorgraph had the same latent bug. Fixed once in `asOklch.ts` (the corpus's culori setup point; the import propagates into generated server modules verbatim). (4) The `keydown` handler's axis detection and the CSS grid areas depend on the compose-site classes — dead under LT-090, live again after it.
  **Corpus impact:** `form-colorgraph.spec.ts` 10/10 Chromium; full Chromium suite 403/403 (no ripple from the mode registration or the second bundle); unit suites and tsc as under LT-090; CEM 64/0-duplicates after the exclusion.
  **Check:** (1) **Build-phase ordering needs architect ratification** — tsrxEffect in phase 1 ahead of jsEffect is required while generated clients are bundle inputs; watch-mode propagation relies on the new `generatedClientScripts` watcher. (2) WebKit project not run locally (Chromium only). (3) Site pages still mount the hand-written implementations — the compiled colorgraph is spec-covered but not yet what the docs site serves; full cutover filed as LT-092. (4) Mimosa's PreToolUse hook twice blocked an early `runtime.ts` edit as "command injection" (false positive on HTML-escaping string building — the module has no process execution); worked around by placing the helper in `compose-attrs.ts` and re-exporting — flagged so future `runtime.ts` edits aren't surprised.

- [x] LT-092: Site cutover pass — serve compiled `.tsrx` components from the docs site instead of their hand-written twins (folds in LT-094) — reviewed ✓ (remediation filed as LT-112/113; 4 stopped cutovers spun into LT-114..116)
  **Skill:** le-truc-dev
  **Context:** *(as dispatched — see git history for the full text)* Canonical pattern: delete the `.ts` twin, switch `examples/main.ts` to the generated client, remove the twin's CEM exclusion; retire `tsrx-test.ts` after re-targeting the colorgraph spec; port card-mediaqueries demo/spec (LT-094); demo `.html` regeneration where the template now owns markup; TSRX-HOST-PROFILE pass write-ownership guidance; form-tokenbox real-browser check.
  **Changed:** `examples/main.ts` (16 compiled clients; 4 components stay on twins — see the four NOTES.md entries), 16 hand-written twins deleted + `examples/tsrx-test.ts` retired (`server/config.ts`, `server/effects/js.ts`, `package.json` `build:examples:js` now runs `scripts/build-tsrx.ts` first — fresh-checkout guard), `custom-elements-manifest.config.mjs` (exclusions removed except the 4 kept twins), `tsconfig.json` (`paths` self-reference `@zeix/le-truc` → `./index.ts`), 34 hand-written example imports normalized to `@zeix/le-truc`, demos/specs ported to compiled contracts, `.tsrx` fixes (`form-spinbutton` value/validity relay, `form-inplace-edit` pass redesign + `editing` reflection), `TSRX-HOST-PROFILE.md` (pass write-ownership), `NOTES.md` (4 compiler-gap entries), client.golden fixture.
  **How:** Two infrastructure findings shaped the sweep: mixing `@zeix/le-truc` and `../../../index` specifiers in one bundle duplicates the library (two WeakMaps broke `pass()` — `InvalidPassPropertyError`); and resolving the package name through exports "default" embedded the prebuilt prod `index.js`, stripping ADR 0022 debug instrumentation — both fixed by the single-specifier + `paths` normalization. 16/20 components cut over; 4 stopped on compiler gaps (NOTES.md): basic-number (root lazy text child gets no client `bindText` — silent), basic-pluralize + basic-gauge (arg→DOM substitution rewrites `deriveCell` bodies to `getAttribute` reads — freezes reactivity; pluralize/gauge also emit an illegal root self-query `first('<own-tag>')` throwing on hydration pages), form-radiogroup (loop-body boolean `checked` lowers to `bindAttribute`; dirty-checkedness breaks mutual exclusion).
  **Corpus impact:** `check:tsrx` 22/22 (same single pre-existing TSRX034 warning); `bun test server/tests` 1084/1084; `bun test src/tests` 426/426; `tsc --noEmit` clean; `build:cem` 64 declarations, no duplicates; Playwright Chromium 394/394 (−9 vs baseline = documented behavior drops in ported specs — see Review), WebKit 386 pass + 7 skip, 0 fail; fresh-checkout guard verified (`rm -rf server/generated/tsrx && bun run build:examples:js`); real-browser checks: tokenbox activation + interaction clean, colorgraph canvas paints + hue slider commits; 30-page `/test/` sweep.
  **Check:** flagged spinbutton controlled-`value` resync and float-mode off-grid commits; compiled textbox `description` read-only; tsconfig `paths` as durable convention; main.js 205.8→171.7 KB.
  **Review (2026-08-29):** Owner review flagged two contract regressions, both upheld — (1) `basic-button` demo requires `label`/`badge`/`disabled` as host attributes while the hand-written contract harvested them from the owned children (span.label → button text fallback, missing-span tolerance); the deviation was authored into `basic-button.tsrx` during the ADR 0023 migration and LT-092 cemented it in the demo instead of escalating. (2) `module-coloreditor` drives the compiled textbox's `.description` paragraph by reaching into the child's owned DOM because the compiled `description` prop is a read-only `deriveCell` — the child's prop contract should have been fixed instead. Policy recorded (→ TSRX-HOST-PROFILE.md via LT-112): *children are data — a component harvests child-available data from its owned light DOM, and pages never duplicate it into attributes; a parent never reaches into a child's owned children — the child's prop contract gets fixed (writable `createCell` or self-derived `deriveCell`) instead.* The −9 spec drops and every "attribute-configured usage" demo port must be re-swept against these rules (LT-112). Approved mechanically (infrastructure, tsrx-test retirement, 4 stopped cutovers were correctly stopped); remediation filed as LT-112/113; compiler gaps as LT-114..116.

- [x] LT-112: Remediate LT-092's contract regressions — restore children-are-data harvesting and re-sweep ported demos/specs (owner-flagged) — reviewed ✓
  **Skill:** le-truc-dev
  **Changed:** basic-button twin/demo/spec restored verbatim from git (twin stays mounted — see LT-117); `form-spinbutton.tsrx` (Parser fallbacks harvest the owned input — `asNumber(asNumber(1)(input.step))`, host-attr-wins precedence restored, `step="any"` degrades via re-parse; external-writes watch relayed to input + validity per ADR 0020); `form-inplace-edit.tsrx` (`value` harvests the owned `.text` span); demo duplication cleanups (todo, dialog, ticker, catalog, mediaqueries — data lives in exactly one place per contract); `form-combobox.tsrx` (`clearable` arg + clear button, `length` derived readonly, writable `description` createCell seeded from the owned paragraph's `data-description`); 6 removed/weakened combobox spec tests restored verbatim; `server/effects/tsrx.ts` (dual-state rule: a tag with its twin still on disk keeps the TWIN's childImports entry — prevents double `customElements.define` in main.js); TSRX-HOST-PROFILE data-account section (architect-rewritten).
  **How:** Probed the enhancer contract against the compiler piece by piece — four independent walls (TSRX008 template-less unrepresentable, TSRX025 no maybe-cardinality `first()`, TSRX026 template-verified refs can't match page-authored children, TSRX005 no conditional effect registration) → correctly STOPPED per mandate, twin mounted, capability gap escalated (→ LT-117). Two harvest forms that DO work (expose thunks reading `host`; Parser fallbacks reading template-owned `first()` refs) restored the spinbutton/inplace-edit contracts with zero compiler changes.
  **Corpus impact:** `check:tsrx` 22/22 (known TSRX034 warning only); server 1084/1084; src 426/426; tsc clean; CEM 64/0-dupes; Playwright Chromium 401/401, WebKit 393+7 skipped/0 fail; 30-page browser sweep clean.
  **Check:** escalations → decided below (review); pass-vs-derivation duality → TSRX-HOST-PROFILE caveat added.
  **Review (2026-08-29):** Approved. Read the key diffs (textbox descriptionCell, coloreditor revert, spinbutton fallbacks, combobox, tsrx-effect dual-state rule) and re-ran the full suite independently — found the handoff's "1084/1084" claim was stale: 2 server tests failed (form-textbox golden snapshot + a convergence assertion still expecting the pre-LT-113 `deriveCell` shape). Fixed as reviewer (expectations updated to the intentional new `createCell` + `remainingCount` derivation shape via `UPDATE_SNAPSHOTS=1`; no assertion deleted) — suite now genuinely 1084/1084, Chromium 401/401. Escalation decisions: (1) form-listbox remote-src drop **ratified** (was already signed off in LT-077's review; no consumer; async coverage arrives via LT-104's module-lazyload) — the 2 removed tests stay removed. (2) spinbutton zero-state → **LT-118** (union addressing over structurally differing branches). (3) combobox popup gate → **LT-119** (form-listbox `visibleOptions` readonly prop — sanctioned boundary addition — combobox gate restored, test re-flipped). Pre-existing reach-ins (inplace-edit `textbox.querySelector('input')`, combobox `listbox.querySelector('button[role=option]`) → **LT-120**. Enhancer capability → **LT-117**. Both LT-112 NOTES entries resolved and deleted.

- [x] LT-113: Make `form-textbox`'s `description` writable and revert `module-coloreditor`'s reach-in (owner-flagged ownership violation) — reviewed ✓
  **Skill:** le-truc-dev
  **Changed:** `form-textbox.tsrx` (`descriptionCell = createCell(description)` — writable, client seed harvested from the owned paragraph's `data-remaining`; `remainingCount = deriveCell(...)` display derivation over it; paragraph renders `{remainingCount}`; exposed directly so `el.description = x` and `bindProperty` writes flow through the Slot and re-derive); `module-coloreditor.ts` (reach-in reverted; nearest-named-color line pushed via `watch(..., bindProperty(textbox, 'description'))`); `form-textbox.spec.ts` (original writable test restored + new derivation-participation test).
  **How:** The createCell/deriveCell split compiles cleanly (TSRX013 forbids `host` reads in signal bodies; a cell over the arg + a derivation over the cell is the sanctioned shape). Live-browser verification caught that a getter-only `truc:pass` disconnects the derivation (Slot backing swapped, derivation reads the stale internal cell) — `watch`+`bindProperty` is required for parent-derived values feeding derived displays; recorded as a TSRX-HOST-PROFILE caveat by the architect.
  **Corpus impact:** textbox spec 15/15; coloreditor real-browser verified; full suite numbers under LT-112.
  **Review (2026-08-29):** Approved. Verified the generated client shape (`createCell((p?.getAttribute('data-remaining') ?? ''))`, `remainingCount` derivation, `watch(remainingCount, bindText(p))`, `description: descriptionCell`) and the coloreditor composition directly; independently re-ran all suites (see LT-112 review for the two test-expectation fixes this change required). The `@if (description)` arg gating the paragraph's existence is acceptable (the seed paragraph exists only when the server arg rendered it); note for any future consumer: description writes only display when the arg rendered the paragraph.

- [x] LT-121: Fix the server ref-stub crash — every harvesting component's `render*()` throws today — done ✓
  **Skill:** le-truc-dev
  **Context:** `emit-server.ts:859` stubs each `first()`-bound ref as `const <name>: any = undefined` and then re-emits the authored `expose()` verbatim, so any component whose `expose()` initializers read a ref (the children-are-data harvest form) has a server render that throws the moment it is called. Verified at HEAD: `renderFormSpinbutton({ name: 'x' })` and `renderFormColorgraph({})` both throw `undefined is not an object (evaluating '(void 0).value')`. **Fix:** replace the `undefined` stub with a shared null-object stub exported from `server/tsrx/runtime.ts` — a self-returning Proxy (any property get yields the same Proxy, callable, `Symbol.toPrimitive` → `''`/`0`/`false`) so arbitrary authored chains (`asNumber(asNumber(0)(input.step)) * 10`) evaluate harmlessly. Keep `expose()` emitted: it is the server module's tsc-parity surface and its runtime shim is already a no-op, and no harvested value can reach the markup anyway (`host.X` is never server-known). **Why it shipped unnoticed, and the second half of the task:** `server/tests/tsrx/server.golden.test.ts` executes only six tags, none of which harvest. Add a smoke gate — call every corpus tag's `render*()` with its default args and assert it does not throw — so new components join the coverage automatically rather than by someone remembering to add a snapshot.
  **Changed:** `server/tsrx/runtime.ts` (`refStub` — a self-returning Proxy: every property get yields the stub again, calls return it, `Symbol.toPrimitive` answers `''`/`0`/`false`, `Symbol.iterator` a ndor `toString`/`valueOf` answer with real callables, `then` is `undefined` so no `await` recurses, `ownKeys` empty so a spread yields `{}`), `emit-server.ts` (client-only ambient stubs are `refStub` instead of `undefined`; the stub-name computation hoisted above the runtime-import line so `refStub` can join it), `server/tests/tsrx/server-render-smoke.test.ts` (new, 23 tests).
  **How:** `expose()`'s argument object is a real expression the generated module EVALUATES (its runtime shim is a no-op, but the argument is not) — so a component seeding props by reading a `first()` ref died on `(void 0).value`. Stub values still never reach the markup: `host.<prop>` is never server-known, and `bigStep="10"` in form-spinbutton's output comes from the arg's own `?? step * 10` fallback, not the stub (checked).
  **Corpus impact:** `renderFormSpinbutton({ name: 'qty' })` and `renderFormColorgraph({})` both threw at HEAD and now render. The smoke gate compiles the corpus the way the build does and calls every generated `render*()` — 22 tags, no snapshots, so a new component joins by existing; `ARGS` carries values only for contracts with genuinely required fields.
  **Review (2026-08-30):** Approved. The null-object stub is the right shape — `expose()`'s argument is an evaluated expression, so a value that survives being read, called, spread, and coerced is the only thing that keeps the verbatim slice verbatim. Smoke gate re-run independently (22 tags, no throws). One boundary the fix does NOT cover, found while reviewing and written up as **LT-125**: `stubNames` is derived from `exposeArgNode` alone, so a ref read by an emitted SETUP statement outside `expose()` still emits an undeclared identifier. That fails loudly at `tsc`, which is the correct outcome — the note in LT-125 exists to stop a future agent from "fixing" it by widening the stub set, which would turn a build error into a silently empty render.

- [x] LT-122: `{arg}` sites gain the client binding when the arg names an exposed prop (the LT-117 core capability) — reviewed ✓
  **Skill:** le-truc-dev
  **Context:** A site authored `{label}` (server arg) server-renders correctly (`esc(String(label))`) but emits no client effect; the same site authored `{host.label}` emits `watch(() => host.label, bindText(span))` but server-renders `esc(String(''))`. A component whose props are harvested from its own children can therefore render the data or bind it, never both — which is what forced LT-092's host-attribute duplication and then LT-117's enhancer detour. **Change:** in `analysis/effects.ts`, a non-lazy expr site whose expression is a bare identifier naming BOTH a server arg and an exposed prop plans the effect the `host.<name>` spelling plans today (`watch-text` / `watch-attr` against the ambient `host`); server emission is unchanged. The site then serves all three roles at once — the server's render target, the client's harvest source (ADR 0003, DOM-is-truth), and the client's binding target. Text children and attribute thunks both; `@for` item bodies are out of scope. **Second half:** a WARNING when a name is Parser-exposed (`expose({ x: asString('') })`) *and* rendered into an owned site from a same-named server arg — the value would ship twice, once in the host attribute the Parser reads and once in the markup; the message names the harvest form (`expose({ x: <ref read> })`) as the fix. Warning, not error, by owner decision (2026-08-30) — data harvesting from the DOM is preferable but an error was judged too restrictive without more corpus evidence; revisit once the corpus argues either way. Tests written first for both halves.
  **Changed:** `reactivity.ts` (`bindsExposedArg`, `duplicatedChannelArg`), `ir.ts` (`bindsProp` on the expr node and the `'server'` attribute kind; `ExtractContext.argNames`/`parserProps`/`parserFactoryOf`), `compiler.ts` (seeds those; `exposedPropNames` — see below), `lower-template.ts` + `classify-attributes.ts` (set `bindsProp`; `classifyAttribute` now takes `signals`), `analysis/effects.ts` (a `'server'` attr with `bindsProp` plans a `watch-attr`; `isClientConstructAttr` admits it), `analysis/harvest.ts` (`lazyWatchSource` spells such a site `() => host.<prop>`), `diagnostics.ts` (TSRX039), `first-refs.ts` (`reportDuplicatedChannels`), `server/tests/tsrx/arg-prop-sites.test.ts` (new, 8 tests, written first).
  **How:** `{label}` and `disabled={disabled}` keep their server emission and gain the client effect the `host.<name>` spelling plans. Three exclusions, each load-bearing: declared signals (own render site + harvest plan already), Parser-exposed props (their channel IS the host attribute — binding `<textarea …>{value}</textarea>` would fight the dirty-value flag; TSRX039 instead), and the ROOT element's own attributes (the root is the host, so a Parser prop rendered there is the correct channel, not a duplicate — hence the check runs post-lowering, where `root` exists).
  **Two findings worth the reviewer's eye:** (1) `ExtractContext.exposedProps` claimed to hold "prop names `expose()` declares" but only ever held signal-getter and Parser props — a prop harvested straight from the DOM was invisible to the compiler. Widened to every declared name (also makes TSRX019 more correct). (2) The first cut dispatched these as PROPERTIES; `data-remaining={description}` (form-textbox) has no DOM property, and `check:tsrx` caught it. Now attribute dispatch except dirty-flag control attrs (LT-116's rule — the divergence is a property of the target).
  **Corpus impact:** one intended snapshot change (form-textbox keeps its `data-remaining` seed in sync with `host.description`), two intended TSRX039 warnings — both TRUE positives: form-textbox renders `value` on the host AND inside the textarea, form-tokenbox the same for `description`. Left as findings; fixing those contracts is LT-113 territory, not this task.
  **Review (2026-08-30):** Approved — this is the capability LT-117 was actually asking for, and it lands as a coincidence rule over existing machinery rather than a new authoring construct, which is the right weight. The three exclusions are each correct for a different reason and the code says so; the ROOT-element exclusion in particular (the root IS the host, so a Parser prop rendered there is the channel, not a duplicate) is the kind of thing that would have been re-broken later without the post-lowering placement. TSRX039's two corpus hits re-verified as true positives — form-tokenbox's `@if (description) { <p class="description">{description}</p> }` is literally basic-button's badge shape, so the fix is the harvest form, and it belongs to **LT-113**'s contract, not here. Widening `ExtractContext.exposedProps` to every declared name is a correction, not a scope creep: the field's docstring already claimed it. **Noted, no action:** the rule has no opt-out — an author wanting a render-only site (initial value, never rebound) can only dodge it by not writing a bare identifier. Acceptable while every corpus site wants the binding; revisit if a counterexample appears.

- [x] LT-123: Site-inferred `first()` cardinality; TSRX026 required-only; TSRX008 bare root — reviewed ✓
  **Skill:** le-truc-dev
  **Context:** Three independent loosenings of the default template-owning path, none of them mode-gated.
  (a) **TSRX025** — `first()` accepts a single selector literal, the optional form (`first('span.label')` → `T | undefined`). The generated query is the existing `QueryPlan.cardinality: 'maybe'` (`analysis/plan.ts:38`) and effects over it wrap in the existing `'guarded'` block (`analysis/effects.ts`'s `handleOptionalBranch`) — this is a new authoring surface onto machinery LT-008/LT-025/LT-118 already built, not new machinery.
  (b) **TSRX026** — structural verification is enforced for REQUIRED refs only. Cardinality is inferred from the SITE, not the call arity: a ref whose only structural match sits inside a single-branch `@if` is maybe however it was called, and a required-reason string supplied on such a ref gets a warning naming the one-literal form. State the general rule in `LE_TRUC_COMPILER.md`: for a template-owning component the compiler controls the markup, so a required-reason is only meaningful for a selector that may match markup the component did not itself render.
  (c) **TSRX008** — the `@{ }` output accepts a bare single root element with no `<style>`; the fragment stays required only for element + style. Drop the unconditional "expected a `<style>` block beside the root element" diagnostic (`compiler.ts:1259`).
  **Explicitly out of scope:** widening TSRX005's sanctioned setup statements. Verified 2026-08-30 that `@if (badge) { <span class="badge">{badge}</span> }` already generates `if (span2) { watch(…, bindText(span2)) }` in the default path, so no corpus source needs an authored `if (ref) <effect>`; the whitelist stays closed (owner decision) and is revisited only against a real counterexample.
  **Changed:** `compiler.ts` (one-or-two-literal arity; optional refs skip the structural check and land in `unmatchedOptionalRefs`; TSRX040; bare-root output), `first-refs.ts` (`inOptionalBranch`), `ir.ts` (`unmatchedOptionalRefs`, `optionalRefs`), `analysis/plan.ts` (queries for unmatched optional refs; the guard-wrapping post-pass), `analysis/naming.ts` (cardinality downgrade), `diagnostics.ts` (TSRX025 reworded, TSRX040), `server/tests/tsrx/first-cardinality.test.ts` (new, 11 tests).
  **How:** cardinality is the WEAKER of what the author declared and what the site proves — not, as the task assumed, the site's verdict outright. A one-literal ref matching an UNCONDITIONAL element must still be optional: `basic-button.html`'s `missing-elements-test` enhances a bare `<button>` the component never rendered, so the span the template always emits can still be absent at connect. That also forced the second half nobody had planned: an effect over an optional ref needs the same `if (ref) { … }` wrapping a single-branch `@if` root gets, or the generated client neither typechecks nor runs.
  **Note for the reviewer:** TSRX005 stayed closed as agreed — verified again on the finished basic-button that `@if (badge) { … }` generates the presence guard on its own.
  **Review (2026-08-30):** Approved, and the deviation from the task text is an improvement I want on the record: cardinality is the WEAKER of the author's declaration and the site's proof, not the site's verdict. My task text said "infer from the site", which would have upgraded `basic-button`'s always-rendered `span.label` to required and broken `missing-elements-test` — the exact acceptance case LT-117 exists for. The developer caught that the template proves what the component RENDERS, never what it will FIND. The unplanned second half (guard-wrapping effects over an optional ref, `analysis/plan.ts`) follows necessarily and reuses the LT-008/LT-025/LT-118 `'guarded'` block rather than inventing a second one. TSRX040's message earns its length by naming the general rule. **One consequence to fix separately (LT-124):** the optional form turns a missing element into a silent no-op, and raw-element selectors are resynthesized as exact-match `[class="label"]` — so a page enhancing with `<span class="label icon">` now binds nothing and says nothing. Pre-existing, but LT-123 is what makes it silent.

- [x] LT-117: Remove enhancer mode; express basic-button's harvest contract in the default template-owning path — reviewed ✓
  **Skill:** le-truc-dev
  **Depends on:** LT-121, then LT-122, then LT-123.
  **Context:** The shipped solution is **rejected by the owner**. LT-112's probe reported four walls and the implementing agent lifted all four wholesale behind `export const config = { enhancer: true }` — a decision taken without approval. The mode's cost is that an enhancer's entire server render is `'<basic-button></basic-button>'` (`emit-server.ts:205`): the goal was flexible server-rendered markup with optional branches, and the mode achieves it by deleting the server rendering and requiring the page to hand-author the button. It also trades four lifted constraints for four new mode-only ones (no function args, tag derived from the function name, no authored template, single `<style>` only). Re-probing the walls against the owner's counterexample (2026-08-30) found the diagnosis itself was wrong: TSRX008 is not a wall on a template-owning component at all; TSRX005 is already satisfied by the `@if` spelling, which generates the guard automatically; TSRX026 already resolves a ref into an `@if` branch and merely mis-types it as required; and the actual blocker was never in the four — it was that `{host.label}` server-renders empty (→ LT-122).
  **Scope:** (1) Delete `config.enhancer` and every enhancer-only branch: `config.ts` (flag + `readEnhancerFlag` pre-scan), `ir.ts` (`ComponentIR.enhancer`, `authoredRefs`), `compiler.ts` (the four wall-lifts, the zero-params check, the tag-from-function-name derivation, the authored-template error, the signal rejection, the `if (ref)` presence-guard parser), `emit-server.ts` (bare-tag stub), `emit-client.ts` (verbatim `authoredRefs`), `diagnostics.ts` (the enhancer note on TSRX025), `server/tests/tsrx/enhancer.test.ts` (whole file), `LE_TRUC_COMPILER.md` §5.1.1, `TSRX-HOST-PROFILE.md` §"Enhancer mode". (2) Rewrite `examples/basic/button/basic-button.tsrx` in the default path: template-owning, `first('button', <reason>)` required, `first('span.label')` optional, `expose({ disabled: button.disabled, label: labelSpan?.textContent ?? button.textContent ?? '', badge: … })`, `<span class="label">{label}</span>` and `@if (badge) { <span class="badge">{badge}</span> }` for the sites.
  **Inner-element classes (owner ruling, 2026-08-30):** a parent may place arbitrary classes on the HOST element to distinguish instances and they pass through to the served DOM (`composeHostAttrs`, verified working — re-verify, do not rebuild). Classes on INNER elements are owned by the child component and driven by server args, never authored by the page: `basic-button` gains `variant?: 'primary' | 'secondary' | 'tertiary' | 'constructive' | 'destructive'` and `size?: 'small' | 'medium' | 'large'`, rendered as `class={`${variant} ${size}`}` on the `<button>` and defaulting to `'secondary'` / `'medium'` when omitted. `examples/basic/button/basic-button.html` currently hand-authors `class="large"` / `class="small tertiary"` / `class="primary"` / `class="constructive"` on its buttons — regenerate the demo so those come from the args, and keep every existing instance visually equivalent.
  **Acceptance:** the twin's contract restored exactly (label ← `span.label` with button-text fallback, badge tolerating absence, disabled ← native button state; reactive and externally settable); `basic-button.html`'s `missing-elements-test` and `text-fallback-test` instances behave as before; the original spec passes verbatim against the compiled client; `check:tsrx` clean across the corpus with no new warnings beyond LT-122's intended one; every other migrated component still compiles unchanged (the three prerequisite tasks all touch the shared default path, so the whole corpus is the regression surface).
  **Changed:** deleted `config.enhancer` and every enhancer branch (`config.ts` incl. `readEnhancerFlag`, `ir.ts` `enhancer`/`authoredRefs`, `compiler.ts` the four wall-lifts + zero-params + tag-from-function-name + authored-template error + signal rejection + `if (ref)` presence-guard parser, `emit-server.ts` bare-tag stub, `emit-client.ts` verbatim refs, `diagnostics.ts` note), `server/tests/tsrx/enhancer.test.ts` (deleted), `LE_TRUC_COMPILER.md` §5.1.1 + §4.1/§5.3/§7 updates, `TSRX-HOST-PROFILE.md` §"Enhancer mode" (deleted; the data-account rule the architect added covers it), `examples/basic/button/basic-button.tsrx` (rewritten), `basic-button.html` (inner classes now match what the args render), `examples/main.ts` + `server/effects/tsrx.ts` (stale comments).
  **How:** the authored source reads like the hand-written twin and the generated client is that factory — `first('button', <reason>)` required, `first('span.label')`/`first('span.badge')` optional, `expose({ disabled: button.disabled, label: labelSpan?.textContent ?? button.textContent ?? '', badge: badgeSpan?.textContent ?? '' })`, both bindings guarded — while the server now renders the real markup (`<span class="label">` carries `esc(String(label))`, the badge branch renders under `if (badge)`, `class` comes from `variant`/`size`).
  **Inner-element classes:** `variant` (primary/secondary/tertiary/constructive/destructive) and `size` (small/medium/large) are server args rendered as `class={`${variant} ${size}`}`, defaulting to `secondary medium`; the CSS gained variant/size rules over the existing `--color-primary`/`--color-success`/`--color-error` tokens. Host-element class pass-through re-verified, not rebuilt (`composeHostAttrs`, form-colorgraph's `class="lightness"` etc.).
  **Corpus impact:** server 1160/1161, `check:tsrx` 22/22 with zero errors, `tsc` clean, CEM 64/0-dupes, src 426/426, Chromium 401/401, basic-button's 5 spec tests pass VERBATIM in both engines (10/10) incl. `missing-elements-test` and `text-fallback-test`.
  **Three pre-existing failures, all in the uncommitted LT-118 WIP and none of them mine** (each verified by stashing my changes and re-running): `examples/form/spinbutton/form-spinbutton.tsrx` does not parse (`Unexpected token (284:6)` — a bare `+` text child in an `@else`), which also fails `form-colorgraph` with TSRX011 → **I stashed that one file** (`git stash list`, newest entry) so the corpus could compile; restore it before continuing LT-118. Also failing at HEAD: `if-branches.test.ts`'s "text-only @else" case, and 3 `tsc` errors (2 in `if-branches.test.ts`, 1 in `analysis/effects.ts:688` `readonly TemplateNode[]`).
  **Review (2026-08-30):** Approved. Enhancer mode is gone with no residue (grepped: no `enhancer` identifier survives in `server/tsrx`), and `basic-button.tsrx` now reads as the reference the host profile claims it is — I read the generated pair rather than the handoff, and the client is the twin's factory with the two harvest sites guarded. The `variant`/`size` args are the correct expression of the ownership rule: inner-element classes are the child's, reached through its declared interface, host classes pass through. Corpus re-verified independently (22/22, the two TSRX039 warnings and the six pre-existing TSRX034 ones, nothing else); server 1160/1161 with the one failure reproduced as the LT-118 WIP's. The rejected-mode decision, the harvest-site rule, and the four-walls re-diagnosis are now spread across TSRX-HOST-PROFILE bullet 4, LE_TRUC_COMPILER §5.3, and this task — good enough to stop a re-proposal, and an ADR remains available if the owner wants one.

- [ ] LT-127: Retire composed-element `ref={}` — address compose sites with `first('<child-tag>.<class>')` (owner decision, 2026-08-30).
  **Skill:** le-truc-dev
  **Depends on:** LT-124 — do it first. LT-124 switches RAW-element class discriminators to `.token` selectors; compose discriminators already emit `.token`. Landing LT-127 first would ship two class-matching semantics in the same `first()` surface, one per element kind.
  **Context:** `ref={}` on a composed element supplies ONLY the query variable's name. The selector is already synthesized without it: `analysis/effects.ts:1194` builds `addQuery(refAttr.name, `${childTag}${discriminator}`, 'one')` from the `composeRegistry` tag plus `composeDiscriminatorClause`'s `.token` clause — `<FormSpinbutton class="lightness" ref={lightnessEl}>` already compiles to a query for `form-spinbutton.lightness`. Typing needs no library change either: `first()`'s `KnownTag<S>` (`src/helpers/dom.ts:70-79`) resolves `first('form-spinbutton.lightness')` through `HTMLElementTagNameMap`, which the child's own `.tsrx` declares, and sub-design 6's child-module side-effect imports put it in scope. **Why retire rather than namespace it `truc:ref`:** `ref` is the one place TSRX declares a binding by ATTRIBUTE instead of by statement — a name usable in setup with no `const` (LT-087 exists because that implicitness broke). Deleting it collapses composed and raw elements onto one addressing mechanism and removes an attribute kind rather than renaming one.
  **The actual blocker, and it is pass ordering, not design:** `classify-attributes.ts:340` records it — the `first()` structural check (TSRX026) runs inside `compileSource`, which has no `composeRegistry` (`index.ts:67`: `compileSource` runs BEFORE the registry is consulted). Fix by resolving compose-targeting selectors where the registry is already threaded (`analysis/plan.ts:415`, `analysis/effects.ts:108`), or by deferring selectors whose tag is not a known raw tag to the second pass. The registry-discovery pass has `composeRegistry === undefined` and must keep tolerating that (the existing LT-015 tolerance at `effects.ts:1181`), never erroring. Reuse `composeNodesBySource`/`composeDiscriminatorClause` for the check — including its existing "multiple same-source instances need a discriminating class/id/data-*" error, which becomes the diagnostic for an ambiguous `first()`.
  **Do NOT delete the synthetic ref.** `{ kind: 'ref' }` serves two purposes: the authored compose attribute (retire this) and the synthetic `{kind:'ref', name}` that `first-refs.ts`/`compiler.ts:1144` attaches to raw elements a `first()` call matched (keep this — it is the mechanism this task migrates compose sites ONTO). Only the authored branch goes: `classify-attributes.ts:162` + `:340-358`, `ast-utils.ts:504`, `emit-server.ts:587`, and the compose-node ref lookups in `analysis/effects.ts`.
  **Contract this promotes from implementation detail to invariant:** addressing a compose site by `.class` requires that `class`/`id`/`data-*` on a compose site actually reach the served DOM. `composeHostAttrs` does this (verified in LT-117), but `composeStaticAttrs`'s docstring currently says the opposite — "never treated as the child's actual rendered output" — because until now those attrs were only compile-time bookkeeping for telling sites apart. Update that docstring and pin the pass-through with a test.
  **Corpus migration, 7 sites:** `form-colorgraph` ×3 already carry `class="lightness"/"chroma"/"hue"` → `first('form-spinbutton.lightness')` etc.; `basic-gauge` (sole `<BasicNumber>` → `first('basic-number')`), `form-combobox` (sole `<FormListbox>` → `first('form-listbox')`), `form-inplace-edit` and `module-list` (sole `<FormTextbox>` each → `first('form-textbox')`). Compose refs are `'one'` (throwing) today, so migrate to the two-literal required form to preserve behaviour unless a site is genuinely optional — the author now picks, per LT-123.
  **Acceptance:** no `ref=` on any composed element in the corpus, `check:tsrx` 22/22, every spec green in both engines, and ADR 0024 sub-design 10 updated (it currently documents `ref={name}` as the naming mechanism).

- [ ] LT-128: Decide `html={}`'s future against current upstream `@tsrx/core` — do NOT namespace it `truc:html`.
  **Skill:** le-truc-dev
  **Context:** Raised in the 2026-08-30 `truc:`-namespacing discussion and deliberately excluded from it. `html={}` is NOT a Le Truc host-owned attribute: `classify-attributes.ts:195` documents it as "the `.tsrx` spelling of the upstream `{html expr}` keyword (newer grammar than the pinned parser)" — a polyfill for core TSRX vocabulary that pinned 0.1.60 cannot parse. Namespacing it would fork from upstream and turn the eventual switch to the real construct into a second migration instead of a deletion. **Unverified:** that comment is the only evidence, and current upstream grammar was not checked (offline). **Do:** confirm against current `@tsrx/core` whether `{html expr}` exists and in what shape; if it does, plan the migration from `html={}` to it (and schedule it with the pin upgrade), if it does not, record that `html={}` is a Le Truc invention after all and re-open the namespacing question. Zero corpus uses today (`form-listbox`'s docstring mentions it; nothing authors it), so nothing is blocked — this is about not painting ourselves into a fork.
  **Not in scope:** `truc:text` was considered and dropped as speculative (owner, 2026-08-30) — the stated case, overriding a server-known empty string with a client-only binding, is already spelled `{host.X}` (what managed form props do) or `{() => …}`.

- [ ] LT-124: Emit class DISCRIMINATORS as token selectors (`span.label`), not exact-match `[class="label"]` (LT-123 review finding).
  **Skill:** le-truc-dev
  **Context:** `analysis/selectors.ts`'s `discriminatorCandidates` builds a class discriminator as `[class="<full value>"]` — so `first('span.label')` in `basic-button.tsrx` is emitted into the client as `first('span[class="label"]')`, and a `class="a b"` element becomes `[class="a b"]`, order-sensitive. Two things make this worth fixing now rather than later. (1) It is INCONSISTENT with the compose-site path: `composeDiscriminatorClause` already emits `.token` clauses and its docstring claims to mirror `discriminatorCandidates`'s priority — the two disagree on semantics, not just priority. (2) LT-123's optional form changed the failure mode: a page that enhances with `<span class="label icon">` used to throw the required-reason and now binds nothing, silently — and page-authored markup (`missing-elements-test`, `text-fallback-test`) is exactly the case optional refs exist for. **Fix:** emit `.token` clauses for `class` (one candidate per token, as the compose path does), keeping `[attr="value"]` for `id`/`type`/`data-*`. `matchesSelector` (used by LT-118's per-branch exclusivity check) parses only the tag + `[attr="value"]` grammar and must learn `.token` too, or per-branch addressing will silently stop rejecting collisions — that is the load-bearing half of this task. **Acceptance:** corpus regenerates with `.token` selectors, every spec green in both engines, and a test pinning that a template element carrying `class="label"` is still found when the page renders `class="label icon"`. Note the twin's authored selector was `span.label` — this restores the contract the author wrote, it does not invent a looser one.

- [ ] LT-125: Diagnose a ref-derived setup local that reaches the server render (LT-121 review finding) — do NOT widen the stub set.
  **Skill:** le-truc-dev
  **Context:** LT-121's `refStub` covers `expose()`'s argument only: `emit-server.ts`'s `stubNames` is computed from `component.exposeArgNode`. A `first()`-bound ref read by an emitted SETUP statement outside `expose()` gets no stub. Reproduced 2026-08-30: `const input = first('input', '…'); const initial = input.value` with `<span class="label">{initial}</span>` compiles with ZERO diagnostics and emits a server module whose body is `const initial = input.value` with no `input` declared — `tsc` then fails with "Cannot find name 'input'", which `check:tsrx` catches. **The failure is loud, and it must stay loud.** The tempting fix — adding these names to `stubNames` — is WRONG: it would make the module compile and render `esc(String(refStub))`, i.e. an empty site where the author asked for a DOM-derived value the server cannot know. That is the LT-092 silent-empty-render class all over again. **Fix:** a diagnostic instead. A setup local whose initializer reads a `first()`-bound ref is client-only by construction; classifying it server-known and splicing it into the markup is the bug. Either exclude ref-derived locals from `serverKnown` (so the site is caught by the existing "no server-renderable value" machinery) or add a dedicated code naming the harvest form as the fix (`expose({ x: <ref read> })` + `{x}`, TSRX-HOST-PROFILE bullet 4). Small and self-contained; the probe above is the test.

- [ ] LT-126: `returnsNumber` does not detect number-signal reads, so no `String()` coercion is emitted (folded in from NOTES.md, 2026-08-30).
  **Skill:** le-truc-dev
  **Context:** `analysis/harvest.ts`'s `returnsNumber` recognises number LITERALS and conditionals over them only — a thunk reading a number-typed signal (`count.get()`) is not detected, so the coercion is skipped. Harmless while the only sink was `bindAttribute`; LT-116's dirty-flag widening makes `value` thunks on native controls lower to `bindProperty`, whose DOMString setter makes it reachable (`value={() => count.get()}` → generated client fails `check:tsrx`). No corpus source does this today, and LT-116's own tests pin only the literal/conditional shapes — so this is a latent gap with no current victim. **Fix:** consult the signal's `inferredType` for identifier `.get()` reads. Small, self-contained, blocking nothing; write the failing fixture first, since nothing in the corpus will fail for you.

- [ ] LT-118: Compiler capability — union addressing over structurally differing `@if`/`@else` branch roots (restores form-spinbutton's zero-state affordance).
  **Skill:** le-truc-dev
  **Context:** The twin's zero-state swaps whole structures (input+decrement ⇄ call-to-action button, aria-label swapped) across branches; `@if`/`@else` union addressing currently requires identical construct text across branch roots, so a structural swap is unrepresentable. Extend union addressing (LT-055/LT-060 lineage) so branch roots with differing structures can each be addressed when their effects bind to the branch that rendered — or, if that's a redesign rabbit hole, escalate with a concrete proposal rather than forcing it. Acceptance: the replaced LT-092 zero-state spec test is restored verbatim, `module-catalog` (sole consumer) stays green.
  **Working-tree state you inherit (folded in from NOTES.md, 2026-08-30):** the uncommitted `examples/form/spinbutton/form-spinbutton.tsrx` WIP does NOT parse — `Unexpected token (284:6)`, a bare `+` text child in an `@else` arm of the zero-state `@if` — which also cascades to `form-colorgraph` (TSRX011). LT-117's session **stashed that one file** to unblock the corpus: `git stash list` → `stash@{0}` "WIP on docs/server-components". `git stash pop` it before starting. Three further failures are yours, all reproduced at HEAD with LT-117/121/122/123 stashed: `if-branches.test.ts`'s "a text-only @else with a constructed @if root addresses per-branch", 3 `tsc` errors (`if-branches.test.ts:51,55` — `(string | undefined)[]`; `analysis/effects.ts:688` — a `readonly TemplateNode[]` passed to `handleOptionalBranch`'s mutable param), and `form-colorgraph.spec.ts`'s "knob drag rewrites host.value" on WebKit only.

- [ ] LT-119: form-listbox `visibleOptions` readonly prop; restore form-combobox's popup-visibility gate (LT-112 escalation decision).
  **Skill:** le-truc-dev
  **Context:** The twin hid the popup when zero options matched (`options.length > 0` in the visibility memo); the compiled listbox exposes no live visible-option state. Per the data account, add the missing child public prop — `visibleOptions` (readonly, derived from the filtered options list) on `form-listbox.tsrx` — and gate the combobox popup on it (compose through the declared interface; no reaching into the listbox's buttons). Re-flip the LT-092-flipped spec test to the original assertion. Small, self-contained; keep both specs green.

- [ ] LT-120: Retire the two pre-existing reach-ins via child public interfaces (LT-113 sweep findings).
  **Skill:** le-truc-dev
  **Context:** Two `querySelector` calls into composed children's owned markup violate the data account: `form-inplace-edit.tsrx`'s `textbox.querySelector('input')` (dblclick/click focus+select) and `form-combobox.tsrx`'s `listbox.querySelector('button[role="option"]')` (ArrowDown focus-first-option). Fix via boundary additions: `form-textbox` exposes a `focus()`/`select()`-delegating method (or focuses its own control via a public `focus` method matching native semantics), `form-listbox` exposes `focusFirstOption()`. Update both parents' handlers to the public calls; specs stay green; no behavior change.

- [x] LT-114: Compiler gap — root lazy text children get no client `bindText` (NOTES LT-092, blocks basic-number cutover) — reviewed ✓
  **Skill:** le-truc-dev
  **Changed:** `server/tsrx/analysis/effects.ts` (`emitTopEffects` root branch now visits the root's lazy text children — per-child managed-prop gate + ambient collection + impure-fold check mirrored from the nested path, then `watch-text` against the ambient `host`, mirroring the LT-028/032 root exemptions; multi-lazy / static-sibling / element-sibling mixes rejected with hazard-naming TSRX005 diagnostics instead of a plausible-but-wrong binding), `server/tests/tsrx/root-lazy-text.test.ts` (new, 8 tests, written first and watched failing), `examples/basic/number/basic-number.tsrx` (reshaped to the twin contract: `expose({ value: asNumber() })` writable + sole authored-thunk root child; generated client is behaviorally the twin's factory), twin deleted + main.ts + CEM exclusion (cutover complete), `examples/basic/gauge/basic-gauge.ts` (side-effect-imports the generated basic-number client per the dual-state rule).
  **How:** The as-authored shape carried both LT-115-class defects, so the cutover reshaped the component instead of fixing the compiler twice: no setup signals remain in basic-number at all. Spec passed UNMODIFIED (10/10); locale inheritance, invalid-config fallbacks + console errors, pre-upgrade writes, live `el.value` all restored and real-browser verified; gauge twin + compiled child verified live (property writes AND `observedAttributes` mutations).
  **Corpus impact:** `check:tsrx` 22/22 (zero new diagnostics); server 1092/1092 (+8); src 426/426; tsc clean; CEM 64/0-dupes; Chromium basic+card 38/38, full suite 401/401 (agent run); WebKit basic 32/32; `/test/basic-number` 12 elements formatted incl. inheritance + Arabic/Chinese numerals + 3 invalid-config fallbacks.
  **Review (2026-08-29):** Approved. Read the effects.ts diff and the reshaped source in full; re-ran check:tsrx / server 1092/1092 / src 426/426 / tsc / CEM / Chromium 38/38 independently. The reject-don't-guess emission gate matches the compiler's established refusal-of-partial-folds posture, and the per-child checks running independent of the gate is the right structure. Reshape trade-offs accepted (formatter re-derived per change — the card-colorscale precedent; server render carries the attribute contract with empty text — no SSR consumer, documented). Decisions on the flagged items: the nested-path asymmetry (silent multi-lazy/static+lazy mixes in `emitConstructEffects`) is folded into LT-115's scope — mirror the root's diagnostics there after a corpus sweep (escalate any offender rather than mass-failing check:tsrx); LT-115's harvest fix must route BOTH root-site branches (paramDomRead + the Pass-3 direct text-site variant found here) through the ambient `host`.

- [x] LT-115: Compiler gap — arg→DOM substitution freezes reactivity + illegal root self-query (NOTES LT-092/LT-114, blocks basic-gauge and basic-pluralize cutovers) — reviewed ✓ (subagent hit a usage-limit outage mid-run; architect completed the verification + recording)
  **Skill:** le-truc-dev
  **Changed:** `server/tsrx/analysis/harvest.ts` (`paramDomRead` gains a tracked-read precedence: for LAZY constructors — `deriveCell`/`deriveStore`/`createMemo`, whose bodies first run after `expose()` installs Slot-backed properties — an arg rendered on the root as a Parser-exposed prop reads `host.<prop>`, a tracked source, instead of the untracked `getAttribute` that froze post-connect updates; LT-024's lost `site.el !== component.root` guard restored; Pass 3's direct TEXT site on the root routes through the ambient `host` — `host.textContent`, never a self-query), `server/tsrx/emit-client.ts` (query loop skips a literal `'host'` entry defensively), `server/tsrx/analysis/effects.ts` (the root's multi-lazy/static-mix TSRX005 diagnostics mirrored onto the NESTED path — corpus sweep found zero offenders), `server/tests/tsrx/root-harvest.test.ts` (new, 5 tests) + nested-path tests, `basic-gauge.tsrx` (reshaped to the twin contract: `value` Parser-exposed with the inner `<meter>`'s value as fallback — children-are-data; `observedAttributes(['value'])` restored; meter mirror via property dispatch), `basic-pluralize.tsrx` (reshaped: no setup signals; category computed inline by authored thunks reading `host` — `getLocale` must walk `closest('[lang]')`, unreachable from signal bodies per TSRX013), both twins deleted + main.ts + CEM exclusions (cutovers complete), `module-todo.html` (demo regenerated with the six-span structure), `LE_TRUC_COMPILER.md` (Pass 3 precedence documented).
  **Corpus impact:** `check:tsrx` 22/22, no new errors; basic-pluralize carries 6 new TSRX034 warnings — the documented impure-ambient flash trade (`Intl` must not fold the build machine's locale; same accepted class as form-listbox's pre-existing warning), pinned in the component docstring. Server 1101/1101 (+9); src 426/426; tsc clean; CEM 64/0-dupes; full Chromium 401/401 — including the two gauge tests that exist for this fix ("updates when the value property changes", "re-parses the value attribute when it mutates at runtime") and all 10 pluralize tests.
  **Review (2026-08-29/30):** Approved. The lazy-vs-eager constructor distinction for the tracked read is the load-bearing correctness insight (an eager initializer runs before `expose()`, where `host.<prop>` would read `undefined` — the agent reasoned this through and gated on it). Read the harvest/effects/emit-client diffs and both reshapes in full; re-ran every gate independently (the subagent died at its usage limit before its own final verification/report). **Decision (pluralize span-narrowing escalation):** accepted — the compiled template always renders all six CLDR category spans and requires them in hydrated markup (demo regenerated); a page omitting individual spans was the twin's defensive-guard tolerance, and the general maybe-children capability is LT-117's scope, not a pluralize-specific concern. NOTES entries resolved: LT-114 interplay + both LT-092 stops (gauge freeze, self-query) deleted — incorporated here and into the compiler doc.

- [x] LT-116: Compiler gap — loop-body boolean dirty-flag attrs must lower to property writes (NOTES LT-092, blocks form-radiogroup cutover) — reviewed ✓
  **Skill:** le-truc-dev
  **Changed:** `server/tsrx/ast-utils.ts` (`DIRTY_FLAG_CONTROL_TAGS` + `isDirtyFlagControlAttr(tag, attr)`), `analysis/plan.ts` (`watch-attr` gains `dispatch: 'attribute' | 'property'`), `analysis/loops.ts` + `analysis/effects.ts` (dispatch computed at both the loop-body and top-level sites: bare host mirror OR dirty-flag control attr → property), `emit-client.ts` (`bindProperty` emission; typed `querySelector<Interface>` for loop descendant targets; `String()` coercion for number-valued thunks), `server/tests/tsrx/dirty-flag-dispatch.test.ts` (7 tests, written first — includes the `<meter value>` element-scoping proof and the string-thunk-over-ordinary-attr scoping proof), form-radiogroup cutover complete (twin deleted, main.ts, CEM exclusion removed, demo regenerated from the server render, `.tsrx` gained the twin's `disabled` per-radio propagation + toggle-preserving class-map form), `module-todo.html` port (`data-value` channel), `LE_TRUC_COMPILER.md` dispatch rule documented.
  **How:** The dispatch keys on **attr × tag, not thunk shape** — the divergence is a property of the target (a string `value` thunk over `<input>` desyncs exactly like a boolean `checked` thunk once dirty). form-checkbox's root mirror is subsumed with byte-identical output; managed-form machinery untouched.
  **Corpus impact:** `check:tsrx` 22/22 (7 documented warnings: 6 pluralize + form-listbox — no new); server 1108/1108 (+7); src 426/426; tsc clean; CEM 64/0-dupes; Chromium 401/401 — the spec was NOT modified, all 20 radiogroup tests verbatim against the compiled client; real-browser: click Female→Male→Other keeps exactly one checked, controlled `el.value` writes move live checks after interaction, zero console errors.
  **Review (2026-08-30):** Approved. Read the ast-utils/loops/effects/emit-client diffs and the spec-assertion changes; re-ran every gate independently (numbers above). **Spinbutton consequence ratified:** the widening flips its non-mirror `value` thunk to `bindProperty`, so the input's `value` attribute stays at the server-rendered default instead of live-syncing — correct per LT-057's own rule (the attribute is the reset baseline, not a live-value channel; the old attribute-syncing was that bug from the write direction), and this resolves the controlled-`value` resync hazard LT-092's review flagged. The rewritten assertions (`toHaveValue` instead of `toHaveAttribute`) are stronger claims of now-correct behavior; form-reset still passes. The `returnsNumber` heuristic gap (number-signal reads under property dispatch lack `String()` coercion; no corpus offender) stays in NOTES.md, folded into LT-093's scope.

- [ ] LT-093: Make TSRX004 honest for credited-but-unportable signal initializers, then thread initializer free names into client placement (LT-036's wall).
  **Skill:** le-truc-dev
  **Context:** Re-confirmed empirically 2026-08-29 (LT-071 re-evaluation): `const DEFAULT = 'red'; const color = createCell(DEFAULT)` consumed only through a style-map still fires TSRX004's "never rendered" message, though the signal IS credited as rendered (`thunkRendered`) — `substituteArgExpr`'s free-name gate rejects the verbatim initializer because the client module may not define the name. Step 1 (small): split the diagnostic — "rendered but initializer not client-portable" (name the offending free names) vs "never rendered". Step 2 (goal): feed signal-initializer free names into `computeClientNeededNames` as client-needed seed positions so plain-setup and import-local names in initializers place client-side; the fixpoint has grown accretively since the NOTES entry (clientSetup statements, composed refs, pass set-thunks — LT-069/087/088), so the plumbing gap is much narrower than when option (b) was judged heavy. Also fold in a compiler unit test for the `imports.plainLocalNames` badFreeNames widening (LT-090 review note — currently unexercised after the LT-091 redesign removed the corpus's two-way pass). Also fold in the LT-116 NOTES entry: `returnsNumber`'s heuristic misses number-signal reads (`count.get()`) in `value` thunks, which now lack `String()` coercion under the property dispatch — consult `inferredType` so the coercion fires for number-typed signal reads (no corpus offender today; add the unit test). No current migration is blocked by this wall (workaround: inline the constant or add a direct render site) — priority below the component tasks.

- [x] LT-094: ~~Port card-mediaqueries' demo page and e2e spec to the reshaped template-owning component~~ — folded into LT-092 (2026-08-29); landed there (demo switched to `label="…"` usage, spec append-test rewritten).

- [ ] LT-095: Migrate card-blogmeta by reshaping it into a template owner with typed byline props (LT-033 decision).
  **Skill:** le-truc-dev
  **Context:** **Design decided 2026-08-29 (checkpoint resolved):** fully-typed props, NO arbitrary pass-through (mediaqueries precedent) — `author` (string), `avatar` (optional URL string), `published` (datetime string), `modified` (optional datetime string), `reading-time` (optional number, minutes). The component's template re-emits ALL the schema.org microdata the old light DOM carried — `itemprop="author"`/`itemscope`/`itemtype="https://schema.org/Person"`, `datePublished`, `dateModified`, and `<meta itemprop="timeRequired" content="PT{n}M">` derived from the reading-time prop — with the avatar `<img>` behind an `@if` on the avatar prop and the modified span a conditional branch on prop presence. Author-supplied arbitrary siblings inside `<card-blogmeta>` are dropped; consumers port to props. Locale formatting and invalid-date handling expressed via setup consts (server-safe, the `fn2Digits` precedent). Consumers to port: `examples/card/blogmeta/card-blogmeta.html` (rewrite to attribute-configured usage), `server/effects/pages.ts` (`emitBlogCards` emits `<card-blogmeta author="…" avatar="…" published="…" modified="…" reading-time="…">`), `docs-src/layouts/blog.html`, the examples.md demo markup. Cutover is same-commit per the canonical pattern (LT-092): delete the `.ts` twin, point `examples/main.ts` at the generated client, drop any CEM exclusion, keep the blog pages rendering correctly (Playwright blog specs or a real-browser check).

- [ ] LT-075: Extend TSRX033 (impure server fold) to static/server-rendered attribute literals.
  **Skill:** le-truc-dev
  **Context:** Follow-up from LT-061. `classify-attributes.ts`'s `kind: 'server'` branch (e.g. `<div title={Date.now()}>`) isn't checked for impure ambients the way template children are. Needs a small refactor of `classifyAttribute`'s generic `invalidAttribute` wrapping (or a post-classification check in `lowerElement`, mirroring TSRX030). No current example exercises this — low urgency, cheap once someone's back in this area.

- [ ] LT-078: Implement conditional branch tree-shaking for `@try`/`@pending`/`@catch` (CHECKLIST §9).
  **Skill:** le-truc-dev
  **Context:** Performance optimization, not a bug fix (LT-065 confirmed current unconditional behavior is already safe). Needs a new usage-graph analysis: shake (emit no client task) only when the resolved value is read nowhere outside its own arm AND the guarding promise depends solely on server-definitive args. `form-listbox.tsrx` is the one real consumer of the async boundary — build fixtures around it, same caution as LT-077.

- [ ] LT-076: Establish a dev-mode signal for generated `.tsrx` client code, then implement the hydration assertion (CHECKLIST §6).
  **Skill:** le-truc-dev
  **Context:** **Architecture decision 2026-08-29:** generation-time inlining. `server/build.ts`/`server/effects/tsrx.ts` gain a dev/prod mode from the build pipeline (the docs site's examples bundle ships dev diagnostics today — `build:examples:js` already defines `DEV_MODE='"true"'` — so site/dev builds pass dev-mode ON; a prod site build flips it off), pass it to the compiler as a `devMode` option, and the compiler INLINES the folded constant into generated client modules. Generated code must never reference `process.env` (bundler-agnostic, constant-folded at generation, same philosophy as the library's own `--define`). With that signal in place, implement CHECKLIST §6's hydration assertion: on upgrade, recompute each folded expression and `console.warn` on mismatch — emitted only under the generation-time dev flag and folded away entirely otherwise.

- [ ] LT-096: Migrate `module-codeblock` to `.tsrx` with same-commit cutover.
  **Skill:** le-truc-dev
  **Context:** Smallest hand-written example (~43 lines). Migrate `examples/module/codeblock/module-codeblock.ts` → `.tsrx` following the corpus precedents and TSRX-HOST-PROFILE.md. Cutover is same-commit per the canonical pattern (LT-092): delete the `.ts` twin, point `examples/main.ts` at the generated client, drop any CEM exclusion, keep the demo/spec green against the served compiled component. Surface compiler gaps in NOTES.md — or fix them directly if small (LT-088 precedent) — never weaken the component to dodge a gap. **Known pre-existing bug to fix during this migration (LT-117 review):** the twin calls `copyToClipboard(code, copy, {...}` bare — the `EffectDescriptor` is created and discarded, so the copy-click listener never attaches (label stays "Copy" on click; verified at HEAD). Per AGENTS.md it needs registration — `watch(() => true, copyToClipboard(...))` or returning it — and a spec assertion that click actually copies/toggles the label (verify via clipboard or button-text state).

- [ ] LT-097: Migrate `module-cem-list` to `.tsrx` with same-commit cutover.
  **Skill:** le-truc-dev
  **Context:** ~39 lines, an `each()` loop over CEM manifest data. Migrate per the canonical pattern (see LT-096). Watch for: loop body reactive attrs on non-root children (the LT-037 fix), selector uniqueness among repeated items.

- [ ] LT-098: Migrate `module-colorinfo` to `.tsrx` with same-commit cutover.
  **Skill:** le-truc-dev
  **Context:** ~86 lines, color info display. Migrate per the canonical pattern (see LT-096); culori usage follows the `asOklch.ts`/`_common` setup point (modes must be registered there, LT-091 finding 3).

- [ ] LT-099: Migrate `module-pagination` to `.tsrx` with same-commit cutover.
  **Skill:** le-truc-dev
  **Context:** ~94 lines, pagination controls. Migrate per the canonical pattern (see LT-096); has a spec — keep it green against `/test/module-pagination`.

- [ ] LT-100: Migrate `module-catalog` to `.tsrx` with same-commit cutover.
  **Skill:** le-truc-dev
  **Context:** ~94 lines, component catalog. Migrate per the canonical pattern (see LT-096); has a spec — keep it green against `/test/module-catalog`.

- [ ] LT-101: Migrate `module-dialog` to `.tsrx` with same-commit cutover.
  **Skill:** le-truc-dev
  **Context:** ~75 lines, native `<dialog>` + `showModal()` orchestration. Migrate per the canonical pattern (see LT-096); has a spec. Watch for: `dialog.` method calls from client-only setup statements (LT-069 gate), focus-related event handlers as bare `on()` statements.

- [ ] LT-102: Migrate `module-splitview` to `.tsrx` with same-commit cutover.
  **Skill:** le-truc-dev
  **Context:** ~77 lines, pointer-capture drag between panes. Migrate per the canonical pattern (see LT-096). Watch for: `setPointerCapture`/`PointerEvent` client-only ambients (LT-069 widened `JS_GLOBALS` for exactly this class of code).

- [ ] LT-103: Migrate `module-scrollarea` to `.tsrx` with same-commit cutover.
  **Skill:** le-truc-dev
  **Context:** ~104 lines, scroll area with `IntersectionObserver`. Migrate per the canonical pattern (see LT-096); has a spec. Watch for: effect-with-cleanup idiom (`watch` + `return () => observer.disconnect()`, the LT-069 acceptance case).

- [ ] LT-104: Migrate `module-lazyload` to `.tsrx` with same-commit cutover.
  **Skill:** le-truc-dev
  **Context:** ~114 lines, `createTask` async loading. Migrate per the canonical pattern (see LT-096); has a spec + mocks (served under `/test/module-lazyload/mocks/...`, resolved from the component dir's `mocks/`). Watch for: async boundary shape — this is one of the few real `@try`/`@pending`/`@catch` consumers alongside `form-listbox` (fieldset auto-wrap, LT-077/086); tree-shaking interplay with LT-078.

- [ ] LT-105: Migrate `module-coloreditor` to `.tsrx` with same-commit cutover.
  **Skill:** le-truc-dev
  **Context:** ~120 lines, color editing UI. Migrate per the canonical pattern (see LT-096); culori usage follows the `_common` setup point. If it composes other form components multiple times, use the static-attr discriminator addressing (LT-087/089/090).

- [ ] LT-106: Migrate `context-media` to `.tsrx` with same-commit cutover.
  **Skill:** le-truc-dev
  **Context:** ~142 lines, the context-protocol example (`provideContexts` + `requestContext`, LT-035's compiled precedents exist in the corpus). Migrate per the canonical pattern (see LT-096). Watch for: context effects' server-side rendering semantics; no spec exists — verify on `/test/context-media` in a real browser.

- [ ] LT-107: Migrate `module-listnav` to `.tsrx` with same-commit cutover.
  **Skill:** le-truc-dev
  **Context:** ~129 lines, navigation list. Migrate per the canonical pattern (see LT-096). Also ports `examples/module/listnav/module-listnav.test.ts` — a unit test file — to run against the compiled artifact (or the served page, matching the corpus's spec conventions); mocks served under `/test/module-listnav/mocks/...` stay working.

- [ ] LT-108: Migrate `module-carousel` to `.tsrx` with same-commit cutover.
  **Skill:** le-truc-dev
  **Context:** ~161 lines, `each()` items + `IntersectionObserver` autoplay gating. Migrate per the canonical pattern (see LT-096); has a spec. Combines the LT-097 loop concerns with the LT-103 cleanup idiom.

- [ ] LT-109: Migrate `module-calctable` to `.tsrx` with same-commit cutover.
  **Skill:** le-truc-dev
  **Context:** ~200 lines, the heaviest `reconcile()` consumer (8 call sites). Migrate per the canonical pattern (see LT-096); reactive lists lower to the compiled `each()`/reconcile path (LT-003) — check loop-body reactive attrs on non-root children (LT-037) carefully.

- [ ] LT-110: Migrate `module-ticker` to `.tsrx` with same-commit cutover.
  **Skill:** le-truc-dev
  **Context:** ~283 lines, the most loop-dense example (`each()` ×11, `MutationObserver` ×6, `IntersectionObserver`, `populate`). Migrate per the canonical pattern (see LT-096). Expect this to stress the loop/effect analysis hardest — surface compiler gaps in NOTES.md rather than restructuring the component away from its demonstrated patterns.

- [ ] LT-111: Migrate `module-todo` to `.tsrx` with same-commit cutover — last hand-written example, completes the corpus port.
  **Skill:** le-truc-dev
  **Context:** ~379 lines, the largest example (`reconcile()` ×10, `each()`, pointer capture). Migrate per the canonical pattern (see LT-096); has a spec. Completing this task satisfies LT-014's trigger (every example outside `test/*` and `docs/*` is then `.tsrx`) — after review, confirm the corpus sweep: no `.ts` component files remain in `examples/` outside `test/`, `docs/`, and `_common` helpers.

- [ ] LT-014: Type-flow diagnostics — Volar language-core plugin over the LT-011 span table (ADR 0024 milestone 4, stage 2).
  **Skill:** le-truc-dev
  **Context:** Blocked on trigger: every example outside `test/*` and `docs/*` is cut over to its compiled client — that means LT-111 (module-todo, the last hand-written module example) AND LT-114..LT-117 finishing the five currently dual-state components (basic-number, basic-gauge, basic-pluralize, form-radiogroup, basic-button). CLI-first (LT-011, done) covers CI/agent workflows; this adds in-editor squiggles via a `@volar/language-core` plugin projecting the generated client module, reusing LT-011's span table.

---

## Done (archive)

Full task-by-task rationale, review notes, and corpus-impact numbers for everything below have been compacted out of this file; see git history (`git log -p -- TODO.md`) for the original entries if needed.

**TSRX compiler bring-up (ADR 0024 milestones 1–4):** LT-001/002 (compiler core + client codegen), LT-005 (isomorphic form-component format), LT-006 (CEM generation parity), LT-003 (reactive lists via `reconcile()`), LT-004 (Volar projection — infeasible, reshaped into LT-011), LT-008 (form-corpus migration enablers), LT-013 (tsrx.dev feature parity — `@switch`/`@try`/`html={}`), LT-011 (CLI-first span-table diagnostics, editor-level deferred to LT-014).

**Composition & children (ADR 0024 sub-design 10):** LT-015 (server-side PascalCase composition), LT-016 (`pass={{ }}` unified prop dispatch), LT-017 (two-way `pass()` codegen), LT-018 (`{children}` insertion), LT-019 (type-check generated server modules), LT-020 (module-list/form-textbox real composition).

**Async boundaries & control flow:** LT-012 (`isPending` routing, residue → LT-025), LT-023 (optional `@if`, hoisted handler consts), LT-024 (widened arg→DOM substitution), LT-025 (reactive `html={}`, `@try` addressing, `createMemo` — found LT-050).

**Example migrations, round 1:** LT-026 (`examples/basic/*`, found LT-027), LT-027 (`freeIdentifiers` TS type-position fix), LT-033 (`examples/card/*` — surfaced 3 genuine cliffs, now LT-071), LT-034 (plain-import placement inference), LT-035 (context protocol — `requestContext`/`provideContexts`).

**Bindings & reactive maps:** LT-030/029 (map-form `bind*()` overloads, merged), LT-028 (`style-map` AttributeIR, gaps → LT-031/032), LT-031 (`class-map` → `bindClass` array-form), LT-032 (root-level `class-map` exemption), LT-036 (style/class-map credited as harvest sites), LT-037 (`plain-imports.ts` traces `'server'`-kind attrs), LT-038 (`watch()` auto-wraps lazy-child thunks).

**Compiler regrouping (M1–M7):** LT-039 (`ir.ts`), LT-040 (`core.ts`), LT-041 (shared predicates → `ast-utils.ts`/`spans.ts`), LT-042 (`walk.ts` visitor), LT-043 (`evaluability.ts`), LT-044 (import handling, removed `node:path` — browser-purity gate), LT-045 (browser-bundle smoke test), LT-046 (remaining walks reviewed, won't-do), LT-021/LT-022 (`compiler.ts`/`analyze.ts` split into locality-focused modules), LT-047/LT-048 (direct unit tests for `evaluability.ts`, `loops.ts`/`naming.ts`), LT-049 (deleted dead `diagnostic.withLine`), LT-050 (`configureHtmlSanitizer()`).

**CHECKLIST.md triage (2026-08-26), conformance & ergonomics:** LT-051 (reactive-lift analysis, TSRX017), LT-052 (removed `&{}` sigil, TSRX018–020), LT-053 (`pass` → `truc:pass`), LT-054 (hard errors + codemod for near-miss React JSX, TSRX021–024), LT-055 (`ref={}` → `first()`, TSRX025–027), LT-056/LT-057 (`form.ts` reset-ordering + `defaultValue` baseline fix), LT-058 (TSRX028 — managed form member shadow), LT-059 (TSRX029 — named inner control), LT-060 (reconciled `form-textbox.revised.tsrx`, found the `@if` union-addressing bug), LT-061 (TSRX033 impure-fold rule, gap → LT-075), LT-063 (hydration — live-property harvest for dirty-flag attrs, assertion → LT-076), LT-064 (TSRX035 duplicate-id-across-arms, fieldset fix → LT-077), LT-065 (tree-shaking preconditions verified safe by omission, optimization → LT-078), LT-066 (TSRX030–032 authoring lint rules), LT-067 (docs pointed at tsrx.dev/llms.txt; eval re-run left as a manual user action), LT-068 (`TSRX-HOST-PROFILE.md` authored).

**2.5.1 form-reset release (shipped 2026-08-27):** LT-072 (ported form-reset fix to `bugfix/form-reset-baseline` off `next`, PR #119), LT-073 (form-docs consistency pass), LT-074 (changelog entry). Tagged `v2.5.1`; `next`/`package.json` now at `2.6.0`.

**Authored-import policy (ADR 0024 sub-design 16, re-amended 2026-08-27):** LT-079 (required explicit FactoryContext imports — superseded same day, highlighting rationale failed empirically), LT-080 (host-profile roster doc fix), LT-081 (migrated 53 test fixtures to the real-export import rule), LT-082 (sub-design 16 implementation itself — `REAL_EXPORT_NAMES`, scope-aware TSRX036/037, per-name import placement — reviewed ✓ 2026-08-29), LT-084 (rewrote host-profile's import section for the sub-design 16 policy).

- [x] LT-082: Implement the sub-design 16 authored-import policy — real-export imports required, FactoryContext ambient again (LT-079 reversal) — reviewed ✓
  **Review (2026-08-29):** Approved. `REAL_EXPORT_NAMES` (`ast-utils.ts`) verified byte-for-byte against `index.ts`'s actual value exports (108/108, no missing, no extra) — the hand-maintained-duplication risk flagged in the handoff is real but currently accurate. `reportLeTrucImportMismatch`'s scope-aware scan correctly handles the `JSXCodeBlock` sequential-scoping case the LT-079 review defect required (verified by reading the `VariableDeclarator`/`BlockStatement`/`Program`/`JSXCodeBlock` branches). Unused-import detection is correctly split: `placeLeTrucImports` (imports.ts) owns "authored but never used" (TSRX014), `reportLeTrucImportMismatch` (compiler.ts) owns "missing" (TSRX036) and "context name in import line" (TSRX037, correctly promoted warning→error) — no overlap, no gap. `emit-client.ts`'s synthesized-import line now correctly guards against emitting an empty `import {  }` once authored imports can cover the whole name set. Re-ran the full verification suite fresh rather than trusting the handoff's numbers: `bun run scripts/check-tsrx.ts` (21/21 clean, only the pre-existing TSRX034 warnings), `bun test server/tests` (1062/1062 — the handoff's "60 failures" note was a pre-LT-081 snapshot, confirmed stale), `bun test src/tests` (426/426), `bun run build:cem` (64 declarations, no duplicate tags). Matches ADR 0024 sub-design 16 and its own "Rejected" log entry on the reversed explicit-import experiment. ADR 0024's status note updated to reflect LT-082 landing.
  **Skill:** le-truc-dev
  **Changed:** see full handoff in git history (`git show 1eacaeee`) — `server/tsrx/{ast-utils,imports,compiler,diagnostics,ir,emit-client,globals.d}.ts`, corpus (17 files), `server/tests/tsrx/{globals,le-truc-imports,client.golden}.test.ts` + 5 snapshots.
