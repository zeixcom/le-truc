# TODO

> Task queue derived from `AUDIT_REPORT.md` (2026-06-25 source audit). Each task traces to a finding (`A#`) and, where applicable, a `REQUIREMENTS.md` requirement. Sequenced by severity; fix security/critical items first, then hardening, then tests, then docs.

- [x] LT-001: Fix `DEV_MODE` to be a boolean — string `"false"` must be falsy — done ✓
  **Skill:** le-truc-dev
  **Context:** Audit A1 (🔴 Critical). `src/util.ts:7` reads `typeof process !== 'undefined' && process.env.DEV_MODE`, which yields the *string* `"false"` (truthy) when the env var is `"false"`, so dev-mode branches run in any non-build runtime (tests, no-build CDN, mis-configured bundlers). Violates S4. Shipped prod bundles are safe (the build `--define` replaces the whole expression), but tests never exercise DEV_MODE branches.
  **Changed:** `src/util.ts` (`DEV_MODE` initializer); `src/tests/util.test.ts` (6 new tests)
  **How:** `const DEV_MODE = typeof process !== 'undefined' && process.env.DEV_MODE === 'true'`. Added tests that re-import the module under controlled env states (unset/`"true"`/`"false"`/`"1"`/`""`) to lock the strict-equality behavior.
  **Check:** Full suite 161/161 (was 142; +6 DEV_MODE tests + 13 from LT-002). `isBoolean(DEV_MODE)` asserted.

- [x] LT-002: Harden `isSafeURL` against whitespace-scheme and protocol-relative bypasses — done ✓
  **Skill:** le-truc-dev
  **Context:** Audit A2 (🔴 Critical). `src/bindings.ts:36-48` let `java\tscript:`, `java\nscript:` (internal whitespace survives `.trim()`), and protocol-relative `//evil.com` through as "safe". Directly undermines M16. No unit tests existed for `isSafeURL`.
  **Changed:** `src/bindings.ts` (`isSafeURL`); `src/tests/bindings.test.ts` (13 new tests)
  **How:** Strip ALL ASCII whitespace (`\t\n\r\f\v`, not just edges) before the scheme regex; reject protocol-relative (`//host`) and backslash (`\\host`, `/\host`) prefixes via `/^[\\/][\\/]/`; otherwise unchanged. Kept `mailto:`/`tel:`/relative/fragment/query allowances.
  **Check:** 13 new cases cover every A2 bypass (internal tab/CR/newline, leading ws, protocol-relative, backslash variants, comment-newline trick) plus 5 allow cases (`tel:`, fragment, query, `page.html`, `ftp:`). Full suite 161/161. Linter clean. **E2E (form-listbox security spec) not yet re-run — no browser env change; recommended before release.**

- [x] LT-003: Enforce `ReservedWords` at runtime — throw `InvalidPropertyNameError` — reviewed ✓
  **Skill:** le-truc-dev
  **Context:** Audit A3 (🟠 High). `InvalidPropertyNameError` (`src/errors.ts:28`) was defined and exported but never thrown; `ReservedWords` was type-level only. A `__proto__`/`constructor` prop name reached `Object.defineProperty` in `#setAccessor`, risking prototype-chain corruption. Especially relevant for `asJSON`-driven props (A15).
  **Changed:** `src/types.ts` (new exported `RESERVED_WORDS` Set + `isReservedWord()` predicate mirroring the `ReservedWords` type); `src/component.ts` (`#initSignals` guard before `createReactiveProperty`); `src/tests/types.test.ts` (7 tests); `examples/test/audit/test-audit.ts` + `test-audit.spec.ts` (E2E: throw on connect).
  **How:** `if (isReservedWord(prop)) throw new InvalidPropertyNameError(this.localName, prop, 'reserved word or Object builtin …')`, placed **before** the `prop in this` guard (which would otherwise silently skip every reserved word, since they are all inherited Object builtins). **Public API added:** `isReservedWord`, `RESERVED_WORDS` exported from `index.ts`.
  **Review:** Approved. Guard placement (before `prop in this`) is the load-bearing correctness fix. See LT-017 for one optional API-surface refinement.
  **Check:** Unit suite 168/168. Typecheck clean. **E2E: `test-audit.spec.ts` 8/8 green (Chromium + WebKit)** — asserts the `InvalidPropertyNameError` surfaces via the `pageerror` listener (custom-element reaction exceptions don't propagate to `appendChild`), names component + prop, and that the reserved property is NOT installed as an own accessor. Full E2E suite 830/830 (was 810).

- [x] LT-004: Run previous cleanup before re-activating `#setup` on reconnect — done ✓
  **Skill:** le-truc-dev
  **Context:** Audit A4 (🟠 High). `connectedCallback` re-ran `runSetup()` on an already-initialized element, overwriting `this.#cleanup` without invoking the prior one. Each reparent/reslot cycle accumulated listeners, `pass()` slot swaps, and `each()` scopes. Directly violated the Reliability requirement "no memory leaks from connected/disconnected cycles."
  **Changed:** `src/component.ts` (`connectedCallback` reconnect branch); `examples/test/audit/test-audit.ts` + `test-reconnect.html` + `test-reconnect.spec.ts` (E2E).
  **How:** In the `if (this.#initialized)` branch, `if (isFunction(this.#cleanup)) this.#cleanup()` before `runSetup()`. First-connect path unaffected (`#cleanup` is `undefined`). No public API change.
  **Check:** Unit suite 168/168. **E2E: `test-reconnect.spec.ts` 6/6 green (Chromium + WebKit)** — reparents 5× then asserts a single click increments `value` by exactly 1 (would be 6 with the leak), plus value-stays-0 and no-throw-on-reparent tests. Full E2E suite 830/830.

- [x] LT-005: Clarify `dangerouslyBindInnerHTML` security contract; guard non-`<script>` vectors — reviewed ✓
  **Skill:** le-truc-dev
  **Context:** Audit A5 (🟠 High). `src/bindings.ts:264-303` sets `target.innerHTML = html` which fires event-handler attributes on non-`<script>` elements (e.g. `<img onerror>`) regardless of `allowScripts`. The JSDoc implied `allowScripts:false` was the safety lever; it only governs inline `<script>` re-execution. `SCRIPT_ATTRS` also omitted `nonce`.
  **Changed:** `src/bindings.ts` (rewrote JSDoc security note; added `sanitize?: (html:string)=>string` option applied before `innerHTML`; added `nonce` to `SCRIPT_ATTRS`); `types/` regenerated; `examples/test/audit/test-audit.ts` + `test-sanitize.html` + `test-sanitize.spec.ts` (E2E). **Public API added:** `sanitize` option on `DangerouslyBindInnerHTMLOptions`.
  **How:** JSDoc now states plainly that `allowScripts:false` does NOT make untrusted HTML safe (innerHTML fires non-script handlers) and that all content must be trusted or sanitized — the `sanitize` hook is the supported way to wire in DOMPurify. Le Truc ships no built-in sanitizer (bundle-size/contract boundary).
  **Check:** Unit suite 168/168. Typecheck clean. **E2E: `test-sanitize.spec.ts` 6/6 green (Chromium + WebKit)** — asserts the `sanitize` hook strips an `<img onerror>` vector before assignment (XSS does not fire), preserves safe content (`<img src>`, `<p>`), and includes a control proving the vector fires without the hook. Full E2E suite 830/830.
  **Review:** Approved as implemented. `sanitize`-as-chokepoint respects the no-sanitizer/no-templating boundary (REQUIREMENTS §7). See LT-016 — Trusted Types is Baseline 2026 and `innerHTML` throws under `require-trusted-types-for 'script'`; the `sanitize` hook is the right place to close that gap.

- [x] LT-006: Isolate scheduler task/callback exceptions per-frame — done ✓
  **Skill:** le-truc-dev
  **Context:** Audit A6 (🟡 Medium). `src/scheduler.ts:8-16` `runTasks` has no try/catch around `tasks.get(element)?.()` or the throttled-callback loop; one throw aborts every later task in the same RAF frame.
  **Changed:** `src/scheduler.ts` (`runTasks`); `src/tests/scheduler.test.ts` (2 new tests)
  **How:** Wrapped both the per-element task call and each throttled-callback invocation in their own `try { … } catch (e) { console.error('[le-truc scheduler]', e) }`, so a throw in one no longer prevents later tasks/callbacks in the same frame from running. No public API change.
  **Check:** New tests assert a throwing `schedule`d task / throttled callback doesn't block a later one in the same frame, and that `console.error` is called once with the `'[le-truc scheduler]'` tag. Full suite 170/170 (was 168). Typecheck clean. Linter clean.

- [x] LT-007: Fail fast on malformed `all()` selector instead of stalling the MutationObserver — reviewed ✓
  **Skill:** le-truc-dev
  **Context:** Audit A7 (🟡 Medium). `src/helpers/dom.ts:191-214` calls `node.matches(selector)` / `node.querySelector(selector)` inside the observer with no guard; a malformed selector throws `SyntaxError` inside the callback (silently swallowed per spec) and the memo stops invalidating. Selector errors should surface at `all()` call time.
  **Changed:** `src/helpers/dom.ts` (`createElementsMemo`, plus `@throws` JSDoc on `createElementsMemo`/`all`); `src/errors.ts` (new `InvalidSelectorError` class); `index.ts` (export); `ARCHITECTURE.md` (one-line note under the Query System section); `src/tests/dom.test.ts` (3 new tests).
  **How:** Eager fail-fast, per the preferred option: `createElementsMemo` runs one `parent.querySelector(selector)` in a try/catch at memo-creation time (before the `createMemo()`/`watched` setup), and re-throws as `InvalidSelectorError` — named after the parent (component, shadow root, or document) and the selector. Since the same selector string is reused for every later `querySelectorAll`/`matches` call inside the memo, validating once at creation also protects the `couldMatch` check inside the `MutationObserver` callback. **Public API added:** `InvalidSelectorError` exported from `index.ts` (new error class alongside `MissingElementError`/`DependencyTimeoutError`).
  **Check:** `src/tests/dom.test.ts` — `createElementsMemo(parent, ':bad[')` throws `InvalidSelectorError` naming the selector; valid selectors (`'div.foo'`) unaffected. No real DOM in the unit-test runtime, so `parent` is a minimal `querySelector`/`querySelectorAll` stub (mirrors the `events.test.ts` stub-DOM style) — `InvalidSelectorError`'s `ShadowRoot`/`Element` `instanceof` checks were guarded with `typeof X !== 'undefined'` so the class itself doesn't throw `ReferenceError` in that DOM-less environment. Full suite 233/233. Typecheck clean. Linter clean.
  **Review:** Approved. `InvalidSelectorError` cleanly extends the existing `Invalid*Error extends TypeError` family (consistent with `InvalidComponentNameError`/`InvalidCustomElementError`/`InvalidReactivesError`); message names the selector, the parent (component/shadow-root/document), and the native cause — actionable per S3's spirit even though S3 only names `MissingElementError` literally. Eager fail-fast (one `querySelector` call at creation) is the right call over the always-invalidate alternative: it surfaces the error synchronously at the `all()` call site with a useful stack trace, instead of asynchronously inside a `MutationObserver` callback on some later mutation. One nit fixed in review: `@since` was `2.0.5`, inconsistent with the `2.0.4` tag used by this same unreleased batch's other new export (LT-005's `sanitize` option) — corrected to `2.0.4` to match. No other concerns.

- [x] LT-008: Guard context-provider getter so a throw doesn't escape into the consumer's Memo — done ✓
  **Skill:** le-truc-dev
  **Context:** Audit A8 (🟡 Medium). `src/helpers/context.ts:152-164` provider does `callback(() => host[context])`; if `host[context]` getter throws, it throws inside the consumer's `Memo.get()` with no provider attribution.
  **Changed:** `src/helpers/context.ts` (`makeProvideContexts` listener); `ARCHITECTURE.md` (one-line note under Context Protocol); `src/tests/context.test.ts` (2 new tests)
  **How:** Wrapped the getter exactly as proposed: `callback(() => { try { return host[context] } catch (error) { if (DEV_MODE) console[LOG_WARN]('provideContexts: getter threw', elementName(host), error); return undefined } })`. Renamed the catch binding to `error` to avoid shadowing the outer `ContextRequestEvent` parameter `e`. No public API change.
  **Check:** New tests assert (1) a throwing getter is swallowed and the consumer's received getter returns `undefined` without throwing, and (2) the DEV_MODE branch logs `'provideContexts: getter threw'` naming the host. Since `DEV_MODE` is a module-level const captured at import time from `../util`, the second test uses `mock.module('../util', …)` to patch the already-bound live binding (flipping `process.env.DEV_MODE` alone has no effect post-import) — restored via `mock.module` back to the real exports in a `finally`; full suite re-run 3× to confirm no cross-file pollution. Full suite 175/175 (was 173). Typecheck clean. Linter clean.

- [x] LT-009: Add missing unit tests for bindings, component lifecycle, and DEV_MODE — done ✓
  **Skill:** le-truc-dev
  **Context:** Audit A11 (🟡 Medium). The unit suite (142 tests) has no coverage for `isSafeURL` (A2), any `bind*` function, `component.ts` lifecycle (`defineComponent` name validation, connect/disconnect, `#initSignals` dispatch order, reconnect — A4), or `DEV_MODE` (A1). Most of this is exercised only by slow E2E.
  **Changed:** `src/tests/bindings.test.ts` (+57 tests for all `bind*` helpers, `setTextPreservingComments`, `dangerouslyBindInnerHTML` shadow/sanitize/allowScripts paths, via a minimal fake-DOM `FakeElement`); new `src/tests/component.test.ts` (+17 tests: name validation, connect/disconnect, `#initSignals` dispatch order, reconnect/LT-004 regression, `prop in this` guard, reserved-word guard — via a minimal fake `HTMLElement`/`customElements` registry, calling `connectedCallback`/`disconnectedCallback` directly); `src/tests/events.test.ts` (+7 tests: passive/throttle scheduling, Memo-target delegation vs. non-bubbling per-element fallback, DEV_MODE warning); `src/tests/reactive.test.ts` (+5 tests: real `makePass` Slot swap/restore, `each()` leave/enter disposal via `createEffect`); `src/tests/dom.test.ts` (+1 test: `resolveDependencies` real 200ms timeout fallback — `resolveDependencies` actually lives in `src/helpers/dom.ts`, not `reactive.ts`, so this landed there instead of the file list above); `src/tests/context.test.ts` (DEV_MODE-warning test from LT-008, fixed — see note below). `isSafeURL`/`DEV_MODE` value coverage were already complete from LT-002/LT-001. No production source changes — test-only.
  **How:** Stub-DOM/fake-registry style throughout, mirroring `context.test.ts`/`events.test.ts`. `component.test.ts` extends `HTMLElement` and defines a fake `customElements` registry as globals so `class Truc extends HTMLElement` / `customElements.define/get` resolve at all — `connectedCallback`/`disconnectedCallback` are invoked directly (legitimate: ordinary prototype methods, not browser magic).
  **Found & fixed in passing:** `mock.module()` (used to flip `DEV_MODE` for already-loaded consumers, e.g. LT-008's `context.test.ts` test) mutates the **live module namespace in place** — restoring via `mock.module(id, () => liveNamespaceImport)` re-feeds the already-mutated values, permanently corrupting `DEV_MODE` for the rest of the `bun test` process (confirmed: caused a real cross-file leak into unrelated `context.test.ts` assertions). Fix: snapshot the namespace into a plain object (`{ ...realUtilNamespace }`) *before* any `mock.module` call, and restore from that snapshot, not the live import. Applied to both `context.test.ts` and `events.test.ts`; verified leak-free across 8+ repeated full-suite runs. Documented inline at both `import * as realUtilNamespace` sites — worth remembering for any future `mock.module` use in this repo.
  **Check:** Full unit suite 233/233 (was 142 at audit time, 175 immediately before this task). Typecheck clean (`tsc -p tsconfig.build.json --noEmit`). Linter clean. Confirmed no test-order flakiness via 8+ repeated `bun test src/tests` runs.

- [ ] LT-010: Fix `LOG_ERROR` documentation drift (symbol does not exist in source)
  **Skill:** tech-writer
  **Context:** Audit A10 (🟡 Medium) + doc drift. `AGENTS.md:36` and `.agents/skills/le-truc-dev/references/source-map.md:32` reference `LOG_ERROR`, but `grep LOG_ERROR src/` returns zero matches — `src/util.ts` exports only `LOG_WARN`.
  **Changed:** `AGENTS.md`, `.agents/skills/le-truc-dev/references/source-map.md` (and any other skill/doc referencing `LOG_ERROR`)
  **How:** Replace `LOG_ERROR` references with `LOG_WARN` (or `console.error`), matching actual source. No code change.
  **Check:** `grep -rn LOG_ERROR . --exclude-dir=node_modules --exclude-dir=docs` returns no stale references outside generated docs.

- [x] LT-011: Make `asBoolean()` case-insensitive — reviewed ✓
  **Skill:** le-truc-dev
  **Context:** Audit A9 (🟡 Medium). `src/parsers/boolean.ts:13-16` treated only exact `'false'` as opt-out, so `"FALSE"`/`"False"` evaluated to `true` (previously asserted in `parsers.test.ts:34-39`). Counterintuitive vs HTML author expectations and inconsistent with the case-insensitive `asEnum`/hex handling. **Product sign-off obtained directly from Esther Brunner (2026-06-25):** approved as a breaking change — rationale is that ARIA attributes carry string boolean values (`aria-hidden="true"`/`"false"`) and should convert to their logical boolean equivalent case-insensitively, same as HTML author expectations generally.
  **Changed:** `src/parsers/boolean.ts` (`asBoolean` — case-insensitive `'false'` comparison + JSDoc); `src/tests/parsers.test.ts` (replaced the case-sensitivity-asserting test with a case-insensitive one; relabeled the whitespace test, which was previously mislabeled "case-insensitive"); `CHANGELOG.md` (new `Unreleased` section, breaking-change entry); `.agents/skills/le-truc/references/parsers.md` (one-line behavior correction).
  **How:** `value != null && value.toLowerCase() !== 'false'`, matching the approved proposal exactly.
  **Check:** Full unit suite 236/236. Both `tsconfig` typechecks clean. Linter clean. **Doc regen note:** ran `bun run build:docs` to refresh `docs-src/api/functions/asBoolean.md`, but it touched ~94 unrelated generated files (full `docs/` + `docs-src/api/*` tree, including new fragments for LT-007's `InvalidSelectorError` and LT-015's `TrustedHTML` that hadn't been regenerated yet) — reverted all of it as out of scope for this task; this repo regenerates docs via a separate dedicated commit (see `ceb43020`). A full doc regen is still owed before release and should bundle this change along with the other not-yet-regenerated public API additions from LT-005/LT-007/LT-015.
  **Review:** Approved. Implementation matches the approved proposal verbatim — single-line change, consistent with `asEnum`'s existing case-insensitive precedent, no scope creep into whitespace handling (correctly left untouched — that's a separate, never-requested behavior). Checked `examples/` and `*.spec.ts` for any test relying on the old case-sensitive bug (`grep` for `asBoolean`/`"FALSE"`/`"False"`) — none found, so this is safe to ship. No ADR needed: this is a parser-semantics correction with established precedent (`asEnum`), not a new pattern or boundary decision.

- [x] LT-012: Strip `__proto__`/`constructor` keys from `asJSON` output (defense-in-depth) — reviewed ✓
  **Skill:** le-truc-dev
  **Context:** Audit A15 (🔵 Low). `asJSON` (`src/parsers/json.ts`) returns `JSON.parse` output unchanged; combined with A3/LT-003 a `__proto__`/`constructor` key could become an own accessor on the host. `JSON.parse` itself is not a pollution vector, but defense-in-depth is cheap once LT-003 lands.
  **Changed:** `src/parsers/json.ts` (`asJSON` — added `JSON.parse` reviver); `src/tests/parsers.test.ts` (3 new tests)
  **How:** Used a `JSON.parse` reviver (second arg) rather than a post-parse copy: `JSON.parse(value, (key, parsed) => isReservedWord(key) ? undefined : parsed)`. The reviver runs bottom-up over every key at every nesting level, and returning `undefined` triggers a spec-defined `[[Delete]]` of that own property — so this also closes the gap for a reserved word nested under an ordinary key (not just top-level), using the same `RESERVED_WORDS`/`isReservedWord` from `src/types.ts` (LT-003). No public API change — `asJSON`'s signature and `SingleMatchHandlers`-style contract are untouched.
  **Check:** `src/tests/parsers.test.ts` — `asJSON({})('{"__proto__":{"x":1}}')` returns an object without an own `__proto__` accessor (top-level); added a nested-`__proto__` case and a `constructor` case. Full unit suite 236/236 (was 233). Both `tsconfig` typechecks clean. Linter clean (auto-fixed `Object.prototype.hasOwnProperty.call` → `Object.hasOwn` in the new tests).
  **Review:** Approved. Internal-only — no review gate was actually required by this task's own status (`done ✓`, not `pending review ⏳`), but confirming on request: reusing the full `RESERVED_WORDS` set (not just `__proto__`/`constructor`) is the right call — it's the same boundary LT-003 already enforces for component prop names, so `asJSON` output can't introduce via a side door what `#initSignals` already blocks via the front door. The reviver approach is preferable to a shallow post-parse copy: same line count, but closes the gap at every nesting depth, not just top-level.

- [x] LT-013: Document the `resolveDependencies` timeout race window in AGENTS.md — done, pending review ⏳
  **Skill:** tech-writer
  **Context:** Audit A13 (🔵 Low). After the 200ms timeout (`src/helpers/dom.ts:322-357`), effects activate against a possibly-`:not(:defined)` child. Behavior is intentional (progressive enhancement, M8) but the race — a child's own `connectedCallback` may clobber state a parent effect just wrote — is not documented.
  **Changed:** `AGENTS.md`
  **How:** Add a bullet under "Surprising Behaviors" noting the timeout-degraded race and recommending that parent effects not write to a timed-out child's props.
  **Check:** Reviewer confirms accuracy.

- [x] LT-014: Throw `InvalidPassPropertyError` on `pass()` binding failure — reviewed ✓
  **Skill:** le-truc-dev
  **Context:** Audit A14 (🔵 Low), redesigned after Architect/user back-and-forth — final decision recorded in [ADR 0011](adr/0011-throw-on-pass-binding-failure.md). `src/helpers/reactive.ts`'s `swapSlots` already throws `InvalidCustomElementError`/`InvalidReactivesError` for two structural preconditions; three more failure modes were silent or `DEV_MODE`-warn-only: (1) `!(prop in target)` — prop doesn't exist on target (line ~283), (2) `toSignal()` returns falsy (line ~292, no diagnostic at all today), (3) `!isSlot(slot)` — prop isn't Slot-backed (line ~303), which is how a non-Le-Truc target (Lit/FAST/vanilla WC — `getSignals()` lazily creates an empty signals record for any element Le Truc never touched) or a read-only/computed Le Truc prop is actually detected. Every entry in `PassedProps` is a declared intent to bind a live signal — none of these three are legitimate "maybe intentional" states, so all three now throw, consistently with the other two checks.
  **Changed:** `src/errors.ts` (new `InvalidPassPropertyError` class); `index.ts` (export it, alphabetically between `InvalidCustomElementError` and `InvalidPropertyNameError`); `src/helpers/reactive.ts` (`swapSlots` rewrite; drop the now-unused `DEV_MODE`/`LOG_WARN` import from `'../util'` — confirm nothing else in the file still needs them); `src/tests/reactive.test.ts`; `CHANGELOG.md` (`Unreleased` → `### Fixed`, breaking-change note — classify as a bugfix/explicit-unhappy-path per user direction, not a feature change, even though it is technically breaking)
  **How:**
  1. New error class in `src/errors.ts`, alongside the other `Invalid*Error extends TypeError` classes:
     ```ts
     class InvalidPassPropertyError extends TypeError {
       constructor(host: HTMLElement, target: HTMLElement, reasons: Map<string, string>) {
         const detail = Array.from(reasons, ([prop, reason]) => `'${prop}' ${reason}`).join('; ')
         super(`Cannot pass from ${elementName(host)} to ${elementName(target)}: ${detail}.`)
         this.name = 'InvalidPassPropertyError'
       }
     }
     ```
  2. Rewrite `swapSlots` as an eager two-phase validate-then-commit (same shape as LT-007's `all()` selector validation — validate everything before any mutation, so a throw never leaves a partial swap): loop once over `Object.entries(props)`, skipping `reactive == null` entries (still a legitimate "not passed this time," not a failure); for each remaining entry, collect into a `failures: Map<string, string>` if `!(prop in target)` (reason: `` `does not exist on ${targetName}` ``), if `toSignal()` is falsy (reason: `'could not be resolved to a signal'`), or if `!isSlot(signals[prop])` (reason: `` `is not Slot-backed on ${targetName} (read-only property, or target is not a Le Truc component)` ``); otherwise push `{ slot, signal }` to a `bindings` array. After the loop: if `failures.size`, `throw new InvalidPassPropertyError(host, target, failures)` — **before** touching any slot. Otherwise, commit: for each binding, capture `slot.current()`, call `slot.replace(signal)`, push the restore closure to `cleanups`. Return the cleanup-running function if `cleanups.length`, same as today.
  3. Remove both `console[LOG_WARN]` call sites and the `DEV_MODE`/`LOG_WARN` imports from this file (no longer used in `reactive.ts` after this change — double check with a repo-wide grep before deleting in case something else in the file picked up a dependency on them).
  4. No change to the two existing top-of-function throws (`InvalidCustomElementError`, `InvalidReactivesError`) or to the `Memo<Element[]>`-target loop in `pass()` — per ADR 0011, matched elements are homogeneous, so letting one element's throw propagate and abort that `createEffect` run (today's existing behavior, confirmed via `createScope`'s `try/finally` not swallowing exceptions) is intentional, not a gap.
  **Check:**
  - Rewrote `'does not swap a property that is not Slot-backed'` → `'throws InvalidPassPropertyError when the target prop is not Slot-backed'`, asserting the throw and that the plain value is untouched.
  - New test: `'throws InvalidPassPropertyError when the prop does not exist on target'`.
  - New test: `'aggregates multiple failing props into a single InvalidPassPropertyError'` — one prop not Slot-backed + one prop missing entirely, in the same call, throws once naming both.
  - New test (atomicity): `'does not leave a partial swap when one of several props fails'` — one good Slot-backed prop + one bad (`'farewell'` doesn't exist) in the same call; after the throw, asserts `slot.current()` on the good prop is still the original target signal, not the host's.
  - Existing `'swaps a Slot-backed property...'` and `'throws InvalidCustomElementError...'` tests pass unchanged.
  - Full unit suite: 239/239 (was 236, +3 net new tests). Both `tsconfig` typechecks clean. Linter clean (no fixes needed).
  - Checked `examples/` for any reliance on the old silent/warn behavior: `examples/test/pass/test-pass.ts` only passes to `basic-number`'s Slot-backed `value` prop (success path, unaffected); no example or E2E spec exercises the non-Slot-backed/missing-prop failure paths, so nothing else needed updating.
  - Also fixed in passing (same paragraph being edited, not scope creep): `.agents/skills/le-truc-dev/references/non-obvious.md`'s "`pass()` Scope is Le Truc Components Only" note cited a nonexistent `src/effects.ts` (pass() actually lives in `src/helpers/reactive.ts`'s `makePass`) — corrected, and added a bullet documenting the new throw behavior and its deferred-activation/uncatchable-by-caller nature (ADR 0007/0011).
  **Review:** Approved. Implementation matches ADR 0011 exactly — all five `swapSlots` preconditions now throw consistently, eager two-phase validate-then-commit, error message names host/target/every failing prop and why (consistent with S3's actionable-error precedent, same as `InvalidSelectorError`). Independently re-ran the full suite (239/239), both `tsconfig` typechecks, and the linter — all clean. Specifically verified the atomicity claim is real, not just asserted: temporarily moved the `throw` to *after* the commit loop (reintroducing the pre-fix ordering) and reran the `'does not leave a partial swap...'` test — it failed exactly as predicted (`slot.current()` no longer matched the original signal); reverted and reran clean. No DX concerns: `Map<string, string>` as the error's internal `reasons` parameter is fine since consumers only need `instanceof InvalidPassPropertyError`, never construct it themselves. Confirmed via `examples/` that no shipped component or E2E spec exercised the old silent-failure paths, so this lands with no hidden fallout.

- [x] LT-015: Support `TrustedHTML` in `dangerouslyBindInnerHTML` via the `sanitize` hook — reviewed ✓
  **Skill:** le-truc-dev
  **Context:** Architect review of LT-005. Trusted Types reached cross-browser **Baseline 2026** (Chrome/Edge since v83, Safari, Firefox in early 2026 — [MDN](https://developer.mozilla.org/en-US/docs/Web/API/Trusted_Types_API), [Can I Use](https://caniuse.com/?search=trusted%20types)). On a page with `Content-Security-Policy: require-trusted-types-for 'script'`, the `target.innerHTML = html` assignment in `dangerouslyBindInnerHTML` throws unless `html` is a `TrustedHTML` instance. Le Truc's only `innerHTML` sink is this function, so TT-enforced consumers currently cannot use it without a per-app policy exemption. The `sanitize` hook added in LT-005 is the natural place to close the gap.
  **Changed:** `src/bindings.ts` (new locally-declared, exported `TrustedHTML` type; widened `DangerouslyBindInnerHTMLOptions.sanitize` to `(html: string) => string | TrustedHTML`; cast at the `target.innerHTML = html` assignment since `lib.dom.d.ts` types `innerHTML` as `string` only; JSDoc on both the option and the function); `index.ts` (export `type TrustedHTML`); `types/` regenerated; `examples/test/audit/test-audit.ts` (new `audit-trusted-html` component whose `sanitize` hook wraps the existing on*-stripper in a real `TrustedHTML` via `window.trustedTypes.createPolicy(...).createHTML(...)`, standing in for DOMPurify's `RETURN_TRUSTED_HTML: true`); `examples/test/audit/test-sanitize-tt.html` + `test-sanitize-tt.spec.ts` (new E2E, kept separate from `test-sanitize.*` so the LT-005 control test — which relies on an *unenforced* page — is untouched). **Public API added:** `TrustedHTML` type, exported from `./src/bindings` and `index.ts`.
  **How:** Widened the `sanitize` return type to `(html: string) => string | TrustedHTML`; `TrustedHTML` is declared locally as `{ toJSON(): string }` (not global) because this project's `lib.dom.d.ts` doesn't ship the real type — structural typing means the genuine DOM `TrustedHTML` still satisfies it. The cast at the assignment site (`(target as { innerHTML: string | TrustedHTML }).innerHTML = html`) reflects the DOM's actual runtime contract, not a type escape. External contract (`SingleMatchHandlers<string>`) unchanged — additive. The CSP itself is delivered in the E2E spec via Playwright route interception (`route.fulfill` overriding the `content-security-policy` response header) rather than a layout-level `<meta>` tag, because the shared `docs-src/layouts/test.html` `<head>` is common to every `/test/*` page — a page-wide CSP there would also break the existing LT-005 control test (which proves the vector is live on an *unenforced* page). Verified directly with both Chromium and WebKit (Playwright 1.61.1 bundles both with native Trusted Types support) before writing the spec.
  **Check:** Unit suite 168/168. Typecheck (`tsc -p tsconfig.build.json` and `tsconfig.json`) clean. Linter clean. **E2E: `test-sanitize-tt.spec.ts` 6/6 green (Chromium + WebKit)** — asserts the page actually enforces Trusted Types (sanity check), that `audit-trusted-html`'s `TrustedHTML`-returning hook satisfies enforcement and still strips the `onerror` vector, and that `audit-sanitize`'s plain-string-returning hook (from LT-005) *still throws* under enforcement — proving the hook's return *type* is what matters, not merely the presence of a `sanitize` option. Full E2E suite 836/836 (was 830; +6, 10 pre-existing skips unrelated to this change).
  **Review:** Approved for the `ok`/non-empty-html path — the `sanitize` widening, `TrustedHTML` type, and cast match ADR-0010's Decision section verbatim, including the Option B rejection. Re-verified independently against real Chromium + WebKit (not just trusting the spec text): structural `{ toJSON(): string }` does match real DOM `TrustedHTML` instances; a plain-string `sanitize` result does throw under enforcement; a `TrustedHTML`-returning one does not.

  **Gap found, not blocking, tracked as LT-016:** `reset()` inside `dangerouslyBindInnerHTML` — used by the `nil` handler and by the `!rawHtml` short-circuit in `ok` — assigns a raw string directly (`element.innerHTML = ''` or `shadowRoot.innerHTML = '<slot></slot>'`) and never calls `sanitize`. Verified empirically (Chromium + WebKit, see LT-016): under `require-trusted-types-for 'script'`, *any* plain-string `innerHTML` assignment throws — including the empty string, there is no spec carve-out — so this throws unconditionally on reset/clear, regardless of a correctly-configured `sanitize` hook. Reproduced live against the actual `audit-trusted-html`/`audit-sanitize` components on `test-sanitize-tt`: both throw on initial connect (their `content` prop starts at `''`). The throw doesn't crash the page or fail a test, which is exactly the problem: `@zeix/cause-effect`'s `match()` catches it internally (no `err` handler is wired in `dangerouslyBindInnerHTML`'s `SingleMatchHandlers`, so it falls back to `console.error`) — the failure is silent, content fails to clear, and the existing `test-sanitize-tt.spec.ts` suite never notices because it only checks `pageerror` and only exercises the non-empty `ok` path. This means ADR-0010's claim that TT-enforced consumers get "full compliance through the same single hook" is currently inaccurate for the reset/clear path; addended ADR-0010 accordingly.

- [x] LT-016: Make `dangerouslyBindInnerHTML`'s reset/clear path Trusted-Types-safe — reviewed ✓
  **Skill:** le-truc-dev
  **Context:** LT-015 review finding (Architect). `reset()` in `src/bindings.ts` — invoked by the `nil` handler and by the `!rawHtml` branch inside `ok` — bypasses `sanitize` entirely and assigns a raw string straight to `innerHTML` (`''` for the plain case, the literal `'<slot></slot>'` for the shadow-DOM case). Verified empirically against real Chromium and WebKit: under `Content-Security-Policy: require-trusted-types-for 'script'`, *any* plain-string `innerHTML` assignment throws a `TypeError` — including the empty string; there is no spec carve-out for it. So on a TT-enforced page, every component using `dangerouslyBindInnerHTML` throws on reset/clear — which includes initial connect whenever the bound property starts at `''`, the common case — no matter how correctly `sanitize` is configured, because `reset()` never calls it. Confirmed live: both `audit-trusted-html` and `audit-sanitize` (the LT-015 test components, `content` initialized to `''`) throw on connect today under a TT-enforcing CSP. It's silent rather than loud: `@zeix/cause-effect`'s `match()` (`node_modules/@zeix/cause-effect/index.dev.js`, the `match` function) wraps the call to `ok`/`nil` in a try/catch and reroutes uncaught exceptions to `handlers.err ?? console.error`; `dangerouslyBindInnerHTML` wires no `err` handler, so the throw becomes a `console.error` call, not a `pageerror`. The existing `test-sanitize-tt.spec.ts` only asserts on `pageerror` and only exercises the non-empty `ok` path, so it does not catch this.
  **Changed:** `src/bindings.ts` (`reset()` inside `dangerouslyBindInnerHTML`; JSDoc `nil` bullet and new Trusted-Types-reset paragraph); `examples/test/audit/test-sanitize-tt.spec.ts` (2 new E2E tests). No public API signature change — `reset()` is internal; `SingleMatchHandlers<string>` is unchanged.
  **How:** `reset()` no longer touches `innerHTML` at all: `element.replaceChildren()` for the plain-element case, `element.shadowRoot.replaceChildren(document.createElement('slot'))` for the shadow-DOM case (a real `<slot>` element built via `createElement`, replacing the `'<slot></slot>'` string literal). Did not route the reset through `sanitize(rawHtml)` — a consumer's sanitizer might not wrap falsy input in `TrustedHTML`, and the shadow-DOM case has no `rawHtml` to sanitize in the first place. `replaceChildren()` never parses HTML, so it isn't a Trusted Types sink and needs no `sanitize` hook or library-owned `TrustedTypePolicy` (consistent with ADR-0010's Option B rejection). The non-empty `ok` path (the actual `sanitize`/`TrustedHTML` plumbing from LT-015) is untouched.
  **Check:** Unit suite 168/168. Typecheck (`tsc -p tsconfig.build.json` and `tsconfig.json`) clean. Linter clean (`bun run lint`; the unrelated `lint:examples` Markdown-checker failures pre-exist and are untouched by this change). **E2E: `test-sanitize-tt.spec.ts` 10/10 green (Chromium + WebKit)** — 2 new tests added: "content starting at the empty string does not throw on connect" (re-navigates with `console`/`pageerror` listeners attached *before* the first `connectedCallback`, reproducing and closing the exact silent-failure window found in review) and "setting content back to the empty string clears it without throwing" (`audit-trusted-html` only — `audit-sanitize`'s plain-string hook legitimately still can't display non-empty content under enforcement, per the existing LT-015 "still throws" test; asserts the target's `innerHTML` is empty after reset and that no `pageerror`/`console.error` fired). Confirmed the two `console.error` calls reproduced during the LT-015 review are gone. Full E2E suite 840 passed, 10 skipped (pre-existing, unrelated).
  **Review:** Approved. `replaceChildren()` / `replaceChildren(document.createElement('slot'))` is the right fix — it sidesteps the Trusted Types sink entirely rather than making assumptions about a consumer's `sanitize` hook, exactly per ADR-0010's Option B reasoning. The narrowed E2E scope (`audit-trusted-html` only for the "had content, now reset" case) is correct: `audit-sanitize` genuinely cannot display non-empty content under enforcement at all, so there's nothing meaningful to reset there — re-confirmed by reading `test-sanitize-tt.spec.ts` in full. Re-ran the full suite myself: unit 168/168, both `tsconfig` typechecks clean, `test-sanitize-tt.spec.ts` 10/10 (Chromium + WebKit), full E2E 840 passed / 10 pre-existing skips.

  **Separate gap found while re-reading the function, not blocking, tracked as LT-017:** `reset()` (both the `nil` handler and the `!rawHtml` branch in `ok`) runs synchronously and never calls `schedule()`, while the non-empty `ok` path defers its write via `schedule(element, ...)`. This predates both LT-015 and LT-016 — neither touched *when* `reset()` runs, only *what* it writes — but it's a real, reproducible bug: if a non-empty write is scheduled for an element and then, in the same synchronous tick (before the next animation frame), the same element's bound value goes falsy, the synchronous `reset()` clears the DOM immediately, but the *already-scheduled, now-stale* non-empty write still fires on the next frame and overwrites the reset right back in. Reproduced empirically against the existing `audit-sanitize` component on the LT-005 `test-sanitize` page (no Trusted Types involved — this is a plain scheduling-order bug): `el.content = '<p>A</p>'; el.content = ''` in the same tick leaves `<p>A</p>` in the DOM after the next frame instead of clearing it. Confirmed non-empty→non-empty same-tick writes correctly dedupe via `schedule()` (later write wins), isolating the bug specifically to `reset()` bypassing the scheduler. This violates REQUIREMENTS.md S5: "rapid re-fires of the same helper within one frame collapse to a single write" — the survivor should be whichever write happened last, not whichever happened to go through `schedule()`.

- [x] LT-017: Route `dangerouslyBindInnerHTML`'s reset path through the scheduler — done ✓
  **Skill:** le-truc-dev
  **Context:** LT-016 review finding (Architect). `reset()` in `src/bindings.ts` runs synchronously and never calls `schedule()`, unlike the non-empty `ok` path which defers its write via `schedule(element, ...)`. If a non-empty write is scheduled for an element and the bound value then goes falsy in the same synchronous tick (before the next animation frame), `reset()` clears the DOM immediately, but the stale already-scheduled write still fires next frame and silently overwrites the reset. Reproduced empirically (Chromium, no Trusted Types involved — plain scheduling-order bug) against `audit-sanitize` on the existing `test-sanitize` page: `el.content = '<p>A</p>'; el.content = ''` in the same tick leaves `<p>A</p>` in the DOM after the next frame. Confirmed non-empty→non-empty same-tick writes dedupe correctly (later wins) — the bug is isolated to `reset()` bypassing `schedule()`. Violates REQUIREMENTS.md S5 ("rapid re-fires of the same helper within one frame collapse to a single write" — the survivor should be whichever write happened last).
  **Changed:** `src/bindings.ts` (both call sites of `reset()` inside `dangerouslyBindInnerHTML`; JSDoc `nil` bullet); `examples/test/audit/test-sanitize.spec.ts` (2 new E2E tests). No public API signature change — `reset` is internal, `SingleMatchHandlers<string>` unchanged; only the internal timing of an existing code path changed, so per the post-task protocol this is classified as a bug fix, not an API-surface change.
  **How:** `nil: reset` → `nil: () => schedule(element, reset)`; the `!rawHtml` branch in `ok` changed `reset()` to `schedule(element, reset)`. Both now go through the same per-element dedup `WeakMap` in `src/scheduler.ts` as the non-empty write, so whichever of the two is scheduled last for a given element is the one that runs — matching the already-correct non-empty→non-empty dedup behavior.
  **Check:** Unit suite 168/168. Both `tsconfig` typechecks clean. Linter clean. Added 2 E2E tests to `test-sanitize-tt.spec.ts`'s sibling `test-sanitize.spec.ts` (no Trusted Types needed — this bug is orthogonal to LT-015/016): "a reset cannot be clobbered by an earlier-scheduled, now-stale write" (the exact repro: non-empty then empty, same tick) and "a non-empty write after a reset, same tick, still wins" (regression guard for the opposite ordering). **Verified the regression test actually catches the bug**: temporarily reverted just the two `schedule()` call sites and reran — both new tests failed on Chromium + WebKit with the predicted stale content (`"<p>stale</p>"` instead of `""`); reapplied the fix and reran clean. Full E2E suite 844 passed, 10 skipped (pre-existing, unrelated) — was 840, +4 (2 in `test-sanitize-tt.spec.ts` from LT-016, 2 new here).
