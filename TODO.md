# TODO

- [x] LT-001: Merge-based validity composition + `relayValidity()` helper — reviewed ✓
  **Skill:** le-truc-dev
  **Changed:** `src/extensions/form.ts` (new `VALIDITY_FLAG_KEYS`/`NATIVE_VALIDITY_FLAG_KEYS` constants, new `ValidatableControl` type, new `mergeValidity()` internal primitive, rewritten `managedSetCustomValidity`, new exported `relayValidity()`), `index.ts` (barrel export: `relayValidity`, `type ValidatableControl`), `src/tests/form.test.ts` (fixed `FakeElementInternals.setValidity` to replace-not-merge, matching real `ElementInternals` semantics; 7 new tests).
  **Review:** Approved, including the `customError` exclusion in `relayValidity()` — correct call, not a deviation to walk back. Copying the control's own `customError` verbatim would have broken the exact composition property this task exists to deliver (a wrapped control's native flags and an externally-set `customError`, e.g. a parent's cross-field constraint, coexisting on the same `internals` — issue #98's own motivating scenario). [ADR 0020](adr/0020-merge-based-validity-composition-and-relayvalidity.md) §2 amended in place (unpublished, so editable directly) to match the implementation and document the rationale. `ValidatableControl`/barrel export look right for DX — structural typing means `HTMLInputElement`/`HTMLSelectElement`/`HTMLTextAreaElement` satisfy it with no casts at real call sites. Docs follow-up: see LT-002.

- [ ] LT-002: Document `relayValidity()` in the form-associated components guide
  **Skill:** tech-writer
  **Context:** `relayValidity()` and `mergeValidity`'s composition fix (ADR 0020) are new public-API-adjacent behavior with no docs-src coverage yet. Add `relayValidity(internals, control, anchor?)` to the form-associated guide alongside the existing `setCustomValidity()`/typed-validity-flags material (see ADR 0016 §7, ADR 0020), with a short "enhanced native input" example (a control wrapping a native `<input>`) showing it re-invoked from an `on(control, 'input', …)` handler. Note explicitly that it excludes the control's own `customError` so it composes safely with `setCustomValidity()` on the same component.
