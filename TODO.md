# TODO

- [x] LT-001: Sidebar navigation — pipeline and layout templates — reviewed ✓
  **Skill:** docs-server-dev
  **Context:** The docs menu is currently a full-width card grid (`server/templates/menu.ts` → `menuItem()`/`menu()`) included identically into every page's `<header>` (`docs-src/layouts/page.html`, `api.html`, `example.html`, `overview.html`; `blog.html` doesn't include it at all today). At 16 pages this no longer scales — replace it with a persistent left sidebar with grouped headings, second time around driven by the new `MENU_GROUPS` config (added in `server/config.ts` alongside the existing `CHAPTERS`/`PAGE_ORDER`).

  **Behavior to build:**
  1. Rewrite `menuItem()`/`menu()` in `server/templates/menu.ts` to render nested `<ul>` groups from `MENU_GROUPS` (in `MENU_GROUPS` order; pages within a group sorted by `PAGE_ORDER`, same as today's `createOrderedSort` usage) instead of iterating `CHAPTERS`. Drop the `<small>${page.description}</small>` caption — item is icon + title only. Keep the emoji icon.
  2. Mark the current page: add `aria-current="page"` and `class="active"` to the menu item matching the page being rendered. This requires per-page rendering instead of writing one cached shared fragment (`MENU_FILE`/`writeFileSafe` in `server/effects/menu.ts`) included verbatim into every page — render the menu per page inside `applyTemplate()` in `server/effects/pages.ts` (same place `{{ section }}` and other per-page template vars are substituted), passing the current page's slug. Update `server/effects/api-pages.ts`/`api.ts`, `examples.ts`, and any other effect that renders a layout with the menu include, consistently.
  3. Restructure `docs-src/layouts/page.html`, `api.html`, `example.html`, `overview.html` (and `blog.html`, closing its existing menu gap) from single-column-with-header-menu to a two-column shell: sidebar + existing `.content-grid` main area, unchanged otherwise. Slim `<header>` down to a top bar (title, mobile toggle button, the existing error `card-callout`) — the menu itself moves out of `<header>` into the new sidebar markup, still rendered via `menu()`.
  4. Do **not** touch the existing secondary/local navigation each of these layouts already has — `api.html`'s `.api-breadcrumb`/`.api-nav`, `overview.html`'s `.overview-pagination`, or any content-level `{% listnav %}`/`<module-listnav>` usage inside pages. Those stay exactly as they are; the new sidebar wraps around them, it doesn't replace them.
  5. `<context-router>` (present in every layout) is inert markup left over from a dropped SPA router — leave it as-is, no client-side routing is being reintroduced. Full page navigation (MPA) is correct here.
  6. Sidebar interactivity (open/close on mobile, `aria-expanded` sync) belongs to LT-002 — coordinate the markup contract (toggle button id/target, sidebar element) with that task so the two land compatibly. If LT-002 hasn't landed yet, the sidebar should still render correctly and be usable (just not collapsible) on its own.

  **Acceptance criteria:** `bun run build:docs` green; `bun test server/tests` green (extend `server/tests/config.test.ts`-style coverage: every `PAGE_ORDER` page belongs to exactly one `MENU_GROUPS` group, and each group's members are contiguous in `PAGE_ORDER`); every existing page URL and anchor still resolves; the active page is visibly marked in the sidebar on every layout; blog post pages (`blog.html` layout) now include the sidebar.

  **Changed:**
  - `server/templates/menu.ts` — `menuItem()`/`menu()` rewritten to render `MENU_GROUPS` (was `CHAPTERS`) with `groupOf()` replacing `chapterOf()`; dropped the `<small>` description caption; both now take an optional `currentSlug` and mark the matching item `aria-current="page"` + `class="active"`; `<section-menu>` root now carries `id="sidebar"`.
  - `server/effects/pages.ts` — `applyTemplate()` gained a `rootPages: PageInfo[]` parameter and now renders `menu(rootPages, currentSlug)` into a new `{{ menu }}` replacement on every call; `pagesEffect` builds `rootPages` once per build from `rootPagesBySlug` (already computed for the chapter stepper) and passes it through. `currentSlug` is the page's own slug for root pages, or `processedFile.section` (e.g. `"blog"`, `"api"`) for pages nested under one, so a blog post or API symbol page marks its parent's sidebar item active.
  - `server/effects/menu.ts` deleted, `menuEffect` dropped from `server/build.ts`, `MENU_FILE` dropped from `server/config.ts`, `docs-src/includes/menu.html` deleted — the cached-shared-fragment/include approach is fully replaced by per-page rendering.
  - `docs-src/layouts/{page,api,example,overview,blog}.html` — restructured into a sidebar + main two-column shell: slimmed `<header>` (title + `#sidebar-toggle` button with `aria-controls="sidebar"` + the existing error `card-callout`), followed by a `.docs-body` wrapper holding `{{ menu }}` and the layout's existing main content (wrapped in `.docs-main` for `api.html`/`example.html`/`overview.html`, which have extra header/meta sections alongside `<main>`). `blog.html` gained the sidebar for the first time. No new CSS added — `.docs-body`/`.docs-main`/`.sidebar-toggle` are unstyled markup hooks for LT-002's `section-menu.css` (currently they stack as plain blocks, which is correct per point 6: usable, not yet a visual two-column layout, until LT-002 lands).
  - `server/tests/config.test.ts`, `server/tests/templates/menu.test.ts` — updated/extended for `MENU_GROUPS` (membership, contiguity, uniqueness) and the new `menuItem`/`menu` active-marking behavior; dropped `MENU_FILE`/description-caption assertions.
  - `server/SERVER.md`, `server/TESTS.md` — updated pipeline diagram, effects table, template table, config table, and the Guide Chapters section (now "Sidebar Menu Groups" + "Guide Chapters") to describe per-page menu rendering and the `MENU_GROUPS`/`CHAPTERS` relationship; removed all `menuEffect`/`MENU_FILE`/`docs-src/includes/menu.html` references.

  **How:** Moved menu rendering from a one-time cached fragment (`menuEffect` → `docs-src/includes/menu.html` → `{{ include 'menu.html' }}`) to a per-page call inside `applyTemplate()`, since marking the active page requires knowing which page is being rendered. `apiPagesEffect`/`apiEffect`/`examplesEffect` needed no changes — none of them call `applyTemplate()` or otherwise render a layout (they only ever produced fragments or fed markdown back into the `docsMarkdown` pipeline, which `pagesEffect` already owns); the include was only ever used from `docs-src/layouts/*.html`, all five of which are covered by the `pagesEffect` change.

  **Check:** (1) The markup contract for LT-002: `#sidebar-toggle` button (`aria-controls="sidebar"`) in the header, `<section-menu id="sidebar">` as the drawer root, `.docs-body`/`.docs-main` wrapper classes available for the two-column grid + sticky positioning — confirm these match what LT-002 actually binds to once it lands. (2) `example.html` and `overview.html` are restructured but still unreferenced by any page's frontmatter (`layout: page` is used everywhere `example`/`overview` might apply) — same as before this task; not something LT-001 introduced, but worth flagging in case that's an oversight rather than intentional. (3) `bun run build:docs`, `bun test server/tests` (746 pass), `bunx biome check ./server`, and `bun run check:links` (196/196 resolve) all green; verified active-page marking and group headings in the built `docs/index.html`/`docs/components.html`/`docs/blog/*.html` output directly.

  **Review:** Approved. Markup contract matches LT-002 exactly, active-page marking and the blog gap fix both verified directly in built output, full `server/tests` + `check:links` green independently reproduced. One layout bug found during review (see LT-002's review note) and fixed in `examples/_global.css` — not this task's fault, but adjacent enough to note here too: the new sidebar's ~300px reduced the horizontal budget available to `.content-grid`, which exposed a pre-existing CSS Grid blowout (code blocks forcing the grid track wider than viewport) that real content only triggers once the budget got tight. `example.html`/`overview.html` being unreferenced by any page (point 2 above) is confirmed pre-existing, left as-is.

- [x] LT-002: `section-menu` component — sidebar interactivity and styling — reviewed ✓
  **Skill:** le-truc-dev
  **Context:** `section-menu` (`examples/section/menu/`) is currently a no-op custom element (`class SectionMenu extends HTMLElement {}` in `examples/main.ts`) styled as a full-width card grid (`section-menu.css`). LT-001 (docs-server-dev, in `TODO.md`) is restructuring the site layout to use `section-menu` as a persistent left sidebar instead. This task makes it a real, working component and restyles it for that role, using only tokens already defined in `examples/_global.css` (spacing scale, `--color-*`, `--font-size-*`) — no new tokens.

  **Behavior to build:**
  1. Turn `SectionMenu` into an actual Le Truc component (`defineComponent`): manages open/closed state for the mobile off-canvas drawer, syncs `aria-expanded` on its toggle button, closes on Escape, on outside click, and on navigation (link click). Coordinate the toggle button's markup (id/attributes) with LT-001, since that task places the button in the slimmed-down `<header>`.
  2. Rewrite `section-menu.css` for the sidebar: `position: sticky`, independently scrollable, fixed width (~16rem, using the `--space-*` scale — pick what reads well against `--content-max-width`/`--breakout-size`), group headings styled like the current `.group` rule (uppercase, `--color-text-soft`, letter-spacing — carry the look forward, just laid out vertically instead of as a grid heading row). Style the active item (`.active`/`[aria-current="page"]`, already used as the selector today) so it's clearly lifted off the sidebar background.
  3. Below the existing `48em` breakpoint already used elsewhere in `_global.css`/`section-menu.css`: sidebar collapses to an off-canvas drawer with a backdrop, opened by the mobile toggle button. Above it: sidebar is always visible, no toggle shown.
  4. Icons stay (emoji, same as today); drop anything the description/caption text was doing layout-wise (grid-template-areas `icon title description` collapses to `icon title`).
  5. Add/update the component's Playwright spec (`module-listnav`'s sibling pattern in `examples/module/listnav/` is a reasonable reference for a nav-style interactive component with its own `.spec.ts`) covering: toggle open/close, Escape closes, outside-click closes, active-item marking renders correctly given a marked-up fixture.

  **Acceptance criteria:** component works standalone in isolation (progressive enhancement — the page's links all work with JS off, since it's an MPA); `bun run build:examples` green; new/updated Playwright spec passes; visually consistent with `_global.css` tokens in both light and dark (`color-scheme: light dark` already set at `:root`).

  **Changed:**
  - `examples/section/menu/section-menu.ts` (new) — real `defineComponent`: exposes `open: boolean`; adds `.js`/`.ready` classes on connect (see How); creates its own `.backdrop` child (decorative, JS-only, so server markup doesn't need it); wires an external toggle button by id (`document.getElementById`, not `first()`, since the button lives outside the host — in the header); closes on Escape, outside click, and link-navigation via one `document`-level `click`/`keydown` listener pair, wrapped in `watch(() => true, …)` per the hand-authored-descriptor convention (`module-listnav`'s `hashchange` listener is the prior-art reference cited inline).
  - `examples/section/menu/section-menu.css` — full rewrite. Mobile-first default (no `.js` class): plain in-flow block, all links reachable with JS disabled. `@media (max-width: 47.999em) section-menu.js`: fixed, full-viewport overlay layer (`pointer-events: none`) containing an absolutely-positioned `<nav>` drawer panel (`translateX(-100%)` closed → `translateX(0)` via `.open`) and `.backdrop`; `.ready` (see How) gates the slide/fade transitions separately from the closed layout itself. `@media (min-width: 48em)`: `position: sticky`, `flex: 0 0 16rem`, independently scrollable, `#sidebar-toggle` hidden. Also added top-level `.docs-body`/`.docs-main` rules (flex row ≥48em, block below) — these aren't inside `<section-menu>` but are the two-column shell LT-001 left unstyled on purpose (see its Changed note 21); documented as a scoping exception, same pattern as `module-dialog.css`'s `body.scroll-lock`.
  - `examples/main.ts` — dropped the `class SectionMenu extends HTMLElement {}` no-op registration; added `import './section/menu/section-menu.ts'` (its `defineComponent` self-registers).
  - `examples/section/menu/section-menu.html`, `section-menu.spec.ts` (new) — standalone fixture + 11-case Playwright spec (initial state incl. active-item marking, open/close via toggle, Escape, outside click, no-close on inside click, close on link click, programmatic `.open`).

  **How:** **Toggle-button contract — flag for LT-001:** `#sidebar-toggle` / `aria-controls="sidebar"` / `<section-menu id="sidebar">`. This isn't a guess — LT-001 had already landed on `main` by the time this task ran, and `docs-src/layouts/*.html` + `server/templates/menu.ts` already use exactly these ids, so `section-menu.ts`'s `TOGGLE_ID` constant and `section-menu.css`'s `#sidebar-toggle` rules match what's actually in the built output (verified: `bun run build:docs` then `grep` on `docs/index.html`). Nothing further for LT-001 to reconcile here.
  Two non-obvious fixes worth flagging in case similar patterns show up elsewhere: (1) inside a `.docs-body` flex row, `section-menu` needs `flex: 0 0 16rem` — `width: 16rem` alone isn't enough, since flex items default to `min-width: auto` and shrink below their set width to fit min-content when the sibling (`.docs-main`, full of prose) wants more room; without the shrink lock the sidebar visually collapsed to icon-only width. (2) a transition on a property gates against the *previous* computed style, including the very first one — `transition: visibility 0s linear var(--transition-short)` on the closed-drawer rule delayed the *first* application of `.js`'s closed state by 200ms on every page load (drawer visibly "open" briefly, non-deterministically caught by Playwright depending on machine load), and even after switching to a transform-only approach, an un-gated `transition: transform …` still animated the drawer visibly sliding shut on load. Fixed by adding `.ready` a `requestAnimationFrame` after `.js` (forces the closed layout to commit as a paint with no active transition, before transitions are enabled for subsequent user-triggered opens/closes) — CSS gates `transition:` declarations on `&.ready` rather than on `&.js` directly.
  Structural-chrome components (`section-hero`, `card-blogpost`, `module-toc`, …) skip the `@demo` JSDoc tag / `.md` gallery entry since they have no `examples.html` showcase entry; `section-menu` follows that precedent now that it's real code, not just markup.

  **Check:** `bun test src/tests` (451 pass) and `bun run test` (832 pass, 10 pre-existing skips — full suite incl. Playwright) both green, run twice back-to-back with default parallel workers to confirm the transition race is actually fixed and not just quieter; `bunx biome check` clean; `bun run build:examples` and `bun run build:docs` both green. Visually verified via Playwright screenshots (not just assertions) at desktop 1200px (light + dark, `emulateMedia`) and mobile 400px (closed + open) against the real `bun run build:docs` output — sidebar renders correctly, active item lifted, group headings styled, mobile drawer starts closed and opens/closes cleanly with backdrop.

  **Review:** Approved. `section-menu.ts`/`.css` re-read in full; the `flex: 0 0 16rem` and `.ready`-gated-transition fixes are both correct, well-reasoned, and the inline comments explaining them are exactly the kind of non-obvious "why" worth keeping. Re-ran the Playwright spec independently (20/20 pass, Chromium + WebKit) and the full example suite (421/421 pass on Chromium) after the `_global.css` fix below, to confirm no interaction.
  Found one real bug via screenshot verification at common laptop widths (1280–1440px, not covered by the spec's 1200px/400px checkpoints): `document.documentElement.scrollWidth` exceeded `clientWidth` by up to 394px — horizontal page overflow, not just a cramped sidebar. Traced it to a pre-existing CSS Grid blowout in `.content-grid` (`examples/_global.css`): a Shiki code-block line without `min-width: 0` on its grid-item ancestor forces the whole `main.content-grid` track wider than its `minmax()`/`min()` clamp intends, once the horizontal budget outside it (now ~300px smaller, eaten by the sidebar) gets tight enough to matter — this was always latent, just never surfaced before because there was always enough spare width. Fixed with one rule: `min-width: 0` added to `.content-grid > .content, > section` in `examples/_global.css` (the standard fix for this exact grid-item-refuses-to-shrink gotcha). Verified: 0px overflow at 1280/1366/1440/1920px afterward; full `bun test src/tests server/tests` (1197 pass), `bun run check:links` (196/196), and the full Playwright suite (421/421 on Chromium) all still green — the fix is additive (only relaxes an implicit minimum), so nothing else regressed. Also tried an initial `min-width: 0` on `section-hero`'s own `2fr 1fr` breakout grid first, suspecting its in-hero TOC box specifically; kept that too (harmless, and it is the more specific fix for that one element) but it alone did not resolve the actual page-level overflow — the `.content-grid` rule was the real fix.

## Notes for future work (no action needed now)

- User flagged that blog post pages might want their own secondary navigation (an archive/recent-posts list, similar in spirit to the existing `.blog-archive` list already used on the blog index) in addition to the new global sidebar. Not specified enough to task out yet — revisit if/when it comes up.
- A "Playground" page is planned for v3.0, to sit between the prose guide and the Reference group. `MENU_GROUPS`/`PAGE_ORDER` should accommodate an extra group without restructuring what's here.
