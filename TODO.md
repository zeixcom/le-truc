# TODO

Task queue for the [ADR 0026](adr/0026-aria-reflection-via-elementinternals-and-bindaria.md) ARIA-reflection work and the v2.6 train. The proof-of-concept (LT-001–LT-006) is complete — gate outcome: GO, ADR 0026 ✅ Accepted and re-targeted from v3.0 to **v2.6** (surface purely additive; 3.0 awaits TSRX 1.0 per ADR 0024). Scope rule (for the PoC tasks): prove each planned channel against the **hardest** real case only; features of similar or simpler shape, and the migration of the example components, are deferred until after the decision gate (LT-006). PoC code lives under `test/poc/` (not `examples/` — that is the published demo surface) with its own Playwright project and a served PoC page; PoC components are throwaway and must not enter the public API.

- [x] LT-001: Computed-accessibility ground-truth test harness — done ✓
  **Skill:** le-truc-dev
  **Changed:** `test/poc/` (new): `serve.ts` (Bun server on :3100, on-demand `Bun.build` bundling with `DEV_MODE="true"`), `pages/basic.html` + `poc-basic.ts` (raw-custom-element probe: internals-only / attribute-only / both), `fixtures/aria.ts` (CDP `Accessibility.getFullAXTree` ground-truth reader, Playwright ariaSnapshot tier, axe-core 4.13 in-page runner over the declaration registry), `poc.spec.ts` (pinned observations), `README.md` (findings matrix); `playwright.config.ts` (poc-chromium/firefox/webkit projects on their own testDir + second webServer); `package.json` (axe-core devDependency).
  **How:** `page.goto('/basic')` probes distinguish the three semantic channels in every configured engine; Chromium-only CDP tests assert the engine's computed tree, all-engine tests assert the tooling and axe tiers. Full suite green: 402 unit, 12 PoC, 771 examples.
  **Check:** Harness distinguishes attribute-set from internals-set semantics in all engines ✓ — findings in `test/poc/README.md`.
  **Correction (2026-08-31, during LT-002):** the original baseline wrote `internals.ariaValuenow` (lowercase `n`) instead of the real IDL attribute `ariaValueNow` — a silent no-op data-property assignment, not a setter call. This produced a false "internals-set `aria-valuenow` never reaches the AX tree" finding. Fixed in `poc-basic.ts`/`poc.spec.ts`/`README.md`: internals-set `role`, `aria-label`, AND `aria-valuenow` all reach the Chromium AX tree, same as their attribute-set equivalents. Playwright's tooling tier still ignores internals on all engines; host-attribute override + reassert semantics confirmed for both name and value; axe 4.13 produces zero internals-related false positives via the registry.
  **Review:** Approved (pre-correction) and stands post-correction — the harness itself was sound; only one probe property was mis-cased. No public API touched.

- [x] LT-002: Host default-semantics channel PoC — hardest case: colorgraph-style hue slider — done ✓
  **Skill:** le-truc-dev
  **Changed:** `test/poc/`: `poc-hue-slider.ts` (new, raw custom element, `internals.role='slider'` + reactive `ariaValueNow`/`ariaValueText` via `throttle()`), `poc-stale-expanded.ts` (new, no-mixing wrinkle probe), `pages/hue-slider.html` (new), `hue-slider.spec.ts` (new, pinned observations), `README.md` (LT-002 findings + LT-001 correction); `serve.ts` (fixed 2 pre-existing TS2345 regex-capture-group errors, unrelated to LT-002 but blocking `tsc --noEmit`).
  **How:** `setHue(deg)` is the deterministic test entry point (no simulated pointer events); Chromium-only CDP tests assert the computed tree, all-engine test asserts throttle dedup via a `flushCount` counter.
  **Check:** Computed tree shows slider role + live `valuenow`/`valuetext` in Chromium ✓ (WebKit ground truth remains unobservable via Playwright — standing limitation, documented in LT-001). Override/restore behaviors match ADR 0026's claims ✓. No glitching under high-frequency updates: 50 synchronous `setHue()` calls collapse to exactly 1 throttled flush ✓. No-mixing wrinkle confirmed real (stale `aria-expanded` permanently shadows internals) with a working mitigation (drop the stale attribute at connect, before first reflecting via internals) — open question for LT-006: should this become part of `bindAria()`'s contract or stay an author responsibility?

- [x] LT-003: Element-reference channel PoC — hardest case: combobox pattern — done ✓
  **Skill:** le-truc-dev
  **Changed:** `test/poc/`: `poc-combobox.ts` (new, real `defineComponent` — not a raw custom element, since `all()`'s reactive Cell is a library concern), `poc-late-ref.ts` (new, `:not(:defined)` edge-case probe), `pages/combobox.html` (new), `combobox.spec.ts` (new, pinned observations), `fixtures/aria.ts` (extended: `propValue()` reads CDP `relatedNodes` for element-reference properties, which carry no plain string value; `AriaNodeSnapshot` gained a `description` field for role-dependent description exposure), `README.md` (LT-003 findings).
  **How:** `poc-combobox.ts` wires `ariaControlsElements`/`ariaDescribedByElements`/`ariaLabelledByElements`/`ariaErrorMessageElements` (static) and `ariaActiveDescendantElement` (reactive, retargeted via ArrowUp/ArrowDown against an `all('[role="option"]')` Cell). Chromium-only CDP tests assert the computed tree; the attribute-clearing side-effect test runs on all engines.
  **Check:** Computed tree reflects each relationship in Chromium ✓ (WebKit/Firefox ground truth remains unobservable via Playwright's CDP access — standing LT-001 limitation). `:not(:defined)` targets confirmed immune to the `first()`/`all()` `DEPENDENCY_TIMEOUT` mechanism (`src/helpers/dom.ts`) — an element reference only needs the target's node identity, nothing to wait for. Removed/re-added options under `all()`'s `MutationObserver` retarget correctly ✓. Attribute-clearing side effect confirmed, with a nuance: the attribute is emptied (`''`), not removed. Two fixture-level false negatives caught and fixed before being pinned: (1) element-reference properties report via CDP's `relatedNodes`, not the flat string `computedAriaTree()` originally read — same failure shape as LT-001's `ariaValuenow` typo; (2) `errormessage` only appears once `aria-invalid` is also set. One real bug in the probe itself, not a platform finding: `watch()` wraps its handler in `untrack()`, so reading `all()`'s Cell only inside the handler body (rather than passing it as a `watch()` source) left the option list frozen at its first snapshot — fixed by `watch([activeIndex, options], ...)`; flagged in README.md as a trap for LT-004's `bindAria()` prototype too.

- [x] LT-004: `bindAria()` contract prototype — done ✓
  **Skill:** le-truc-dev
  **Changed:** `test/poc/`: `poc-bind-aria.ts` (new — `bindAria()` prototype, mirrors `src/bindings.ts`'s map-form/`SingleMatchHandlers`/DEV_MODE debug-attribution shape from ADR 0023), `bind-aria.test.ts` (new, bun:test — every mapping row, null-target no-op, debug attribution, `@ts-expect-error` pins), `poc-hue-slider.ts` (LT-002's reactive `ariaValueNow`/`ariaValueText` writes rewired through `bindAria()`, invoked imperatively — no signal graph in this raw custom element), `poc-combobox.ts` (LT-003's `ariaActiveDescendantElement` watch rewired through `bindAria()` + a tracked thunk `watch()` source, replacing the array-source form), `README.md` (LT-004 findings). Merged `next` into this branch first (brings in ADR 0023's now-accepted map-form `bind*` overloads, targets v2.6).
  **How:** `bindAria(target: ARIAMixin | null | undefined, name: keyof ARIAMixin & string)` returns `SingleMatchHandlers<AriaValue>` (`AriaValue = boolean | number | string | Element | readonly Element[]`, deliberately excluding `null`/`undefined` from the generic parameter — required by cause-effect's `SingleMatchHandlers<T extends {}>` constraint — while still guarding `value == null` at runtime, the same "typed optimistically, guarded defensively" split `bindAttribute`'s/`bindStyle`'s map-form `ok(map)` already has). `ok()` coerces boolean→`'true'`/`'false'`, number→decimal string, passes string/`Element`/`Element[]` through, and null-clears; `nil` also null-clears; a nullish target makes every handler a no-op.
  **Check:** Every mapping row exercised by `bind-aria.test.ts` (12 tests, bun:test, no browser) ✓. `@ts-expect-error` pins confirmed load-bearing by temporarily removing one and observing the expected `TS2345` before restoring ✓. Ergonomics verified against the thunk source form (combobox activedescendant — the only single-source form compatible with a `SingleMatchHandlers`-returning helper; the array-source form cannot carry one, a finding in its own right) and imperative standalone invocation (hue-slider, no `watch()` at all); prop-key and bare-Signal source forms were not separately exercised beyond the bun-test level, since no PoC component's ARIA property is driven by either — noted here rather than fabricating a probe for it. Full regression pass green: 422 unit (`bun test src/tests`), 12 bind-aria unit, 0 `tsc --noEmit` errors, biome clean, 26 poc Playwright (stable across 3 repeated runs), 771 examples Playwright.
  **Review:** Approved at the LT-006 gate — reviewed ✓. The signature survives the hard cases; refinements folded into ADR 0026 rather than back into the PoC: (a) the `null`/`undefined` mapping row is documented as a runtime guard under a `T extends {}`-optimistic type, (b) a map form is added for ADR 0023 parity (not prototyped here — the implementation task must prototype it), (c) stale-attribute removal joins the helper's contract (§1). The LT-004 finding that `watch()`'s array-source form cannot carry `SingleMatchHandlers` is deliberately *not* in the ADR — it is a `watch()` question, filed separately.

- [x] LT-005: Element-internals-declaration registration + axe-core gate PoC — done ✓
  **Skill:** le-truc-dev
  **Changed:** `test/poc/`: `poc-truc-registration.ts` (new — prototypes the `Truc` constructor's proposed registration addition: unconditional `globalThis._elementInternals ??= new WeakMap(); registry.set(this, internals)` after `attachInternals()`), `pages/registration.html` (new), `registration.spec.ts` (new, pinned observations), `README.md` (LT-005 findings).
  **How:** `<poc-reg-button-trap>` (`internals.role='button'`, no native button, plus an invalid `aria-checked` content attribute) vs. `<poc-unreg-button-trap>` (byte-for-byte identical, deliberately never registered) on the same page, same `axe.run()` pass — the actual "silent-pass → flagged" comparison. `<poc-reg-button-ok>` is the negative control (registered, no invalid attribute). The connect/disconnect/reconnect identity test moves one instance in and out of the DOM twice via JS.
  **Check:** axe report confirms the trap flips from silent-pass (unregistered) to flagged with `aria-allowed-attr` (registered) ✓; the control stays clean ✓ (registration alone doesn't blanket-flag). One WeakMap entry per instance confirmed: `attachCount === 1` and identical `ElementInternals` object across two remove/re-append cycles ✓ — not from cleanup logic, but because `attachInternals()` lives in the constructor, which only ever runs once per instance. **Finding for LT-006**: of axe 4.13's four named ElementInternals-aware rules (`aria-allowed-attr`, `aria-prohibited-attr`, `aria-required-parent`/`children`, list/listitem), only `aria-allowed-attr`/`aria-prohibited-attr` can actually see an internals-only role in practice — `aria-required-parent`/`aria-required-children` declare `selector: '[role]'` (an attribute-presence CSS selector, confirmed by reading `node_modules/axe-core/axe.js`'s compiled rule metadata) that excludes internals-only elements before role computation ever runs. The originally-planned `list`/`listitem` trap (from that same subset) was tried first and stayed silent regardless of registration for exactly this reason — dropped in favor of the verified `aria-allowed-attr` button case. Full regression pass green: 422 unit, 12 bind-aria unit, 0 `tsc --noEmit` errors, biome clean, 34 poc Playwright (stable across 3 repeated runs), 771 examples Playwright.

- [x] LT-006: PoC findings → ADR 0026 decision gate — done ✓
  **Skill:** architect
  **Decision: GO**, with a narrowed claim about tooling coverage. ADR 0026 moves 🔄 Proposed → ✅ Accepted (2026-08-31), targeting v3.0.
  **Changed:** `adr/0026-*.md` (Status; §1 stale-attribute rule; §2 amendments (a)–(b); §3 axe-reach qualification; §5 testability bound; new **PoC validation** section with per-channel verdicts; corrected/added Consequences; one new rejected alternative; Related), `adr/0016-*.md` (amendment banner on "ARIA reflection: available but not promoted"), `.agents/skills/adr-keeper/references/adr-index.md` (status), `TODO.md` (LT-004 review resolved; LT-007–LT-011 queued).
  **Verdicts:** host default semantics ✅ viable; consumer override/reassert ✅ viable; element references ✅ viable with caveats (`aria-invalid` gate, attribute emptied-not-removed); `bindAria()` contract ✅ viable with a refined signature; registry + axe gate ⚠️ viable but with a materially narrower reach than axe's docs imply; Firefox/WebKit engine trees ❌ unverifiable in CI.
  **Falsified claim:** ADR 0026's Consequences previously asserted the nested-element safety net was restored. It is not — `aria-required-parent`/`aria-required-children` are gated by a `[role]` *attribute* selector, so exactly the nesting case ADR 0016's advisory named stays invisible. Rewritten as a Bad/trade-off with guidance: structural/composite roles stay on the attribute channel; internals reflection is for host-level widget and state semantics.
  **Open question resolved (from LT-002):** stale-attribute removal becomes **part of `bindAria()`'s contract**, not an author responsibility. The gate first argued the opposite (the platform cannot tell a stale SSR echo from a consumer override); that was overturned in review — under ADR 0003/M3 the distinction is *temporal*, so a one-time removal at binding activation clears only the server-rendered echo and leaves post-connect overrides intact. Contract: the server-rendered attribute is the initial value; the component then owns the property reactively via internals. Scoped to `ElementInternals` targets only, and ordered after parsers. Two prerequisites, both specified in §1 and required by LT-007.
  **Out of scope, filed separately:** `watch()`'s array-source form cannot carry `SingleMatchHandlers` (LT-004 finding 2). That is a `watch()` API question, not an ARIA one — kept out of ADR 0026 entirely. Resolved 2026-08-31 at architect review: matrix completion, not a new shape — folded into the v2.6 train as LT-012, no ADR.
  **Check:** ADR accepted with evidence ✓; PoC distilled into the ADR's PoC validation table (raw findings stay in `test/poc/README.md`, referenced from Related) ✓; deferred scope enumerated as LT-007–LT-011 ✓.

- [x] LT-012: Complete `watch()`'s overload matrix — handlers flavor + tuple types for the array-source form — **v2.6 scope** — reviewed ✓
  **Skill:** le-truc-dev
  **Changed:** `src/helpers/reactive.ts` (new `ResolvedReactive`/`ResolvedReactiveValues`/`ResolvedReactiveSignals` mapped types; the array-source overload split into a plain-handler flavor typed as the per-position-inferred tuple and a new `MatchHandlers` overload implemented as a `match(signals, handlers)` pass-through; the single-source final `match()` call narrowed with `as SingleMatchHandlers<any>` — TS cannot correlate the `Array.isArray(source)` early-return with the handlers union); `src/tests/reactive.test.ts` (5 tests: tuple delivery + re-run, mixed-form inference with a load-bearing `@ts-expect-error` pin, `nil` on any unset source → recovery, `err` collection from a rejected Task source, `stale` on a seeded re-computing Task source); `index.js`/`types/` rebuilt; CHANGELOG `[Unreleased]` entry; one ARCHITECTURE.md sentence.
  **How:** `MatchHandlers` was already re-exported from the barrel (cause-effect parity), so no export change. The mapped types resolve each source form exactly as the single-source overloads do (prop key → `P[K]`, `Signal<V>` → `V`, thunk → `Awaited<V> & {}`), so `ok` receives the same per-position value type as its single-source counterpart. The DEV_MODE debug companion stays host-level pulse only (no `bind*`-produced closure to attribute). Binding helpers stay unary — the tracked thunk remains the N→1 reduce step for helper composition, now stated in ARCHITECTURE.md and the CHANGELOG.
  **Check:** `tsc --noEmit` clean repo-wide — including `examples/test/watch/test-watch.ts` (the one live array-form call site, exercised on all three engines), whose `([n, l])` now types `[number, string]`; 427 unit tests pass (+5); bundle ceiling 5/5; biome clean; full Playwright 805 pass / 33 expected engine skips. Pin load-bearingness verified by temporary removal (TS2322 positional mismatch observed, pin restored). Runtime of existing call sites unchanged — `match()` already delivered the tuple; only `{ ok }` became the full handlers object. Out-of-scope decision (array→`bind*` helper composition stays unbuilt; the thunk is the sanctioned spelling) recorded in ARCHITECTURE.md and CHANGELOG rather than an ADR.
  **Review:** Approved (2026-08-31). The handlers flavor is a pass-through to cause-effect's `match()`, whose routing semantics are tested upstream — the new tests correctly pin only the wiring (tuple delivery, per-position inference, nil/err/stale routing). The mapped types mirror the single-source overloads' resolution exactly, so `ok` receives the same per-position value type as its single-source counterpart; both array flavors now invoke handlers identically to the single-source form (raw `match()` pass-through), and the DEV_MODE companion stays host-level for both. The one cast TS forces (`as SingleMatchHandlers<any>` after the array early-return) is necessary and documented. Compile-time-only tightening of a previously-`any[]` surface is minor-appropriate for v2.6.

- [x] LT-007: Implement `bindAria()` in `src/bindings.ts` — **v2.6 scope** — reviewed ✓
  **Skill:** le-truc-dev
  **Changed:** `src/bindings.ts` (`AriaValue` type; `ariaAttributeName()` IDL→attribute mapping — deliberately not kebab-case; `bindAria()` single + map form with the stale-attribute rule; export block + module docstring); `index.ts` (barrel: `bindAria` + `type AriaValue`); `src/internal.ts` (`internalsHosts` reverse-lookup WeakMap, exported module-privately); `src/component.ts` (the reverse lookup's constructor population line — pulled forward from LT-008 per the prerequisite note, so this task is testable standalone); `src/tests/bindings.test.ts` (22 tests: mapping table, map form, stale rule, 4 `@ts-expect-error` pins); `examples/test/aria/` (new: `test-aria.ts` + `test-aria.html` + `test-aria.spec.ts`); `examples/main.ts` (import); `index.js`/`types/` rebuilt; CHANGELOG `[Unreleased]` entries.
  **How:** Close to the planned move from `test/poc/poc-bind-aria.ts`: same coercion table, same "typed optimistically (`AriaValue`), guarded defensively (`value == null`)" split, same debug-attribution branch. The stale-attribute removal lives in the shared `assign()` path with a per-property one-shot `cleared` Set in the handlers closure — the hook analysis from the 2026-08-31 clarification stands (first value assertion; not helper-call time, not every `ok`, never on `nil`/null clears). **One refinement to the recorded plan:** the clarification said "all statically declared names are removed in one sweep at the first `ok`"; implementation analysis found the sweep wrong for the map form — a nullish entry must *clear* that property, and §2 says clearing "restores attribute authority", so sweeping would blank properties the component has only cleared, never asserted. Removal is therefore **per property, at that property's own first non-nullish assertion**; reduces exactly to the single-form semantics. Test-component note: parsers read the attribute named by the *prop* (`this.getAttribute(key)`), so an `aria-expanded` echo cannot be adopted by a parser — `<test-aria>` adopts it with a factory-time `host.getAttribute('aria-expanded')` read before wiring the binding, which is the same ordering the rule requires (adoption before activation).
  **Check:** All green: `tsc --noEmit` clean; 451 unit tests (+22); biome clean; bundle tiers 5/5 (minimal 8412 B / 9216 B ceiling); full Playwright 823 pass / 33 expected engine skips — including the new spec on Chromium AND WebKit: (a) SSR `aria-expanded="false"` adopted (prop reads `false`), attribute removed at first assertion, internals `'false'` read back through the declaration registry, later reflection updates reach internals; (b) consumer override set post-connect **survives a subsequent binding update** (the sharpened check — a single-read assertion would have passed a buggy every-`ok` removal); (c) pending Task source keeps the attribute authoritative while routing `nil`, removes only at the first resolved assertion; (d) `Element` target (`#inner` span) mirrors via the attribute with no removal. IDL→attribute mapping unit-tested across all four shapes plus the kebab trap (`ariaLabelledByElements` removes `aria-labelledby`, leaves `aria-labelled-by` alone).
  **Review:** Approved (2026-08-31). Implementation matches ADR 0026 §1/§2 point for point — per-property one-shot removal in the shared `assign()` path, `nil`/nullish clears restoring attribute authority, `Element` targets exempt, unregistered internals degrading to a no-op; the per-property refinement of the recorded sweep wording is correct for the map form and the ADR was aligned in the same change set. DX is consistent with the `bind*` family: `keyof ARIAMixin` naming, `AriaValue`'s typed-optimistic/guarded-defensive split mirrors `bindAttribute`/`bindStyle`'s map forms, nullish-target degradation mirrors `bindState()`, debug attribution is Element-only with `bindState()`'s rationale. The stale-attribute removal satisfies REQUIREMENTS.md's "preserve existing accessibility attributes unless explicitly overridden" constraint — the explicit `bindAria()` opt-in per property *is* the override declaration, and the consumer attribute channel stays authoritative afterwards. The sharpened override-survival Playwright check (update fired after the consumer re-set the attribute) is exactly the right test for a one-shot contract. Noted, not actionable: two `bindAria()` calls on the same target+property would each remove once — an author-side no-mixing violation the ADR already rules out; no runtime guard warranted beyond `bindProperty`'s precedent.

- [x] LT-008: Register `ElementInternals` in the element-internals-declaration registry — **v2.6 scope** — reviewed ✓
  **Skill:** le-truc-dev
  **Changed:** `src/component.ts` (`elementInternalsRegistry()` module-private resolver — `globalThis._elementInternals ??= new WeakMap()`, idempotent against a pre-existing registry — plus the constructor's unconditional `elementInternalsRegistry().set(this, internals)` on the line after the `internalsHosts` set; no `DEV_MODE` gate, no cleanup logic); `src/tests/component.test.ts` (2 tests: registration + reverse lookup populated, registry shared/idempotent across instances); the axe gate itself lives in LT-007's `examples/test/aria/` spec.
  **How:** Faithful to `test/poc/poc-truc-registration.ts`, landed on the real `Truc` constructor. One difference from the PoC: the trap elements are **real `defineComponent` components**, not hand-registering raw elements — so the spec proves the registry visibility the library now grants every Le Truc component automatically.
  **Check:** The LT-005 trap, ported: `<test-aria-trap>` (internals-set `role='button'` in the factory body, invalid `aria-checked` attribute) is flagged `aria-allowed-attr` by an in-page `axe.run()` on both Chromium and WebKit, and `<test-aria-trap-ok>` (no invalid attribute) stays clean — axe-core injected via `page.addScriptTag`, no bundle cost. Unit tests confirm one registry entry per constructed instance and `??=` adoption semantics. Bundle tiers 5/5 (core-form 9208 B, core-checkbox 9239 B — the two constructor lines cost ~nothing).
  **Review:** Approved (2026-08-31). Faithful to the PoC and to ADR 0026 §3: unconditional registration (audits run against production builds — correctly no `DEV_MODE` gate), `??=` idempotency pinned by test, no public accessor (the protocol rejects one), stopgap cleanly removable. Registration lives inside the existing `attachInternals()` try block, so the `attachInternals()`-failed path registers nothing — the right shape. The axe gate living in LT-007's spec (real `defineComponent` traps, so the constructor's registration is what axe sees) is the stronger proof over the PoC's hand-registered raw elements. Verified alongside LT-007: 451 unit, tsc clean, Playwright green on Chromium and WebKit.

- [x] LT-013: Prepare meaningful VoiceOver/AT test fixtures for LT-009 and LT-010 — done ✓
  **Skill:** le-truc-dev
  **Changed:** `test/poc/pages/hue-slider.html` (visible `16rem × 2rem` box with a hue-gradient background — a zero-size element is a coin flip for AX-tree/AT exposure); `test/poc/poc-hue-slider.ts` (ArrowLeft/ArrowRight → `setHue(hue ± 5)`, focus-scoped keyboard driver alongside the existing pointer path); `test/poc/poc-stale-expanded.ts` (`tabIndex = 0`, click handler, and Enter/Space keydown handling toggling `expand()`/`collapse()` in place — `role="button"` carries no native activation); `test/poc/README.md` (new **LT-013** section: manual VO/NVDA procedure — caption panel, QuickNav-off note, computed-tree-inspector pairing per engine — plus two result tables ready to fill in: LT-009's PoC-page properties, and a pre-migration baseline for `form-combobox`/`form-colorgraph` in their current attribute-channel form).
  **How:** Two verification tiers, not one — VO/NVDA speech alone can't distinguish "AT doesn't voice this property here" from "the engine dropped it," so the recorded procedure pairs each manual pass with the engine's own computed-tree inspector (Safari Web Inspector's Accessibility node sidebar, or Accessibility Inspector via Xcode, for WebKit; Firefox Accessibility Inspector for Gecko) as the ground-truth tier LT-001's CDP harness can't reach outside Chromium. `/combobox` and `poc-late-ref` needed no fixture changes — the combobox's `<input>` already carries real keyboard focus and an ArrowUp/ArrowDown/Escape handler, and `poc-late-ref` only proves static element-reference wiring at construction time.
  **Check:** `tsc --noEmit` clean; poc-chromium 20/20 pass; poc-firefox/poc-webkit 14 pass + 26 expected CDP-tier skips (pre-existing standing limitation, unchanged); 451 unit tests pass; `bind-aria.test.ts` 12/12 pass; biome clean on `test/poc`. The manual AT passes themselves are **not run** — they need an interactive human session and are explicitly out of an agent's reach; this task's scope was preparing fixtures a human can act on, not producing results. `test/poc/README.md`'s two tables are structurally in place with `—` placeholders, no fabricated entries. **Gates LT-009** (LT-009's manual verification is only meaningful now that these fixtures are fixed) — LT-009 stays open pending the actual VO/NVDA session.

- [x] LT-009: Cross-engine AT verification for internals-set semantics — done ✓, WebKit verified; Firefox/NVDA unverified by necessity
  **Skill:** le-truc-dev
  **Changed:** `test/poc/README.md` (new LT-009 results table, folded into the LT-013 section — every internals-set property on `/hue-slider` and `/combobox` checked against WebKit's computed-tree ground truth (Safari Web Inspector's Accessibility panel) and against VoiceOver's actual announcement).
  **How:** Manually verified 2026-08-31 by Esther Brunner — Safari 27, VoiceOver, macOS 27 beta ("Golden Gate") — using the LT-013 fixtures (hue slider now has a visible box and an ArrowLeft/ArrowRight keyboard driver; the stale-expanded toggle is now focusable and clickable). Two-tier method per property: the Web Inspector Accessibility panel confirms WebKit's AX tree carries the internals-set default (role, live valuenow/valuetext, the combobox's activedescendant/describedby/labelledby/errormessage relationships, the `:not(:defined)` element reference); VoiceOver confirms it is actually announced. Firefox + NVDA could not be tested — no Windows/Linux machine with NVDA was available.
  **Check:** Every internals-set property checked reached WebKit's computed tree and was announced correctly by VoiceOver, including the no-mixing mitigation case (`#toggle-mitigated` announces identically to an attribute-set equivalent, confirming `bindAria()`'s stale-attribute removal is AT-visible, not just CDP-visible). **No property failed WebKit** — nothing is disqualified from LT-010 migration on WebKit grounds. **Firefox/NVDA gap accepted as a standing limitation**, per the tester's explicit judgment call: WebKit and Chromium independently agreeing on every property is treated as sufficient basis to assume Gecko/NVDA parity for LT-010's purposes; a future Firefox/NVDA divergence is treated as a Firefox- or NVDA-side bug report, not evidence against ADR 0026, but this assumption is unverified and recorded as such in `test/poc/README.md` — re-test before relying on it for a Firefox-specific claim. **Gates LT-010** — cleared for WebKit; Firefox stays an open, explicitly-accepted risk rather than a blocker.

- [x] LT-010: Migrate examples where reflection is strictly better — **v2.6 scope** — reviewed ✓
  **Skill:** le-truc-dev
  **Changed:** `examples/form/combobox/form-combobox.ts` (`ariaDescribedByElements = [descriptionEl]` replaces the `descriptionId`-then-`setAttribute('aria-describedby', …)` ID plumbing, imperative one-time wiring per ADR 0026 §2; the combined `watch(isExpanded, expanded => { listbox.hidden = !expanded; textbox.ariaExpanded = String(expanded) })` split into `watch(isExpanded, bindVisible(listbox))` and `watch(isExpanded, bindAria(textbox, 'ariaExpanded'))` — the former just switches manual property assignment to the existing `bindVisible()` helper already used elsewhere in this file (`clearBtn`), the latter replaces the manual `String(expanded)` coercion); `examples/form/colorgraph/form-colorgraph.ts` (the hue slider's reactive `aria-valuenow`/`aria-valuetext` — previously two `setAttribute()` calls inside `watch(color, …)` — replaced by `watch(() => ({ ariaValueNow, ariaValueText }), bindAria(sliderEl, ['ariaValueNow', 'ariaValueText']))`, the map form); `examples/form/combobox/form-combobox.spec.ts` (4 assertions re-pointed from `toHaveAttribute('aria-describedby', 'timezone-description')` to an in-page `ariaDescribedByElements.includes(...)` check — see How).
  **How:** Both migrations stay on the **attribute channel**, not the internals channel — `textbox` and `sliderEl` are native `Element`s (no `ElementInternals` involved in either component), so `bindAria()`'s only effect here is replacing hand-rolled boolean/number coercion with the helper's, not moving anything to a channel that Playwright can't see. One real finding while implementing: assigning `ariaDescribedByElements` on an `Element` does **not** mirror the target's `id` into the content attribute — it mirrors as present-but-empty (`aria-describedby=""`), confirmed against LT-003 finding 5 (`test/poc/README.md`), which this task's authors had read as being about the *clearing* case only. The relationship itself is real (confirmed via the IDL property and, for the identical PoC pattern, via LT-009's WebKit/VoiceOver pass) but is genuinely invisible to `toHaveAttribute`-style string assertions — an ADR 0026 §5 testability loss that happens on the *attribute* channel, not just the internals one. Re-pointed per the task's own instruction not to silently delete a test: the four affected assertions now read `el.ariaDescribedByElements?.includes(description)` via `page.evaluate()` instead of the attribute string. `form-listbox`'s `[aria-selected="true"]` and the static `role="slider"`/`aria-valuemin`/`aria-valuemax` (colorgraph HTML) were left untouched, per the no-migration list. No component writes both channels for the same property on the same element (verified per-file).
  **Check:** `tsc --noEmit` clean; `bun test src/tests` 451/451; biome clean on `src`/`examples/form`; bundle regression 5/5 unaffected (examples aren't in the measured bundles); full `examples/` Playwright suite 398/398 on Chromium, and the two changed specs (35 tests) additionally green on WebKit — confirms the re-pointed `ariaDescribedByElements` assertions and the `bindAria()`-driven `aria-expanded`/`aria-valuenow`/`aria-valuetext` writes hold cross-engine, consistent with LT-009's WebKit/VoiceOver finding for the same pattern on the PoC pages.
  **Review:** Approved (2026-08-31). Both migrations are exactly the ADR 0026 §5 case ("reactive host state that nothing selects on moves to `bindAria()` — on a native element, the attribute mirror keeps the value CSS-visible") — neither touches `ElementInternals`, so there's no channel change and no loss of the attribute-based reference implementation REQUIREMENTS.md §4 asks examples to demonstrate. `bindAria()` usage matches its ADR examples closely (single form for `ariaExpanded`, map form for the two hue-slider properties), and the split of the combobox's combined `watch(isExpanded, expanded => {...})` into two single-purpose `watch()` calls is consistent with how other `bind*` helpers are composed elsewhere in the examples. The `ariaDescribedByElements`-empties-the-attribute finding is not new — ADR 0026's own Consequences already names it ("assigning any `aria*Elements` property empties the mirrored content attribute … code testing 'was this set via attributes' needs empty-string semantics") — so the re-pointed IDL-property assertions are the ADR's own guidance applied correctly, not a workaround. Non-blocking note for a future pass, not worth its own task: the four re-pointed `page.evaluate()` blocks in `form-combobox.spec.ts` are identical — a small `describesDescription(page)` test helper would remove the duplication, but this is test-only cleanup, not an API or architecture concern.

- [x] LT-014: Tear down `test/poc/` and fold its ARIA-reflection coverage into the regular Playwright suite — done ✓
  **Skill:** docs-server-dev
  **Changed:** `examples/test/fixtures/aria.ts` (new — `computedAriaTree()`/`findAriaNode()`/`ariaSnapshotOf()`/`runAxe()`/`engineOf()`, ported verbatim from `test/poc/fixtures/aria.ts`); `examples/test/aria/test-aria.ts` (new `testAriaLateRef` component — `internals.role = 'button'` + a constructor-time `ariaDescribedByElements` wire to a `:not(:defined)` target, mirrors `poc-late-ref.ts`); `examples/test/aria/test-aria.html` (`#default` gained `role="button"` so its `aria-expanded` reflection has a role that exposes an `expanded` AX property; new `<test-aria-late-target>`/`<test-aria-late-ref>` markup); `examples/test/aria/test-aria.spec.ts` (3 new Chromium-only CDP-tier tests: internals-reflected `ariaExpanded` reaches the AX tree and updates live, internals-set `role` reaches the AX tree, and a new `describe` block for the `:not(:defined)` element-reference case — IDL readback plus the AX-tree `description` field); `playwright.config.ts` (removed the `poc-chromium`/`poc-firefox`/`poc-webkit` projects and the second `webServer` entry); `adr/archive/0026-poc-findings.md` (moved from `test/poc/README.md`, retitled, a closing LT-014 note added pointing at the live coverage); `adr/0026-aria-reflection-via-elementinternals-and-bindaria.md` (Related: new Evidence link to the findings file); `CHANGELOG.md` (fixed the now-dead `test/poc/README.md` link); `test/poc/` deleted in full (`serve.ts`, `fixtures/`, `pages/`, all `poc-*.ts`, all `*.spec.ts`, `bind-aria.test.ts`).
  **How:** Most of the PoC's pinned coverage already had a live equivalent by LT-007/LT-008/LT-010 — override/reassert, the stale-attribute mitigation, and the axe registry-gate trap were already in `test-aria.spec.ts` (that spec exercises the real `bindAria()`, not the PoC's prototype). The genuine gaps were the CDP computed-tree tier itself (`fixtures/aria.ts` had never been ported, so no example spec could read Chromium's actual AX tree) and the `:not(:defined)` element-reference edge case (no example wires an ARIA relationship to an undefined custom element). Closed both: ported the fixture unchanged (only doc comments repointed from `test/poc/README.md` to `adr/archive/0026-poc-findings.md`), added the three CDP tests to `test-aria.spec.ts`, and added `testAriaLateRef`/`<test-aria-late-target>` reproducing the `poc-late-ref.ts` probe against the shipped registry. Kept nothing PoC-only as a standalone regression guard beyond that — `poc-basic.ts`'s three-mode (internals/attribute/both) comparison and `poc-hue-slider.ts`'s throttle-dedup check were LT-001/LT-002 platform proofs already fully absorbed into ADR 0026's text and into `test-aria.spec.ts`'s override/reassert tests; keeping raw duplicates would test the platform, not the library. The `test/poc/README.md` findings log is preserved verbatim as `adr/archive/0026-poc-findings.md` (historical record, not living docs) with a closing note that its named `poc-*`/`test/poc/*` paths no longer exist, and linked from ADR 0026's Related section.
  **Check:** `tsc --noEmit` clean; `bun test src/tests` 451/451; biome clean on `examples`/`adr`; `examples/test/aria/test-aria.spec.ts` 23/23 pass on Chromium, 20 pass + 3 expected Chromium-only skips on WebKit (the CDP tier); full `examples/` Playwright suite 794/794 pass + 10 expected skips on Chromium and WebKit combined, `test/poc` no longer referenced by `playwright.config.ts` or any script. No net loss of assertions: every LT-001–LT-013 finding that described still-relevant platform/library behavior now has a live counterpart in `examples/test/aria/`; the PoC's one-time learnings (the `ariaValuenow` casing typo, the axe rule-selector investigation, the `all()`-untracked-read trap) stay text-only in `adr/archive/0026-poc-findings.md`, since there is nothing for a Playwright assertion to pin about a typo that was fixed before it ever shipped.

- [x] LT-011: Rewrite the ARIA docs advisory around the two-channel policy — **v2.6 scope** — done ✓
  **Skill:** tech-writer
  **Changed:** `docs-src/pages/components.md` (new **ARIA Reflection** section between "Synchronize State with Effects" and "Extensions": `bindAria()` single + map form with a realistic disclosure example, coercion table, the stale-attribute rule as a `.note` callout ("The server-rendered attribute is the initial value" — removal is free helper behavior, post-connect consumer overrides still win, never write both channels), a "Choose the Channel" policy table condensed from ADR 0026 §1, the axe-core reach paragraph carrying §3's qualification precisely (only `aria-allowed-attr`/`aria-prohibited-attr` act on an internals-only role; nesting rules inspect only `[role]` attributes → structural roles stay on the attribute channel), and the `ariaOwnsElements` withholding); surgical consistency edits: map-form helper list and clear-semantics sentence now include `bindAria()`, the `bindProperty()` vs `bindAttribute()` tip re-points ARIA reflection properties to `bindAria()` (it previously recommended `bindProperty` for `ariaLabel`/`ariaExpanded`/`ariaDisabled`), and the Form Association `internals` paragraph cross-links the new section; `docs-src/pages/api.md` (nav entries for `bindAria` and `type AriaValue`, alphabetical); `docs/` and `docs-src/api/` regenerated via `bun run build:docs`.
  **How:** The task premise was stale in one respect: the 2026-08 components-page rewrite (PR #115) had already *removed* ADR 0016's "keep explicit `aria-*` attributes" advisory, leaving the page silent on reflection — so the work was adding the positive two-channel guidance, not replacing advisory text. Verified no surviving docs/skills/README text tells authors to avoid ARIA reflection (the July 2026 blog post stands as a historical record, untouched). ADR 0026 itself had already been compacted (no PoC/LT references) before this pass.
  **CEM answer (recorded per the task's check):** the Le Truc CEM plugin (`@zeix/cem-plugin-le-truc`) needs **nothing** for `bindAria()`, as expected. The plugin surfaces class-level manifest features (attributes, members, css parts, the `static formAssociated` flag); `bindAria()` is factory-scope effect wiring with no class-level metadata — no static props, no `observedAttributes`, no reserved members — so `cem analyze` output is unaffected, exactly as for `bindState()`/`bindAttribute()` today.
  **Check:** `bun run build:docs` passes; the built `docs/components.html` renders the new section; `docs/api/functions/bindAria.html` and `docs/api/type-aliases/AriaValue.html` generated, matching the new nav links; Markdoc tags balanced (8 sections, 14 callouts). Docs-only change — no unit/browser surface touched.
