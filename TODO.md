# TODO

- [x] LT-001: Inlined TSRX compiler — emitter core (ADR 0024 milestone 1) — reviewed ✓
  **Skill:** le-truc-dev
  **Summary:** Built the in-repo compiler (`server/tsrx/`) on pinned `@tsrx/core` 0.1.60 — parses `.tsrx`, emits server HTML render functions, extracts verbatim tag-scoped CSS, maintains the component registry; wired into `server/build.ts`. Golden-tested against the hand-written `.html`/`.css` artifacts.

- [x] LT-002: Inlined TSRX compiler — client codegen, sanctioned subset (ADR 0024 milestone 2) — reviewed ✓
  **Skill:** le-truc-dev
  **Summary:** Generates `defineComponent()` factories from the same sources (bindings, events, refs, server-data `@for` + `each()`, hoisted-const rebinding, harvest rules). Golden-tested against the hand-written trio; typechecks via emit-then-check.

- [x] LT-005: Isomorphic format for form components — extensions, attribute-driven props, `internals` (ADR 0024 acceptance gate) — reviewed ✓
  **Skill:** le-truc-dev
  **Summary:** Added `export const config` for extension activation (`formAssociated`/`observedAttributes`), Parser-exposed attribute-driven `expose()` props, `internals`/`defineMethod()` support — unblocking form-component authoring in `.tsrx` (ADR 0024 sub-design 8).

- [x] LT-006: Custom Element Manifest generation for `.tsrx` components (ADR 0024 acceptance gate) — reviewed ✓
  **Skill:** le-truc-dev
  **Summary:** Generated clients now carry the component JSDoc verbatim so `@custom-elements-manifest/analyzer` + `@zeix/cem-plugin-le-truc` extract identically from compiled output; corpus compiles before `cem analyze`. Equivalence golden-tested (ADR 0024 sub-design 9).

- [x] LT-003: Reactive lists — `@for` over a `List` → template extraction + `reconcile()` (ADR 0024 milestone 3) — reviewed ✓
  **Skill:** le-truc-dev
  **Summary:** Server renders keyed items in place plus an extracted `<template>`; client lowers to `reconcile()` with a generated `bindItem`. Un-gated `module-list.tsrx`.

- [x] LT-004: Type-flow wiring — Volar projection + editor tooling (ADR 0024 milestone 4) — reviewed ✓ (approach reconsidered → LT-011)
  **Skill:** le-truc-dev
  **Summary:** Delivered `globals.d.ts`'s ambient contract, child-module side-effect imports, and emit-then-check in CI. The Volar-projection half proved infeasible (`createVolarMappingsResult` is transform-coupled to Ripple's own esrap output) and was reshaped into LT-011 (CLI-first span tables).

- [x] LT-008: Form-corpus migration enablers — arg→DOM harvest substitution, `@if`, setup side-effects — reviewed ✓
  **Skill:** le-truc-dev
  **Summary:** Added arg→DOM-site substitution harvest, `@if` conditional templates (union addressing), client-only setup side-effect statements, a `formAssociatedCheckbox` fixture, and more parser ambients — unblocking `form-textbox`/`form-checkbox` migration.

- [x] LT-009: `verify:cem` duplicate-tag guard — done ✓
  **Summary:** `scripts/verify-cem.ts` now fails with an exclude hint on duplicate `tagName` across declarations.

- [x] LT-010: Template-literal-safe reindentation in codegen — done ✓
  **Summary:** `server/tsrx/indent.ts` correctly reindents generated code around template literals, nested interpolation, and comments.

- [x] LT-013: tsrx.dev feature parity — `@switch`, `@try` error boundaries, `html={expr}`, parse-error hints — reviewed ✓
  **Skill:** le-truc-dev
  **Summary:** Added `@switch`, sync `@try`/`@catch` error boundaries, server-side sanitized `html={expr}` dynamic rendering, and parse-error hints for unsupported newer-grammar constructs — every tsrx.dev feature now handled deliberately (supported or gated).

- [x] LT-011: Type-flow diagnostics — CLI-first span-table mapping (ADR 0024 milestone 4, reconsidered) — done ✓
  **Skill:** le-truc-dev
  **Summary:** Compiler records a span table mapping generated code back to `.tsrx` source; new `check:tsrx` CLI runs `tsc` and remaps diagnostics to authored line:col. Editor-level (LSP) diagnostics deferred to LT-014.

- [x] LT-015: Component composition — server-side PascalCase invocation (ADR 0024 sub-design 10) — reviewed ✓
  **Skill:** le-truc-dev
  **Summary:** A capitalized JSX tag bound to a `.tsrx` import composes that component server-side (splices its `render<Name>()` output); top-level attributes forward as typed server args.

- [x] LT-016: Explicit `pass={{ ... }}` special attribute — unify client-prop dispatch (ADR 0024 sub-design 10, amends sub-design 4) — reviewed ✓
  **Skill:** le-truc-dev
  **Summary:** Replaced shape-inferred client-prop dispatch with one explicit `pass={{ }}` attribute for both raw and composed custom-element targets. Composed-element `pass` requires an explicit `ref` (known limitation: can't address a duplicated composed child).

- [x] LT-017: Two-way `pass()` codegen — wire `{ get, set }` descriptor entries (ADR 0024 sub-design 4/10) — reviewed ✓
  **Summary:** `pass={{ prop: { get, set } }}` now emits real two-way bindings; getter-only shorthand unchanged.

- [x] LT-018: Children — `{children}` insertion point (ADR 0024 sub-design 10) — done ✓
  **Skill:** le-truc-dev
  **Summary:** A composed element's children substitute into the child's bare `{children}` expression, rendered unescaped; a missing required `children` argument is a real type error.

- [x] LT-019: Extend `check:tsrx` to type-check generated server modules (ADR 0024 sub-design 10, extends LT-011) — done ✓
  **Skill:** le-truc-dev
  **Summary:** Generated server modules are now type-checked too, so a missing/mistyped composed server arg or child is a real compile error, not silent.

- [x] LT-020: Migrate `module-list`/`form-textbox` example to real composition (ADR 0024 sub-design 10, proof) — done ✓
  **Skill:** le-truc-dev
  **Summary:** `module-list.tsrx` composes `<FormTextbox>` instead of hand-duplicating its markup. Fixed two pre-existing compiler bugs only surfaced by real composition (pass-1 registry was empty; ref-only composed elements weren't addressed).

- [x] LT-012: Async boundaries via `isPending` routing (ADR 0024 sub-design 13) — done ✓ (residue queued as LT-025)
  **Skill:** le-truc-dev
  **Summary:** `@try`/`@pending`/`@catch` routes on `isPending(signal)`; all three arms server-render and are `hidden`-toggled client-side via one `watch()` call — mirroring `module-lazyload.ts`'s hand-written shape.

- [x] LT-021: Split `compiler.ts` into locality-focused sibling modules (architecture deepening) — reviewed ✓
  **Skill:** le-truc-dev
  **Summary:** Extracted `ast-utils.ts`, `infer-type.ts`, `classify-attributes.ts`, `lower-template.ts`, `config.ts` from a 2363-line `compiler.ts` (now 811 lines). `analyze.ts`'s own monolithic-function problem deferred to LT-022.

- [x] LT-023: Optional `@if` branches (no `@else`) + hoisted event-handler consts (ADR 0024 sub-design 11) — done ✓
  **Skill:** le-truc-dev
  **Summary:** A single-branch `@if` now addresses via a non-throwing `first()` (`'maybe'` cardinality); hoisted setup consts usable as shared event handlers across branches. Fixed a latent branch-root selector-uniqueness bug along the way.

- [x] LT-024: Widen arg→DOM-site substitution to any descendant attribute + multiple args per initializer (ADR 0024 sub-design 12) — done ✓
  **Skill:** le-truc-dev
  **Summary:** A signal's client harvest can trace multiple arg identifiers to any descendant's rendered attribute, not just the root's one. `form-textbox.tsrx`'s reactive `description` reaches full hand-written parity.

- [x] LT-026: Migrate `examples/basic/*` (button, hello, number, pluralize, gauge) to TSRX — done ✓
  **Skill:** le-truc-dev
  **Summary:** Converted all five `examples/basic/*` components, exercising patterns the original 5-component corpus never hit. Found and fixed two real compiler defects (Parser-exposed lazy-child rendering; `freeIdentifiers` mishandling TS type positions, split to LT-027). `basic-gauge` drops its live CSS-custom-property updates pending LT-028/LT-029.

- [x] LT-027: `freeIdentifiers` doesn't recognize TS type-annotation positions, causing spurious TSRX004 (found migrating `basic-gauge.tsrx`) — done ✓
  **Summary:** `freeIdentifiers`'s generic AST walk no longer treats type annotations/casts as value reads, fixing false-positive `badFreeNames` rejections.

---

- [x] LT-030: Merge `feat/bindings-multiple-overloads` — map-form `bind*()` overloads — done ✓
  **Skill:** le-truc-dev
  **Summary:** Merged into `docs/server-components` (4 conflicts resolved). Delivered `bindStyle`/`bindAttribute`/`bindClass`/`bindProperty`/`bindState` array-target overloads matching LT-029's proposed shape, full test coverage.

- [x] LT-029: Add map-form overloads to `bindStyle()`/`bindAttribute()`/`bindClass()`/`bindProperty()` (`src/bindings.ts`) — done ✓ (delivered by LT-030's merge)
  **Skill:** le-truc-dev
  **Summary:** Verified the merged implementation against the proposed signatures and semantics — exact match. No net-new work needed.

- [x] LT-028: No sanctioned `.tsrx` lowering for reactive host-level style/CSS-custom-property updates — reviewed ✓
  **Skill:** le-truc-dev
  **Summary:** Added a `style-map` `AttributeIR` kind for `style={() => ({ … })}`, bypassing the custom-element gate on both descendant elements and the component root (lowers to `watch(thunk, bindStyle(host, keys))`). Server-side renders the initial value via a new `styleAttr()` runtime helper. `basic-gauge.tsrx` migrated off its static-string workaround.
  **Review:** Approved. Left two consistency gaps against `class-map`, split out as LT-031/LT-032.

- [x] LT-031: Migrate `class-map`'s lowering to `bindClass`'s array-form overload — one `watch()` call instead of N — done ✓
  **Skill:** le-truc-dev
  **Summary:** Added a `watch-class` `TopEffectPlan` kind; `class-map` now pushes one of these instead of looping per key, lowering to one `watch(thunk, bindClass(el, [keys]))` call. Pure codegen consolidation, behavior unchanged.

- [x] LT-032: Extend the component-root reactive exemption to `class-map` (host-level class-map) — done ✓
  **Skill:** le-truc-dev
  **Summary:** Extended the root's `style-map` exemption to `class-map` (previously blocked outright at the root, not just under-optimized); added matching root-level class rendering server-side.

- [ ] LT-014: Type-flow diagnostics — Volar language-core plugin over the LT-011 span table (ADR 0024 milestone 4, stage 2)
  **Skill:** le-truc-dev
  **Context:** Scheduled after the `examples/` wholesale migration to `.tsrx` completes. CLI-first (LT-011) covers CI and AI-agent workflows; human authors still need in-editor squiggles. A `@volar/language-core` plugin projects the generated client module as the virtual file, reusing LT-011's span table — no new mapping mechanism. Trigger: last example in `examples/` ported to `.tsrx`.

- [x] LT-022: Restructure `analyze.ts`'s monolithic `analyzeClient` into explicit passes over a shared context (architecture deepening, deferred from LT-021) — done ✓
  **Skill:** le-truc-dev
  **Summary:** `analyze.ts` (2,512 lines) deleted; `server/tsrx/analysis/` now holds `plan.ts` (plan vocabulary + orchestration), `selectors.ts`, `naming.ts`, `harvest.ts`, `loops.ts`, `effects.ts` — each pass over an explicit shared `AnalysisContext`, bodies moved verbatim to avoid transcription risk.

- [x] LT-025: Async-boundary residue — reactive `html={() => …}`, optional addressing inside plain `@try` bodies, `createMemo` as a recognized signal constructor (split off LT-012) — reviewed ✓
  **Skill:** le-truc-dev
  **Summary:** `html={() => …}` lowers to `watch(thunk, dangerouslyBindInnerHTML(el))` (ADR 0010 sanctioned sink); a plain `@try`'s body and `@catch` arm are each independently DOM-existence-guarded like a single-branch `@if`; `createMemo` added as a recognized signal constructor with a sync server shim, closing a latent host/internals-in-signal-compute crash shared with `deriveCell`/`deriveStore`.
  **Review:** Approved. Flagged that `.tsrx` authors had no syntax to opt IN to a client-side HTML sanitizer — filed as LT-050.

- [x] LT-033: Migrate `examples/card/*` to TSRX, surface edge-cases/cliffs — done ✓
  **Skill:** le-truc-dev
  **Summary:** Migrated `card-collapsible`, `card-callout`, `card-blogpost` cleanly (the latter two are valid server-only components via `{children}` insertion). Three genuine compiler cliffs surfaced and left hand-written: `card-mediaqueries` needs context protocol support, `card-colorscale` needs plain-module imports, `card-blogmeta` reformats arbitrary light DOM (doesn't fit the template-ownership model). Findings recorded in `NOTES.md` for triage.

- [x] LT-034: Import-placement inference for plain (non-`.tsrx`) imports (ADR 0024 sub-design 14) — done ✓
  **Skill:** le-truc-dev
  **Summary:** New `plain-imports.ts` classifies every plain import's bindings by free-identifier usage into server-only/client-only/both, rewrites relative specifiers for the flat output directory, and warns on dead imports (TSRX014). Surfaced and fixed three pre-existing bugs blocking `card-colorscale`'s re-migration: unguarded server-known lazy-child fallback rendering, plain setup consts never actually reaching the client module despite being documented as verbatim-in-both, and a Parser-factory `any`-stub shadowing a resolvable import.

- [x] LT-035: Context protocol support — `requestContext()`/`provideContexts()` in `.tsrx` (ADR 0024 sub-design 15) — done ✓
  **Skill:** le-truc-dev
  **Summary:** `requestContext(Context, fallback)` is a recognized signal-constructor-like call (server-known fallback required, renders `createCell(fallback)` server-side, exempt from harvest-site requirements); `provideContexts([...])` joins the client-only ambient allowlist. `card-mediaqueries.tsrx` re-migrated as the acceptance case, reshaped from a light-DOM enhancer into a props-driven component (real behavior change, flagged for the stale demo/e2e spec).

- [x] LT-036: `recordSites` doesn't credit `style-map`/`class-map` usage as a harvest site (spurious TSRX004 on signals used only there) — done ✓
  **Skill:** le-truc-dev
  **Summary:** Signals read inside a `style-map`/`class-map` object or a computed reactive thunk are now credited into a `thunkRendered` set, allowing verbatim initializer reuse instead of a false-positive TSRX004 — root and descendant map forms both covered.

- [x] LT-037: `plain-imports.ts` never traces `'server'`-kind attribute expressions, so a plain import used only there is mis-diagnosed as unused and dropped from the server module (LT-034 review) — done ✓
  **Skill:** le-truc-dev
  **Summary:** Added a `'server'`-kind attribute branch to the server-expression walk so such imports land in the always-server-rendered bucket instead of being dropped.

- [x] LT-038: `watch()` overload resolution fails for a lazy child whose expression is an arbitrary call/member expression, not thunk-wrapped automatically (found migrating `card-colorscale.tsrx`, LT-034) — done ✓
  **Skill:** le-truc-dev
  **Summary:** Any lazy-child expression that isn't a bare identifier, string literal, or already an arrow thunk now auto-wraps in one before emission — the compiler now does automatically what the manual `&{() => …}` workaround proved works.

- [x] LT-039: Extract the shared IR vocabulary into `ir.ts` (compiler regrouping M1) — done ✓
  **Skill:** le-truc-dev
  **Summary:** New leaf `ir.ts` owns the whole IR vocabulary as type-only exports; all sibling modules re-pointed from `./compiler`, eliminating the type-level hub-and-spoke.

- [x] LT-040: Extract `core.ts` — single `@tsrx/core` value-import adapter; kill the compiler⇄lower-template value cycle (compiler regrouping M2) — done ✓
  **Skill:** le-truc-dev
  **Summary:** New leaf `core.ts` re-exports the five pinned `@tsrx/core` values; `lower-template.ts`/`emit-server.ts` call them directly instead of through `compiler.ts` wrappers, removing the last runtime value cycle. Pin-isolation preserved: an upgrade now touches `core.ts` + `core-shim.d.ts` only.

- [x] LT-041: De-duplicate shared predicates/helpers; bring `CONTEXT_HELPERS` under the vocabulary parity test (compiler regrouping M7) — done ✓
  **Skill:** le-truc-dev
  **Summary:** Six duplicate helper homes collapsed into `ast-utils.ts`/`spans.ts` (`nodeType`, `hostPropOf`, `objectKeys`, `sanitizeVarName`, `reindent`, `FACTORY_CONTEXT_MEMBERS`). The last one gained a type-level parity test against the real `FactoryContext<ComponentProps>`, so a library rename now fails CI instead of drifting silently.

- [x] LT-042: Consolidate the bespoke `TemplateNode` walks into one `walk.ts` visitor (compiler regrouping M3) — done ✓
  **Skill:** le-truc-dev
  **Summary:** New `walk.ts` (`childNodes`, `walkTemplate`, `collectAttrs`) absorbs six genuinely-uniform walks (plain-imports' four generators, `collectComposeElements`, `collectRefs`). Walks whose recursion IS the semantics (selector exclusivity counting, early-exit searches, depth-guarded/stateful passes) deliberately left bespoke.

- [x] LT-043: Single home for the server-known evaluability rule — `evaluability.ts` (compiler regrouping M4) — done ✓
  **Skill:** le-truc-dev
  **Summary:** New leaf `evaluability.ts` exports `dependenciesOf`/`isServerEvaluable`, replacing three byte-identical copies; all server-render gate sites now call the shared implementation directly.

- [x] LT-044: Merge import handling into one module; remove `node:path` (compiler regrouping M6; browser-purity gate for ADR 0025) — done ✓
  **Skill:** le-truc-dev
  **Summary:** New `imports.ts` owns all source-import collection and placement (compose + plain), replacing Node's `posix` path APIs with pure-string equivalents — the compiler is now browser-pure (ADR 0025 sub-design 6).

- [x] LT-045: Browser-bundle build + smoke test pinning compiler purity (ADR 0025 enabler) — done ✓
  **Skill:** le-truc-dev
  **Summary:** New `scripts/build-tsrx-browser.ts` bundles the compiler for `target: 'browser'` with `external: ['node:*']` so a reintroduced Node import survives as a literal unresolvable specifier rather than being silently polyfilled; a new test confirms the bundle is byte-identical in output to the direct Node import. Seeds the playground's compile worker (ADR 0025).

- [x] LT-046: (Optional) Extend `walk.ts` coverage to the remaining bespoke `TemplateNode` walks — or document why not — done ✓
  **Skill:** le-truc-dev
  **Summary:** Reviewed all six holdouts; none generalize without losing behavior (exclusivity arithmetic, early-exit searches, a depth-conditional predicate, stateful ordering). Closed as won't-do, reasoning recorded in `LE_TRUC_COMPILER.md` §7.

- [x] LT-047: Direct unit test for `evaluability.ts` (`dependenciesOf`/`isServerEvaluable`) — done ✓
  **Skill:** le-truc-dev
  **Summary:** New `evaluability.test.ts` (5 tests) pins both functions directly against compiled fixtures rather than only transitively.

- [x] LT-048: Standalone unit tests for `analysis/loops.ts` (`runLoops`) and `analysis/naming.ts` — done ✓
  **Skill:** le-truc-dev
  **Summary:** New `loops-naming.test.ts` (10 tests) at the same hand-built-context granularity as `analysis.test.ts`: server-data and reactive-`List` `@for` planning, `uniqueName` collision suffixing, `addQuery` dedup/registration.

- [x] LT-049: Delete unused `diagnostic.withLine` (`diagnostics.ts`) — done ✓
  **Skill:** le-truc-dev
  **Summary:** Confirmed zero call sites repo-wide beyond its own definition; deleted.

- [x] LT-050: Client-side sanitizer-configuration path for TSRX reactive `html={}` (LT-025 review finding) — reviewed ✓
  **Skill:** le-truc-dev
  **Summary:** New `configureHtmlSanitizer()` (mirrors the server-side runtime helper by name) sets a module-level default sanitizer, read fresh per `dangerouslyBindInnerHTML().ok()` call; an unconfigured call stays byte-identical to today's raw passthrough. No compiler changes needed — the generated bare `dangerouslyBindInnerHTML(el)` call picks up whatever default the host app configures.
  **Review:** Approved. Recorded as an ADR-0010 amendment (additive to the existing `sanitize` hook, not a new decision surface).

---

## TSRX conformance & migration ergonomics (CHECKLIST.md triage, 2026-08-26)

Triaged against the compiler source. Three checklist items were **verified as already satisfied** and produced no task: statements-before-output inside control-flow bodies (`lower-template.ts` `lowerBranchBody` + `client-stmt` already handle it, §1), selector-literal type inference (`ElementFromSelector<S>` in `src/helpers/dom.ts` already yields precise non-nullable types, §3), and equality-by-value (`List`/`Store` already compare by value, §6).

- [x] LT-051: Reactive-lift analysis — lift lexical reads, hard-error on opaque call boundaries (CHECKLIST §2) — reviewed ✓
  **Skill:** le-truc-dev
  **Changed:** new `server/tsrx/reactivity.ts` (`classifyChild`, 145 lines); `lower-template.ts` (`liftsToReactive` wrapper, plain `{expr}` children now classified); `diagnostics.ts` (TSRX017); new `server/tests/tsrx/reactivity.test.ts` (11 tests).
  **How:** `classifyChild` walks the child expression collecting *reads* (`sig.get()`, `sig.<member>`, `host.<prop>` — the object is consumed at the MemberExpression, so it never counts as an escape) and *escapes* (a signal or `host` identifier reached as a bare value). Escapes → TSRX017; else reads → lift; else static. An authored `{() => …}` thunk short-circuits to reactive and is never inspected, so the override stays legal everywhere. Nested callbacks are walked, with params shadowing outer signal names.
  **Check:** (1) `host` is treated as reactive in *any* member position, so `{host.setFormValue}` would lift — harmless in child position but arguably sloppy; worth a decision before LT-052 widens the surface. (2) Bare `host` passed to a call is currently an escape (TSRX017) alongside signals — stricter than the checklist's wording, which names only signals. (3) TSRX017's fix-it always suggests wrapping the whole expression; it does not try to narrow to the offending subexpression.
  **Corpus impact:** none — every plain `{expr}` child in `examples/` is a server arg (`label`, `children`, `unit`, …), so all goldens are byte-identical. Verified `check:tsrx` emits the same 4 pre-existing diagnostics before and after.
  **Context:** Prerequisite for LT-052 — do this first. The lift line is *lexically visible reactive read* vs. *read behind an opaque call boundary*, not the current *single read* vs. *compound expression*. `{host.validationMessage}` and `{length.get() === 0}` must both lift; `{formatRemaining(maxlength, length)}` must not lift silently — it needs an explicit thunk. When the analysis cannot prove a child expression is static, emit a new hard diagnostic (`TSRX017`) demanding an explicit `{() => …}` thunk rather than falling back to a static server emit. Rationale: a missed lift is invisible — the server folds it, the HTML is correct, the demo looks right, and it never updates; over-lift is loud, under-lift survives review. Explicit thunks stay legal everywhere as an override.
  **Review:** Approved. Re-derived the escape/read walk in `reactivity.ts` by hand against the doc-comment's own example table — it's correct. On the three flagged Check items: (1) `host.setFormValue`-style method reads only classify reactive in *child* position, where rendering a bare function as text is already nonsensical and never occurs in the corpus — not worth gating. (2) treating bare `host` as an escape alongside signals is the right call, not just consistent with it: `host` denotes no single value, so a bare `{host}` in a call is exactly as untraceable as a bare signal. (3) the fix-it's whole-expression wrap is a reasonable default; narrowing to the offending subexpression is a nice-to-have, not worth a follow-up task on its own. `bun test server/tests/tsrx` (249/249) and `check:tsrx` confirm the claimed zero corpus impact — same 4 pre-existing diagnostics before and after.

- [x] LT-052: Remove the `&{}` lazy-child sigil; plain `{}` carries reactivity (CHECKLIST §1) — reviewed ✓
  **Skill:** le-truc-dev
  **Changed:** `lower-template.ts` (sigil branch deleted → TSRX018; `markPositionallyReactive`; TSRX019 gate); `diagnostics.ts` (TSRX018/019); `ir.ts` + `compiler.ts` (`ExtractContext.exposedProps`); `analysis/effects.ts` (`managedPropRead` — TSRX010 now fires on `host.<prop>`); codemod across 15 example files and 13 test files; `client.golden` expectation + `form-textbox` snapshot.
  **How:** `&{x}` → `{x}`, and `&{'prop'}` → `{host.prop}`. Three sub-problems the task description didn't anticipate: (1) `@catch`'s error param and a reactive `@for`'s item are reactive *by position*, not by declaration, so the lift rule alone left them static and `validateListBody` saw zero slot-fill holes — marked in `lowerTry`/`lowerFor`, recursing through element children since `{item}` is routinely nested; (2) `{'validationMessage'}` is indistinguishable from literal text without the sigil, so string-literal prop naming is retired via TSRX019; (3) `watch('validationMessage', …)`'s string-key overload becomes `watch(() => host.validationMessage, …)`'s thunk overload.
  **Check:** (1) The managed-prop watch no longer proves extension ordering through `watch()`'s FormFactoryContext key overload — it now rests on `host.validationMessage` typechecking against a `FormAssociatedElement`-typed host. Equivalent in effect, but a different enforcement route; confirm that's acceptable. (2) TSRX019 includes the managed props unconditionally (`config` is parsed after template lowering), so `{'validationMessage'}` errors even without `formAssociated`.
  **Follow-up landed:** binding-position `&{`/`&[` now diagnosed as TSRX020 (`reportLazyPatterns` scans the whole module AST — the pinned parser does set `lazy` on the patterns, verified by probe). The dead string-literal branches in `emit-server.ts`'s `lazyValueExpression` and `harvest.ts`'s `lazyWatchSource` are removed, along with the latter's now-unused `component` parameter; TSRX004/TSRX010's messages no longer instruct authors to write the retired `&{…}`.
  **Context:** Depends on LT-051. `&{}` is not a TSRX construct at all: `@tsrx/core` 0.1.60 defines `&{…}`/`&[…]` as lazy *destructuring patterns* in binding position (`types/index.d.ts:347`, `LazyPattern`), and our "lazy child" is a string-adjacency hack in `lower-template.ts:535` — a `JSXText` whose raw value *ends in* `&` immediately before a `JSXExpressionContainer`. Side effect of that hack: `<span>Q&{a}</span>` silently swallows the `&` today. Delete the sigil-detection branch and the `lazy` flag's authoring surface (the IR `lazy` field may stay as an internal "is reactive" marker set by LT-051's analysis). Ship a codemod rewriting `&{x}` → `{x}` across the corpus (29 occurrences, 10 files) and make a stray `&` before an expression container a hard diagnostic with a fix-it. Lazy destructuring is not applicable to Le Truc at all — server composition needs eager snapshot evaluation — so also diagnose `&{`/`&[` in binding position in this host profile.
  **Review:** Approved. `markPositionallyReactive`'s recursive walk correctly mirrors `validateListBody`'s nesting assumption, and `grep`ing the corpus confirms zero remaining `&{` occurrences — the codemod is complete. On the flagged Check items: (1) resting managed-prop-watch enforcement on `host.validationMessage` typechecking against `FormAssociatedElement` is an equivalent, and arguably more direct, enforcement route than the old string-key overload — no concern. (2) `exposedProps` including `MANAGED_TEXT_PROPS` unconditionally (`compiler.ts:550-556`) is deliberate and documented inline; it means a component that isn't form-associated and happens to want the literal string `"validationMessage"` as text would get TSRX019 pointed at a prop that doesn't exist on its host — but that string is not something anyone authors as literal UI copy, so the trade-off is sound as shipped. No follow-up needed on either.

- [x] LT-053: Rename the `pass` special attribute to the namespaced `truc:pass` (CHECKLIST §1) — reviewed ✓
  **Skill:** le-truc-dev
  **Changed:** `ast-utils.ts` (`attrName` flattens `JSXNamespacedName`); `classify-attributes.ts` (accepts `truc:pass`, rejects bare `pass` as TSRX006); codemod of 6 sites across 4 example files and 3 test files; 2 new tests in `pass.test.ts`.
  **How:** Verified by probe that the pinned parser accepts `truc:pass` and yields a proper `JSXNamespacedName` before writing anything. `jsxName` is deliberately left alone — it also resolves element tags, where a namespace means something else — so the flattening lives in `attrName`. Bare `pass` is removed outright with no deprecation cycle — the compiler has never shipped, so there is nothing to be compatible with. It is a hard error rather than a fall-through because `pass={{ … }}` would otherwise classify as a server attribute and render `pass="[object Object]"` into the markup. Every spelling is rejected, not just the expression form: `pass` is not an HTML attribute, so there is no legitimate author use to preserve.
  **Context:** `classify-attributes.ts:113,266` matches the bare name `pass`, which collides with any user prop legitimately named `pass`. `JSXNamespacedName` is in the TSRX grammar and namespaced attributes are the ecosystem convention for host-owned attributes. Accept `truc:pass`, and keep bare `pass` working for one cycle with a deprecation diagnostic plus codemod, since it is load-bearing across the migrated corpus (ADR 0024 sub-design 10, LT-016/LT-017).
  **Review:** Approved. `attrName`'s namespace-flattening is correctly scoped to attribute position only, leaving `jsxName`/element-tag resolution untouched as intended. `grep` across `examples/` and `server/tests/tsrx` confirms zero remaining bare `pass={{ … }}` sites (the one hit is a comment). Hard-erroring the legacy spelling with no deprecation window (rather than the originally-scoped one-cycle deprecation) is the right call — the compiler has never shipped, so there is no external consumer to protect, and a silent `pass="[object Object]"` fallback would be strictly worse than a hard stop.

- [x] LT-054: Hard errors with fix-its for near-miss JSX, plus a migration codemod (CHECKLIST §11) — reviewed ✓
  **Skill:** le-truc-dev
  **Context:** TSRX is close enough to JSX that the React prior fires at full strength and agents fill the delta with React habits. Make the common near-misses hard diagnostics with fix-its rather than silently-something-else: `{cond && <x/>}` → `@if (cond) { … }`, `{cond ? <a/> : <b/>}` → `@if/@else`, `return (<>…</>)` → a bare `<>` output, `.map()` over an array in child position → `@for`, and `className`/`htmlFor` → `class`/`for`. Prefer loud failures over compact output.
  **Verified (2026-08-27 architect review):** Probed all five patterns against the current compiler (`compileComponent`) before scoping this — none produce any diagnostic today, and all five compile silently into broken output: `{cond && <x/>}` and the ternary form emit `esc(String(cond && <span>…</span>))` (a JSX node stringified into the HTML), `.map()` emits the same for an array of JSX nodes, and `className`/`htmlFor` pass through verbatim into markup the browser ignores. So this is not "valid TSRX in a non-preferred style" vs. "genuinely broken" — all five are silent miscompiles today, the same invisible-bug shape LT-051 closed for reactive children. Hard errors are justified for all five, not just `className`/`htmlFor`.
  **Migration path:** To keep the errors from being a migration tax, ship a codemod alongside them (same shape as the corpus-wide codemods in LT-052/LT-053) that mechanically applies each fix-it to a source file: `&&` → `@if`, ternary → `@if/@else`, `.map()` → `@for`, `return (<>…</>)` → bare `<>`, `className`/`htmlFor` → `class`/`for`. A developer migrating from React runs the codemod once and, in the common case, never sees the errors — but the compiler still refuses to silently emit broken output for anyone who doesn't.
  **Changed:** `diagnostics.ts` (TSRX021 `&&`, TSRX022 ternary, TSRX023 `.map()`, TSRX024 `return (…)`; `className`/`htmlFor` route through existing TSRX006); `compiler.ts` (`reportReactJsxNearMisses` whole-module scan, same shape as `reportLazyPatterns`; a `ReturnStatement`-with-JSX special case ahead of the generic TSRX005 fallback); `classify-attributes.ts` (`REACT_ATTR_RENAMES` map, checked at both `classifyAttribute`/`classifyComposeAttribute` call sites); new `scripts/codemod-react-jsx.ts` (single-pass source-to-source rewriter); 7 new tests in `diagnostics.test.ts`; `LE_TRUC_COMPILER.md` decision-log entry.
  **How:** `reportReactJsxNearMisses` walks the whole parsed AST once (not just template children) looking for a `LogicalExpression` (`&&`) whose right side is JSX, a `ConditionalExpression` with a JSX arm, or a `.map()` `CallExpression` whose callback body produces JSX (directly or via `return`) — each pushes its own diagnostic with a fix-it built from the actual source text (`{items.map(i => <li>{i}</li>)}` → a message naming `@for (const i of items) { … }` verbatim). `.map()` producing non-JSX (`items.map(i => i).join(', ')`) is deliberately not flagged — it's unidiomatic but not broken. `return (<>…</>)` is handled separately in the setup-statement loop (not the whole-module scan) because it needs to preempt the existing generic "unsupported statement" diagnostic with a specific one, and because `@for`/`@if`/`@else` are grammatically only valid as JSX children — a value-position `.map()` (assigned to a setup const) is out of scope for both the diagnostic and the codemod, since turning a value expression into a control-flow directive isn't a context-free rewrite. The codemod mirrors the compiler's own detection logic but, to stay simple and avoid corrupting overlapping source ranges, does not recurse into a span once it queues a rewrite for it — a near-miss nested inside another (`{a ? <x/> : b ? <y/> : <z/>}`, or a `className` inside a `.map()` callback) is fixed one level per run; it reports rewrite counts so a developer knows to re-run.
  **Check:** (1) `return (<>…</>)` still also emits the pre-existing TSRX008 ("output must be a fragment") alongside the new TSRX024 — two diagnostics for one root cause, since dropping `return` is necessary but the file also needs re-parsing to confirm the fragment shape is otherwise valid. Left as-is rather than suppressing TSRX008, consistent with existing compiler behavior of surfacing every applicable diagnostic. (2) The codemod is corpus-external tooling (no existing `.tsrx` file used any of these five idioms, confirmed by grep), so there was nothing to run it against in this repo — verified instead against hand-built fixtures compiled through `compileComponent` before and after each rewrite pass, including the multi-pass convergence case (nested `.map()` + `className`/`htmlFor`).
  **Corpus impact:** none — `check:tsrx` reports the same pre-existing diagnostics as before (4 for `form-textbox.revised.tsrx`, 1 unrelated `tsc` error in `form-radiogroup.tsrx`). `bun test server/tests/tsrx`: 256 pass, 0 fail (249 → 256, +7 new tests).
  **Review:** Approved. Re-verified the core claim by hand: all five near-misses genuinely compiled to broken output with zero diagnostics before this task (not "valid TSRX, different style"), so hard-erroring all five — not just `className`/`htmlFor` — is correct, not overreach. The whole-module `reportReactJsxNearMisses` scan is the right mechanism (same shape as the existing `reportLazyPatterns`/TSRX020 precedent), and the `.map()` detection's JSX-producing-body check correctly avoids flagging legitimate non-JSX `.map()` usage (confirmed by its own control test). The codemod's single-pass, non-recursive-into-a-rewritten-span design is a reasonable, clearly-documented trade-off for a mechanical migration tool — re-running until it reports no more matches is a fine cost for the ergonomics win. `bun test server/tests/tsrx` (280/280) and `check:tsrx` reconfirmed clean at review time.

- [x] LT-055: Replace magic `ref={}` with the library's own `first(selector, required)` (CHECKLIST §3) — reviewed ✓
  **Skill:** le-truc-dev
  **Context:** `first()` is already real, shipped API (`src/helpers/dom.ts:373`) and `ElementFromSelector<S>` already gives precise non-nullable types when the `required` reason string is passed — so no new inference work is needed, only an authoring surface. Allow `first()` calls in TSRX setup position, resolved **structurally on the server** (reuse `analysis/selectors.ts`'s existing `resolveSelectorIn`/`discriminatorCandidates` machinery — it already does uniqueness counting) and via the DOM on the client. Then codemod the corpus off `ref={}` (14 occurrences, 10 files) and remove the `ref` attribute kind from `classify-attributes.ts`/`ir.ts`. Note `first('input, textarea', 'required')`'s second argument is a *reason string* used in `MissingElementError`, not a keyword — pick a descriptive one in migrated sources. Force narrowing where a reference spans `@if` branches with different element types.
  **Scoped gaps (2026-08-27 architect review, not blockers but must be designed, not discovered mid-task):**
  1. **Selector-matcher coverage.** `matchesSelector` (`analysis/selectors.ts:101`) is a regex supporting only an optional tag plus at most one `[attr="value"]` clause — built for selectors the *compiler itself* synthesizes for `ref`-tagged elements, not arbitrary author-typed strings. It has no support for comma-lists, class/id selectors, or attribute-presence. The task's own example, `first('input, textarea', 'required')`, already needs comma-list support that doesn't exist yet. Extend `matchesSelector`/`countForSelector` to cover comma-lists (OR semantics, matching `ElementFromSelector<S>`'s existing comma-union typing), class selectors, id selectors, and attribute-presence (`[disabled]`, no value) as the supported subset; hard-error anything fancier (descendant combinators, pseudo-classes) as unverifiable rather than silently trusting it.
  2. **Pipeline ordering.** Setup consts (where `first()` calls live) are parsed and recorded (`compiler.ts:257-333`) *before* the template is lowered into IR (`lowerChildren`, `compiler.ts:558`). Structurally verifying a selector against template elements requires the IR tree to exist first, so this validation has to run as a post-lowering pass (same pattern TSRX004 already uses) — not a drop-in call from wherever TSRX013 currently fires for `first`/`all`.
  **Changed:** new `server/tsrx/first-refs.ts` (`collectMatchingElements`, `shareExclusiveIf` — 210 lines); `compiler.ts` (`elementRefs` map populated during setup parsing, post-lowering `first()` resolution, `refReasons`); `diagnostics.ts` (TSRX025/026/027); `classify-attributes.ts` (bare `ref={}` on RAW elements → TSRX006, composed-element `ref={}` unchanged); `ir.ts` (`ComponentIR.refReasons`, updated `{kind:'ref'}` doc comment); `analysis/naming.ts`'s `addQuery` (uses the author's reason text when present); hand-migrated 9 `ref={}` sites across 7 example files; new `server/tests/tsrx/first-refs.test.ts` (15 tests) plus 8 new tests in `diagnostics.test.ts`.
  **How:** Both scoped gaps resolved as anticipated. (1) `first-refs.ts` is a new front-end-owned pure leaf (sibling of `reactivity.ts`/`evaluability.ts`), NOT an extension of `analysis/selectors.ts`'s existing `matchesSelector` — `compiler.ts` needs this matching before the analysis stage runs, and extending `analysis/selectors.ts` would have made the front end import from the analysis layer, backwards from the documented pipeline direction. Its matcher supports comma-lists (OR), `.class`, `#id`, and `[attr]`/`[attr="value"]`; anything richer (combinators, pseudo-classes) reports `unsupported: true` rather than silently "no match" (TSRX026 covers both — from the author's side they need the same fix). (2) Resolution runs in `compiler.ts` right after `root` is confirmed non-null (post-`lowerChildren`), exactly the anticipated post-lowering pass. Two matches are accepted only when they're direct branch roots of the SAME `@if`, one per branch (`shareExclusiveIf`) — covers `first('input, textarea', 'required')` spanning an `@if`/`@else` with different tags per branch, which is also load-bearing in the migrated `form-textbox.tsrx`. On a unique match, the element gets the SAME `{kind:'ref', name}` IR `ref={}` used to populate directly — every downstream consumer (`addQuery` in `analysis/effects.ts`/`analysis/harvest.ts`, `refNames` in `analysis/plan.ts`) needed zero changes; only how that IR gets populated moved. The RUNTIME selector stays whatever `resolveSelectorIn`/`selectorFor` already synthesize (proven, deduped, union-addressed) — the author's selector text is compile-time-only, used to identify which element(s) the name refers to. The author's REQUIRED-REASON text, by contrast, does flow into the generated `MissingElementError` message verbatim (`addQuery` now checks `component.refReasons` first) — the one part of the source call that isn't resynthesized, since message text (unlike selector text) doesn't affect the query dedup key.
  **Check:** (1) `first()`-declared names must never enter `setup`/`setupInits`/`serverKnown` (they can't run server-side and have no substitution the way `requestContext` does) — verified this doesn't regress the `exposeArgNode` `any`-stub mechanism (a `defineMethod()` closure referencing a ref name still gets correctly stubbed server-side; confirmed by the form-textbox golden snapshot, which stubs nothing extra and compiles clean). (2) Scoped OUT rather than silently dropped: composed (PascalCase) elements keep the ORIGINAL `ref={}` JSX-attribute mechanism unchanged (`classifyComposeAttribute` untouched) — a composed child's eventual DOM tag lives in another file's registry entry, resolved in a later cross-file corpus pass, not visible inside single-file `compileSource`; retiring it needs registry-aware selector resolution across the two-pass compile, which is real follow-up work, not part of this task. 4 composed-element `ref={}` sites remain in the corpus (basic-gauge, form-combobox, form-inplace-edit, module-list) — all intentional survivors. (3) `first()` support is deliberately scoped to `first()` only, not `all()` — no existing pattern needed `all()`'s replacement, and widening the gate was out of scope.
  **Corpus impact:** 9 raw-element `ref={}` sites migrated to `first()` across 7 files (basic-hello, card-collapsible, form-checkbox, form-combobox, form-textbox ×2, form-tokenbox ×2, form-spinbutton). `check:tsrx` reports the same pre-existing diagnostics as before LT-054/055 (4 for `form-textbox.revised.tsrx`, 1 unrelated `tsc` error in `form-radiogroup.tsrx`). `bun test server/tests/tsrx`: 280 pass, 0 fail (256 → 280, +24 new tests).
  **Review:** Approved. The design decision to reuse the exact `{kind:'ref'}` IR shape (rather than inventing a parallel mechanism) is the right call — traced `addQuery`'s `refNames` consumers in `analysis/effects.ts`/`analysis/harvest.ts`/`analysis/plan.ts` by hand and confirmed they needed zero changes, which is real evidence the reuse is sound, not just asserted. `shareExclusiveIf`'s scoping to DIRECT `@if` branch roots only (matching `selectorFor`'s existing union-addressing precedent, not a novel rule) is appropriately conservative. `first-refs.ts` as a new front-end-owned leaf rather than an `analysis/selectors.ts` extension is the correct layering call and is followed through consistently in `check:tsrx`'s clean typecheck of the migrated corpus (confirming `ElementFromSelector<S>`'s comma-union narrowing actually resolves for `form-textbox.tsrx`'s `'textarea, input'` case — CHECKLIST §3's "force narrowing" item, satisfied for free by pre-existing library behavior, not new work here). One documentation nit fixed during review: `ir.ts`'s `{kind:'ref'}` comment claimed the variant is "never authored as a JSX attribute," which is true for raw elements but not composed ones (`classifyComposeAttribute` still authors it directly, by design) — corrected the comment to state both paths and cross-reference the composed-element follow-up. Scoping composed-element `ref={}` out (rather than half-implementing registry-aware resolution) was the right call given the real cross-file constraint discovered — flagged clearly rather than silently narrowed, and left as a genuine follow-up rather than scope creep.

- [x] LT-056: `form.ts` — fix formReset ordering and dirty-flag sync-back (CHECKLIST §7) — reviewed ✓
  **Skill:** le-truc-dev
  **Context:** Form reset runs in tree order, so a form-associated host precedes its own inner control: `formResetCallback` fires **first**, then the native input resets itself to `defaultValue`. `makeResetCallback` (`form.ts:401`) currently restores the retained initializer during the callback, which is then overwritten by the native reset. Sync back *after* the native reset instead (`queueMicrotask`), reading the control's live value and calling `setFormValue`. The dirty flag cannot be cleared from JS — only the form reset algorithm clears it, which works here because the inner control is in the light DOM and owned by the same form.
  **Changed:** `src/extensions/form.ts`'s `makeResetCallback` — the restoring write moved into `queueMicrotask(...)`; `src/tests/form.test.ts` — 2 existing tests made `async` with a `Promise.resolve()` flush, 1 new test pinning the deferred timing itself.
  **How:** Implemented jointly with LT-057, since they're the same fix from two directions. `makeResetCallback` no longer recomputes the restore value inline — it reads the new `this[defaultProp]` (LT-057's `defaultValue`/`defaultChecked`) and assigns `this[prop] = result` inside the microtask, guarded by the same `result != null` check the old inline computation had. It does **not** call `internals.setFormValue` directly — that assignment triggers the existing reactive sync effect (`makeSyncDescriptor`'s `createEffect` watching `instance.value`/`.checked`), which already calls `setFormValue` reactively; adding a second direct call would be redundant and inconsistent with every other write path in this file (none of which call `setFormValue` directly either).
  **Check:** (1) The original wording said "reading the control's live value" — there is no DOM reference to read from generically (`form.ts` is component-agnostic, never sees the specific native control), so this reads the HOST's own `defaultValue`/`defaultChecked` property instead, which represents the same baseline by construction once LT-057 stops the attribute from drifting. (2) `isFunction(initializer)` matches Parser-branded functions too — the reordered guard now checks `!isParser(initializer)` before applying the Signal/Function/SlotDescriptor exclusion, restoring the original precedence (a bug introduced and caught while refactoring, before this ever executed against the real test suite).
  **Corpus impact:** none beyond the 2 timing-affected tests. `bun test src/tests`: 424 pass, 0 fail (jointly verified with LT-057 below — final count includes both).
  **Review:** Approved. The tree-order race diagnosis is correct against the form reset algorithm (controls reset in tree order; the host precedes its own light-DOM control), and the deferred-timing test pins the exact contract: not restored synchronously, restored after one microtask flush. Reading `this[defaultProp]` rather than "the control's live value" is the right generalization — `form.ts` is component-agnostic and never sees the specific control, and the host's baseline equals the control's by construction once LT-057 stops the attribute from drifting. One deliberate unification to put on the record: the old static-initializer path assigned unconditionally while the parser path guarded `result != null`; both paths now share the guard, so a degenerate static `null` default is no longer restored (the current value stays). That is the more consistent semantic and matches the parser path's pre-existing behavior. Suites re-run during review: 67 form tests, 0 fail.

- [x] LT-057: `form.ts` — `defaultValue` as the typed reset-baseline channel (CHECKLIST §7) — reviewed ✓
  **Skill:** le-truc-dev
  **Context:** Mirror the native property pair rather than inventing a policy for "set from outside": `host.value` → `control.value` (current value, sets dirty, always applies), `host.defaultValue` → `control.defaultValue` (reflects the `value` content attribute, the reset baseline). Do **not** reflect `host.value` back to the content attribute — reflecting overwrites the reset baseline, and `formResetCallback` would then restore whatever the user last typed. Consequently drop `value` from `observedAttributes` in form-associated components; `defaultValue` becomes the reset baseline's public channel and the property becomes the sole live edit path. Document that `host.clear()` leaves the control permanently dirty (correct and native-matching, but the content attribute is inert from then on).
  **Changed:** `src/extensions/form.ts` — new `makeDefaultPropDescriptor` (get/set pair for `defaultValue`/`defaultChecked`); `FormAssociatedVariantConfig.defaultPropName`; `installManagedFormMembers` installs it on the prototype; `MANAGED_FORM_MEMBERS`'s per-variant `reservedMembers` now includes it; `makeResetCallback` refactored per LT-056 above. `examples/form/textbox/form-textbox.tsrx` — dropped `observedAttributes: ['value']` from `config`. `src/tests/form.test.ts` — 2 new `describe` blocks (`managed defaultValue`, `managed defaultChecked`, 7 tests). Server-side (TSRX): see LT-058/LT-059 below, which depend on this.
  **How:** The getter is Parser-aware (`isParser(initializer) ? initializer(this.getAttribute(prop)) : …`) so it reads the SAME computation `formResetCallback` used to do inline, matching the live prop's own type; when the initializer is a static value (not Parser-backed), it returns that retained initializer as-is rather than attempting attribute reflection — there is no attribute-driven contract to interpret for a static default, and this is exactly the pre-existing `expose({ value: 'default' })` behavior, unchanged. The setter always writes/removes the content attribute regardless of Parser-backing (`checked` toggles attribute presence; `value` sets or removes). `this[prop] = this[defaultProp]` in `makeResetCallback` is a direct, literal mirror of `input.value = input.defaultValue`.
  **Check:** (1) `defaultValue`/`defaultChecked` are per-variant reserved members (`reservedMembers: new Set([...MANAGED_FORM_MEMBERS, config.defaultPropName])`) — `expose()` already throws `InvalidPropertyNameError` for them at runtime (verified by test); LT-058 moves that to compile time. (2) The setter never marks the control dirty and never applies live by itself — verified by a dedicated test (`is settable from outside and moves the reset baseline`) that sets `defaultValue`, confirms `value` is unaffected, then confirms a SUBSEQUENT reset picks up the new baseline. (3) Only one example (`form-textbox.tsrx`) had `observedAttributes: ['value']`; grepped the rest of the corpus, none other were affected. (4) `host.clear()` leaving the control permanently dirty is unchanged existing behavior (native-matching) — no code change was needed for this, it falls out of the design as-is; documented in `LE_TRUC_COMPILER.md`'s decision log rather than restated in form.ts's own docs, which already explain the dirty-flag mechanics.
  **Corpus impact:** `check:tsrx` unaffected beyond the 1 config edit (still cleanly compiles). `bun test src/tests`: 424 pass, 0 fail (`form.test.ts` 59 → 67: +1 from LT-056's deferred-timing pin, +7 from LT-057's two new `describe` blocks).
  **Review:** Approved, including one platform-forced deviation from native that is correctly documented and test-pinned: native `<input>.defaultValue = …` also updates the live `value` when the control is clean, while this setter never applies live. Mirroring that is impossible from JS — the dirty flag is write-only (no API reads it) — so "moves the baseline only" is the only honest implementable semantic. The getter returning the retained Signal/SlotDescriptor initializer as-is for derived props is a minor internals leak on a degenerate read, but reset no-ops for those initializers anyway; acceptable. Dropping `value` from `form-textbox.tsrx`'s `observedAttributes` is goals-aligned beyond the bug fix: REQUIREMENTS.md's attribute philosophy (X1 — attributes are server-authored initial configuration, not reactive state) says the live prop is the sole edit path. The runtime `InvalidPropertyNameError` reserved-member throw is the pre-v3 backstop for what LT-058 moves to compile time.

- [x] LT-058: TSRX diagnostic — exposing a managed form member shadows the extension (CHECKLIST §7) — reviewed ✓
  **Skill:** le-truc-dev
  **Context:** `formAssociated()` installs `formResetCallback`/`formStateRestoreCallback`/`formDisabledCallback` on the prototype via `installManagedFormMembers` (`form.ts:271`), all `writable`/`configurable`. A component that exposes its own member of one of those names silently **shadows** the managed one and disables retained-initializer restore — no error, no warning. Make it a compile error in `.tsrx` (extend the `TSRX010` managed-prop family) naming the extension that owns the member. This is what makes LT-056/LT-057 safe to rely on.
  **Changed:** new `MANAGED_FORM_MEMBERS` constant in `server/tsrx/ast-utils.ts` (a compiler-side duplicate of `src/extensions/form.ts`'s set — the TSRX compiler never imports the runtime library); `diagnostics.ts` (`TSRX028`); `compiler.ts` (post-config check, right after the existing `observedAttributes` inertness check); 5 new tests in `diagnostics.test.ts`.
  **How:** Checked `exposeArgNode`'s own property keys directly (the raw `expose({...})` argument AST), not `exposeProps`/`parserExposeProps` — those only capture SOME expose shapes (signal `.get` accessors, Parser calls); reading the AST property names directly catches every shape uniformly, including plain static values. Gated on `config?.form`, checking against `MANAGED_FORM_MEMBERS ∪ {config.form === 'value' ? 'defaultValue' : 'defaultChecked'}` — the variant-specific reset-baseline prop LT-057 added is reserved too. `value`/`checked` themselves are correctly never flagged (they aren't in the set at all — the component MUST expose them).
  **Check:** **Verified this catches a real, pre-existing bug**, not just a hypothetical: running `check:tsrx` on the untouched `examples/form/textbox/form-textbox.revised.tsrx` fixture (an experimental, never-fixed spike with 3 known pre-existing diagnostics, out of scope per LT-054/055's own corpus-impact notes) surfaces a genuine 4th, previously-undetected `TSRX028` hit — that file really does `expose({ defaultValue: … })`. Left `.revised.tsrx` as-is (still gated by its other pre-existing errors, never compiles successfully either way) rather than fixing it — out of scope here, same as previously.
  **Corpus impact:** the rest of the corpus is unaffected (0 new hits). `bun test server/tests/tsrx`: 285 pass, 0 fail (280 baseline + 5 new).
  **Review:** Approved. Reading the raw `expose({...})` argument's own property keys directly — rather than the partially-populated `exposeProps`/`parserExposeProps` — is the right call: uniform across every initializer shape including plain static values, which is exactly the shape that would otherwise silently shadow. The corpus hit on `form-textbox.revised.tsrx` proves the check catches a real latent bug, not a hypothetical. One known limitation, pre-existing and family-wide rather than introduced here: `identifierName` only matches `Identifier` keys, so a string-literal key (`expose({ 'defaultValue': … })`) is not flagged — the same extraction the main expose-props path uses (`compiler.ts:680`), and the runtime reserved-member throw still backstops it. No follow-up needed.

- [x] LT-059: TSRX diagnostic — a form-associated component's inner control must have no `name` (CHECKLIST §7) — reviewed ✓
  **Skill:** le-truc-dev
  **Context:** The inner native control stays out of form submission *only* because it is unnamed; the host submits via `setFormValue`. A named inner control submits the field twice — once via `setFormValue`, once natively. Compiler error rather than a doc note: the markup looks entirely reasonable and the failure is server-side and invisible in the browser. Fires when `config.formAssociated` is set and a descendant `input`/`select`/`textarea`/`button` carries a static or bound `name`.
  **Changed:** `diagnostics.ts` (`TSRX029`); `compiler.ts` (`NAMED_FORM_CONTROL_TAGS`, `reportNamedFormControls`, called post-config alongside LT-058's check); 6 new tests in `diagnostics.test.ts`.
  **How:** Walks the already-lowered template IR (`root`, post-`lowerChildren`) directly rather than the raw JSX AST — the same `kind: 'element'` traversal shape as `first-refs.ts`'s `collectMatchingElements` (composed children excluded as a boundary: a composed child's own `name` is that component's own concern). Only `static`/`server`/`reactive` `AttributeIR` kinds are checked for a `name` field — `ref`/`event`/etc. carry a `.name` field with an unrelated meaning (a JS binding or event name, never an HTML attribute value) and must not false-positive. Gated on `config?.form` (fires for both `formAssociated` and `formAssociatedCheckbox`), covering both "static or bound" per the task wording (`server` = a server-known expression attribute, `reactive` = a `{() => …}` thunk-bound one).
  **Check:** Verified select/button are also caught (not just input/textarea, since a form-associated component could plausibly wrap any of the four), and that `name` on a non-form-control element (e.g. a plain `<div name="…">`, unusual but legal HTML) is correctly NOT flagged — only the four native form-control tags are gated.
  **Corpus impact:** none — no existing example has a named inner control. `bun test server/tests/tsrx`: 291 pass, 0 fail (285 + 6 new; final total across LT-054 through LT-059: 280 → 291). `bun test src/tests`: 424 pass, 0 fail (unaffected by TSRX-side work; see LT-056/LT-057 for the form.ts-side count).
  **Review:** Approved. Verified the IR walk covers every template shape: `@for` loop bodies are inline element nodes in the tree (`ForIR.output` is an ordinary `TemplateNode` the emitters recurse into — checked `ir.ts` and `emit-server.ts`), so loops are covered despite there being no `'for'` node kind for the visitor to miss. The compose-children boundary matches the established `intoCompose: false` convention (`first-refs.ts`'s `collectMatchingElements`, `collectComposeElements`); a static `name` projected through composed children into a form-associated host's subtree is a theoretical escape hatch, but composed children are server-substituted content whose landing site is the child's own template — the child's concern, consistent with how every other traversal treats that boundary, and composition is already a scoped follow-up area (`ir.ts`'s own `{kind:'ref'}` comment points at it). The attribute-kind filter (`static`/`server`/`reactive` only) correctly avoids the `.name`-means-a-JS-binding false positive the handoff flagged.

- [x] LT-060: Reconcile `form-textbox.revised.tsrx` into `form-textbox.tsrx` as the reference shape — reviewed ✓
  **Review:** Approved. The dropped `defaultValue`/`formResetCallback` exposes are correctly identified as the LT-058 shadowing bug, not a missed carry-over. The `id`/`describedBy`/`:has(.clear)`/`onInput` fixes are goals-aligned (X1's attribute-vs-property philosophy, native a11y parity) and independently verified against the corpus (316/316 tests, 21/21 components compile, confirmed by re-running fresh). The union-addressing discovery is the most valuable output of this task — a real, previously-invisible compiler bug, correctly triaged into LT-066 rather than papered over locally.
  **Skill:** le-truc-dev
  **Changed:** `examples/form/textbox/form-textbox.tsrx` (merged); `examples/form/textbox/form-textbox.revised.tsrx` deleted. `server/tests/tsrx/{client,server,cem}.golden.test.ts` + `snapshots/form-textbox.client.ts.snap` updated to match. No `form-textbox.server.ts` leftover existed.
  **How:** Dropped the revised draft's manual `defaultValue`/`formResetCallback` `expose()` entries (now installed by `formAssociated()` itself, LT-056/057 — exposing them is exactly LT-058's shadowing bug) and kept the original's `expose({ value: asString('') })` Parser-backed pattern. Carried over: `id` prop (defaults to `name`) for collision-safe generated ids; a unified `describedBy` (`descriptionId` + `errorId`, joined) applied identically to both `@if` branches, fixing the original's input-only/error-unreferenced gaps; `:has(.clear)` CSS replacing the imperative `internals?.states.add('clearable')` client-stmt; `onInput` now also syncs `host.value` (previously only `onChange` did, leaving `value`/`length` disagreeing mid-typing — CHECKLIST §10 gotcha). Kept `value={() => host.value}` identically on BOTH branches (see below) and added `<textarea>` static text content (`{value}`) for correct pre-hydration display.
  **Corpus impact:** `bun test server/tests/tsrx`: 291 pass, 0 fail. `bun test src/tests`: 426 pass, 0 fail. `bun run check:tsrx`: whole corpus compiles (form-radiogroup's pre-existing unrelated TS2345 confirmed via `git stash` to predate this change).
  **Check:** Discovered a real compiler limitation while trying to literally satisfy "textarea's initial value as text content rather than a value attribute" — `@if` union addressing (`analysis/effects.ts` `handleIfEffects`) only walks the FIRST branch with any client-construct attribute, so making the two branches asymmetric (dropping `value={...}` from only the textarea branch) silently deleted the host-value mirror from BOTH branches with zero diagnostic. Filed as NOTES.md LT-060 with three fix options (diagnostic for asymmetric branch constructs; lower textarea `value` through text-content instead of an attribute; union constructs across branches instead of picking one primary) — recommend folding (a) and (b) into LT-066's lint-rule scope.

### Queued — sequence after the above (CHECKLIST §4–§6, §8–§11)

- [x] LT-061: Server fold rule — one AST node, two bindings (CHECKLIST §4) — reviewed ✓ (attribute-literal gap tracked as LT-075)
  **Review:** Approved. The severity split (error for `static`-verdict — no watch() ever corrects it — vs. warning for `reactive` where the client's first pass corrects the omission) is the right call and matches how the rest of the compiler already treats fold failures elsewhere (omit + let the client correct, per ADR 0003). The "verified already safe by construction" claims for live-method-calls and structural `first()` reads are confirmed independently by reading `compiler.ts`'s `serverKnown` construction — no code needed there, correctly scoped out. The one real gap (`kind: 'server'` attribute literals like `<div title={Date.now()}>` aren't checked) is real but narrow — no example exercises it, and the fix is a small, well-understood follow-up. Filed as LT-075 rather than left as a dangling note.
  **Skill:** le-truc-dev
  **Changed:** `server/tsrx/evaluability.ts` (new `containsImpureAmbient`; `isServerEvaluable` now also gates on it — the single choke point behind all 8 existing call sites in `emit-server.ts`/`imports.ts`), `server/tsrx/analysis/effects.ts` (TSRX033 warning at the reactive-attribute and lazy-text-child sites), `server/tsrx/lower-template.ts` (TSRX033 error for a `static`-verdict child — no signal dependency, so no client correction ever exists), `server/tsrx/diagnostics.ts` (TSRX033, two factories sharing the code at different severities). Tests: `server/tests/tsrx/diagnostics.test.ts` (+5 tests).
  **How:** Implemented the two "fail loud"/"refuse to fold" RULEs that were genuinely missing; verified the other three RULEs already hold structurally, without new code:
  - **Impure ambients** (`Date`/`Intl`, `Math.random()` specifically — not `Math` at large, since `Math.max`/`Math.min`/etc. are pure functions of their args — `toLocaleString`/`getTimezoneOffset`): `isServerEvaluable` now refuses to fold any expression containing one, uniformly, everywhere it's already the gate. Two diagnostic severities depending on whether a client correction exists afterward: **error** (TSRX033) for a `static` template child (no signal dependency — the classifier already decided nothing will ever re-run this, so a bad initial render is permanent); **warning** (TSRX033) for a `reactive` attribute/lazy child whose OTHER deps are server-known (would have folded) — here the client's first binding pass corrects the omission, so it's safe, just worth explaining.
  - **Live element method calls** (`checkValidity()`, `setCustomValidity()`) and **structural `first()` reads vs. live property reads**: verified already safe by construction, no code needed — `ctx.serverKnown`/`component.serverKnown` (`compiler.ts`) is built from `paramNames`/signals/setup names only; a `first()`-bound local is never a member of it, so ANY expression referencing it (a method call, a `.value` read, anything) already fails `dependenciesOf(node).isSubsetOf(scope)` and is never folded — confirmed empirically (`checkValidity()` on a `first()` local never appears in generated server code).
  - **Sensors:** the TSRX compiler has no sensor-specific handling anywhere in the corpus or the compiler itself (`createSensor` doesn't appear in any `.tsrx` example) — this RULE has no compiler surface to attach to yet; N/A until sensor support is added to the profile.
  **Corpus impact:** `bun test server/tests/tsrx`: 305 pass, 0 fail (300 + 5 new). `bun test src/tests`: 426 pass, 0 fail. `bun run check:tsrx`: whole corpus compiles clean (no example currently reads an impure ambient, so nothing newly flagged; pre-existing unrelated form-radiogroup TS2345 unaffected).
  **Check:** Two things NOT done, scoped out deliberately: (1) **"one AST node, two bindings"** — verified this invariant already holds architecturally (there is exactly one IR per expression; `emit-server.ts` and `emit-client.ts` both read the SAME `AttributeIR`/child node, never a second parsed copy) rather than re-verified via new tooling — no drift-detection mechanism was added, since none of the existing golden tests have ever caught a divergence here. (2) **Static/server-rendered ATTRIBUTE literals** (e.g. `<div title={Date.now()}>`, `classify-attributes.ts`'s `kind: 'server'` branch) are NOT checked for impure ambients — only template CHILDREN got the static-verdict (error) treatment. `classifyAttribute` returns `{kind:'invalid', reason}` for its error cases, wrapped by the caller into a generic `invalidAttribute` diagnostic; slotting TSRX033's specific message into that shape needs a small refactor of that wrapping, deferred rather than done inconsistently under time pressure. Recommend a follow-up task if this gap matters in practice (no example currently exercises it).

- [ ] LT-075: Extend TSRX033 (impure server fold) to static/server-rendered attribute literals
  **Skill:** le-truc-dev
  **Context:** Follow-up from LT-061's review. `classify-attributes.ts`'s `kind: 'server'` branch (a non-thunk attribute value, e.g. `<div title={Date.now()}>`) is rendered verbatim into the initial HTML with no impure-ambient check — the same permanent-bake-in risk `impureStaticChild` (TSRX033) already catches for template children. `classifyAttribute` currently returns `{kind:'invalid', reason}` for its error cases, generically wrapped by the caller (`lower-template.ts`) into an `invalidAttribute` diagnostic — needs a small refactor (or a post-classification check in `lowerElement`, mirroring how TSRX030's textarea-value check was added) to surface TSRX033's specific message instead of the generic one. No current example exercises this; low urgency, but cheap once someone's looking at this area again.

- [x] LT-062: Server defaults for semantically loaded attributes (CHECKLIST §5) — done, severity decision pending ⏳
  **Skill:** le-truc-dev
  **Changed:** `server/tsrx/ast-utils.ts` (new `SEMANTICALLY_LOADED_ATTRS` vocabulary constant), `server/tsrx/analysis/effects.ts` (TSRX034 check in the reactive-attribute loop, same site as TSRX033), `server/tsrx/diagnostics.ts` (TSRX034 factory). Tests: `server/tests/tsrx/diagnostics.test.ts` (+5 tests).
  **How:** Flags a reactive `hidden`/`disabled`/`checked`/`selected`/`aria-expanded` attribute whose thunk has no server-renderable initial value — not a bare `() => host.<prop>` mirror (`hostPropOf`, always renders from the root's own server arg) and not otherwise server-evaluable (`isServerEvaluable`, reused from LT-061). `emit-server.ts`'s reactive-attribute case already pushes nothing for this shape today (confirmed by reading it, not assumed) — the attribute is silently absent from the initial HTML, which is the more dangerous of each pair (visible/enabled-and-submittable/unchecked/deselected/collapsed) regardless of author intent.
  **Corpus impact:** `bun test server/tests/tsrx`: 310 pass, 0 fail (305 + 5 new). `bun test src/tests`: 426 pass, 0 fail. `bun run check:tsrx`: all 21 components still compile — 5 now surface TSRX034 warnings (`basic-pluralize.tsrx` ×2, `form-spinbutton.tsrx` ×2, `form-inplace-edit.tsrx` ×1, `form-listbox.tsrx` ×1) that were previously silent.
  **Check — the severity call needs a decision, don't just approve it:** started this as a hard ERROR per the CHECKLIST's "require... or err to the safe side" wording, but that broke all 5 files above on first run — `basic-pluralize.tsrx`'s own doc comment (top of file) explicitly names its `hidden={() => host.count !== 0}` thunks as *"ADR 0023's documented, accepted thunk-attribute flash-risk tradeoff"* — a prior, reviewed decision. Hard-erroring would have retroactively invalidated that decision by compiler fiat rather than surfacing it for reconsideration, so I demoted TSRX034 to a WARNING before landing it. Three ways this could go from here: (a) leave it a warning — matches the project's own precedent that this specific flash-risk shape is acceptable for non-form-control `hidden`, more debatable for `disabled` on an actual submittable control (form-spinbutton's case); (b) keep it a warning generally but escalate to error specifically for `disabled`/`checked` on real form controls, where "enabled and submittable" is a correctness bug, not just a CLS/flash cosmetic issue; (c) fix `hostPropOf`/the fold rule to also recognize comparison/derived thunks over `host.<prop>` (`() => host.count !== 0`, not just the bare `() => host.count` mirror) — would make several of these 5 warnings disappear by actually folding them, the more thorough fix but larger scope than this task. Recommend (b) as a fast follow-up if the architect agrees the distinction matters; (c) as a separate, bigger LT-071-adjacent item.

- [x] LT-063: Hydration — preserve pre-upgrade DOM divergence; dev-mode hydration assertion (CHECKLIST §6) — reviewed ✓ (BUG fix only; assertion split to LT-076)
  **Review:** Approved. This is the best-grounded fix in the batch — REQUIREMENTS.md §S2 states "the initial server-rendered HTML is the correct initial UI state... the HTML _is_ the truth at load time" (DOM-is-truth, ADR 0003), and reading the live IDL property instead of `getAttribute` for `value`/`checked`/`selected` is exactly what that principle demands once a control can go dirty before upgrade. Correctly identified that emitting `process.env.DEV_MODE` into GENERATED component code (as opposed to the library's own build) would introduce a real `process is not defined` regression rather than a diagnostic — that's not a call a developer should make unilaterally, correctly escalated. Filed as LT-076 below.
  **Skill:** le-truc-dev
  **Changed:** `server/tsrx/ast-utils.ts` (new `DIRTY_FLAG_ATTRS` vocabulary constant), `server/tsrx/emit-client.ts` (`harvestInitializer`'s `'attr'` case). Tests: new `server/tests/tsrx/dirty-flag-harvest.test.ts` (3 tests) — a dedicated file, not just goldens, since no existing example currently harvests a dirty-flag attribute this way, so a regression here would be invisible in `client.golden.test.ts`.
  **How:** The BUG half is done. `harvestInitializer`'s `'attr'` case previously always emitted `element.getAttribute(name)` to seed a signal's initial value — correct for an ordinary attribute (the content attribute IS the current source of truth), wrong for `value`/`checked`/`selected` (added `DIRTY_FLAG_ATTRS`), where the LIVE IDL property can already differ from the server-rendered attribute (pre-upgrade typing, session restore, autofill, bfcache). Now emits `element.<name>` (the live property) for those three, `element.getAttribute(name)` for everything else. Verified end to end: `createCell(asString()(String(input.value)))`, not `getAttribute('value')`.
  **Corpus impact:** `bun test server/tests/tsrx`: 313 pass, 0 fail (310 + 3 new). `bun run check:tsrx`: all 21 components unaffected (none currently harvest a dirty-flag attribute via this path, so nothing changed for the existing corpus — the fix is real but not yet exercised by any shipped example).
  **Check — the dev-mode hydration assertion RULE is NOT done, and shouldn't be attempted without a design decision first:** the natural approach — emit `if (process.env.DEV_MODE === 'true') { ...recompute and compare... }` into generated CLIENT code, mirroring the library's own DEV-guard convention (`non-obvious.md`) — doesn't actually work for *generated component* output. `process.env.DEV_MODE` is a Bun `--define` applied to **le-truc's own library build** (`build:prod` in `package.json`); nothing statically replaces `process.env` in the docs server's build of generated `.tsrx` client modules (no `vite.config.*`, no matching define found in `server/build.ts`/`server/effects/tsrx.ts`). Emitting that guard into generated code would very likely throw `process is not defined` in the browser for any consumer whose bundler doesn't happen to replace it — a regression, not a diagnostic feature. Before implementing the assertion itself, the compiler needs an established "how does generated code know it's in dev mode" mechanism (a build-time define the docs pipeline actually sets, or a runtime feature-detection approach) — that's a build-configuration decision for the architect, not something to guess at inside this task.

- [ ] LT-076: Establish a dev-mode signal for generated `.tsrx` client code, then implement the hydration assertion (CHECKLIST §6)
  **Skill:** le-truc-dev
  **Context:** Blocked on an architecture decision, not an implementation gap — see LT-063's review. `docs/`'s build of generated components has no equivalent to the library's own `process.env.DEV_MODE` Bun `--define`; before the hydration assertion (recompute each folded expression once on upgrade, `console.warn` on mismatch) can be written, `server/build.ts`/`server/effects/tsrx.ts` needs an actual mechanism for generated code to know it's running against a dev build — most likely a matching `--define` the docs dev server sets and the prod build strips, mirrored into `emit-client.ts`'s codegen as a new ambient. Decide the mechanism first (this is the actual open question — see conversation), then implement the assertion using it.

- [x] LT-064: `@try`/`@pending`/`@catch` — non-active branches must not submit (CHECKLIST §8) — reviewed ✓ (duplicate-id check only; fieldset wrap split to LT-077)
  **Review:** Approved for what it does — the duplicate-id-across-arms diagnostic (TSRX035) is correct and cheap. Agree with NOT attempting the `fieldset[disabled]` wrap inside this batch: `form-listbox.tsrx` is a real, shipping consumer of the async boundary, not a stub, and this is a genuine cross-cutting lowering change (`emit-server.ts` + `emit-client.ts`'s pending/resolve/reject state machine + likely `ir.ts`) with real risk of silently corrupting it. That said, this leaves a real, un-tracked SECURITY-adjacent gap — a named form control inside a non-active `@try`/`@catch` arm submits twice today — filed as its own task below rather than left to rot in a Check note.
  **Skill:** le-truc-dev
  **Changed:** `server/tsrx/analysis/effects.ts` (`handleTryEffects` — duplicate-`id`-across-arms check), `server/tsrx/diagnostics.ts` (TSRX035). Tests: `server/tests/tsrx/diagnostics.test.ts` (+3 tests).
  **How (what's actually done):** Only the "check or namespace duplicate `id`s across branches" RULE — a literal `id` (walking every element under each arm, not just arm roots) appearing in more than one of `@try`/`@catch`/`@pending`'s branches is now a hard error (TSRX035), since all three render into the initial HTML at once regardless of which is hidden.
  **NOT done — the actual BUG fix (auto-wrap non-active branches in `fieldset[disabled]`) was deliberately not attempted.** Reasoning: this is a real lowering change across `emit-server.ts` (inject a wrapper element around non-active arms) AND `emit-client.ts` (the async-boundary state machine would need to toggle the wrapper's `disabled` as it transitions pending→ok/error, not just toggle `hidden` as it does today) AND likely `ir.ts` (representing the wrapper). `examples/form/listbox/form-listbox.tsrx` is the corpus's ONE real user of the async `@try`/`@pending`/`@catch` boundary (its remote-fetch mode) — a working, tested feature, not a stub. Given the size of this change and that it touches the one place in the compiler where a mistake would silently corrupt a real, shipping async-rendering feature, this needs its own scoped design/implementation pass with dedicated test budget, not something to attempt inside a larger batch under time pressure. The two DOC items (disabled-fieldset `<legend>` escape; `<fieldset>` invalid inside `<tr>`/`<select>`/`<ul>`/`<dl>`) are also not yet written down anywhere — they belong in `LE_TRUC_COMPILER.md` once the wrapping mechanism actually exists, since documenting limits of a feature that isn't built yet would be premature.
  **Corpus impact:** `bun test server/tests/tsrx`: 316 pass, 0 fail (313 + 3 new). `bun run check:tsrx`: all 21 components unaffected — `form-listbox.tsrx`'s real `@try`/`@pending`/`@catch` usage has no id collisions, so TSRX035 doesn't fire on it.
  **Recommend:** scope the `fieldset[disabled]` wrapping as its own follow-up task (own LT-number), sized for a dedicated session — it needs new fixtures exercising the async boundary's pending→ok and pending→error transitions with a NAMED form control inside, asserting the control is submittable only in the active arm at each transition, before it can be trusted against `form-listbox.tsrx`'s real usage.

- [ ] LT-077: Auto-wrap non-active `@try`/`@pending`/`@catch` arms in `fieldset[disabled]` (CHECKLIST §8)
  **Skill:** le-truc-dev
  **Context:** The actual BUG LT-064 didn't fix: `hidden`/`display: none` exclude nothing from form submission — only `disabled` does — so a named form control in a non-active arm submits alongside the active one today. Needs a dedicated session, not a batch item: touches `emit-server.ts` (wrap non-active arms in a synthetic `fieldset[disabled]`, reset its `border`/`padding`/`margin`/`min-width`) AND `emit-client.ts` (the async-boundary state machine must toggle the wrapper's `disabled` on every pending→ok/error transition, not just `hidden` as it does today) AND likely `ir.ts` (representing the wrapper in IR). Write new fixtures exercising the pending→ok and pending→error transitions with a named control inside, asserting single-submission at each step, BEFORE touching `form-listbox.tsrx` — it's the corpus's one real, shipping consumer of this feature and must not regress. Document the two DOC items once the wrapper exists: descendants of a disabled fieldset's own first `<legend>` are not disabled (authors must not put controls there), and `<fieldset>` is invalid inside `<tr>`/`<select>`/`<ul>`/`<dl>` (a generic wrapper can't be used in those contexts — authors hand-craft per-control `disabled` instead).

- [x] LT-065: Branch tree-shaking preconditions (CHECKLIST §9) — reviewed ✓ (verified safe by omission; optimization split to LT-078)
  **Review:** Approved. Good instinct to actually re-derive what the four RULEs mean when NO shaking exists rather than treating "not implemented" as automatically a gap — correctly found that the compiler's current unconditional behavior already matches CHECKLIST §9's own safe fallback path, so there's no live correctness issue. Agree this is a performance optimization, not a bug, and agree it shouldn't be attempted inside this batch for the same reason as LT-064 (real risk to `form-listbox.tsrx`, the one real async-boundary consumer, without a dedicated test budget for the transition space).
  **Skill:** le-truc-dev
  **Changed:** nothing — verification only, no code change.
  **Check:** Searched the whole compiler (`grep -rn "shake" server/tsrx`) — there is no tree-shaking logic anywhere; every `@try`/`@pending`/`@catch` async boundary always emits the full client-side pending/resolve/reject task, unconditionally, today. Re-reading CHECKLIST §9's four RULEs against that: three of the four describe either the shaking OPTIMIZATION itself (shake when both preconditions hold; emit no client task when they do) or an explicit fallback (render `@pending` visible, let the client resolve, when only precondition 1 holds) — and the fourth ("never shake without meeting both") is a warning against a WORSE, unbuilt behavior, not a description of anything that exists. Since the compiler never shakes at all, its current behavior already matches the "don't shake" fallback path unconditionally — no example gets the worse behavior the RULEs warn against, because none of the shaking machinery that could produce it exists. So there is no active correctness gap here, only a missing performance optimization.

- [ ] LT-078: Implement conditional branch tree-shaking for `@try`/`@pending`/`@catch` (CHECKLIST §9)
  **Skill:** le-truc-dev
  **Context:** Performance optimization, not a bug fix (see LT-065's review — current unconditional behavior is already safe). Needs a genuinely new usage-graph analysis with no existing pass to build on: cross-reference whether an async boundary's resolved value is read anywhere OUTSIDE its own arm's markup (no effect, no exposed property elsewhere reads it) and whether the guarding promise depends solely on server-definitive args (a reactive `src` disqualifies it). When both hold, emit no client task at all — the arm becomes static markup. If only the first holds, the resolved value needs a transfer mechanism to the client that doesn't exist yet (deferred until a concrete second use case justifies building it) — until then, keep rendering `@pending` visible and let the client resolve, same as today. Same caution as LT-077: `form-listbox.tsrx` is the one real consumer of the async boundary; build fixtures around it, don't just extend theoretically.

- [x] LT-066: Component-authoring lint rules (CHECKLIST §10) — reviewed ✓
  **Review:** Approved, including the TSRX030 exemption the Check asked me to confirm. The reasoning holds: a reactive host-mirror thunk on `<textarea value={...}>` dispatches as `bindProperty` (never an attribute write), so pairing it with static text content for the pre-hydration gap is sound — the only thing wrong with that shape is the dead, ignored `value="..."` attribute left in the markup, cosmetic noise, not the CLS-causing empty-content bug the rule targets. Flagging it too would be a false positive against form-textbox.tsrx's own now-reference shape. TSRX031 is the standout fix in this whole batch — it closes a silent, zero-diagnostic correctness bug (a construct unique to one `@if` branch vanishing entirely) that could easily have shipped invisibly in a future component. Deferred items (unreferenced-alert-id, name-derived id collisions, nullable-derived-cell binding) are correctly identified as needing false-positive-risk discussion before becoming hard errors — leaving them as prose gotchas for now rather than forcing a premature heuristic is the right call.
  **Skill:** le-truc-dev
  **Changed:** `server/tsrx/diagnostics.ts` (TSRX030/031/032 codes + factories), `server/tsrx/lower-template.ts` (`lowerElement` — TSRX030), `server/tsrx/analysis/effects.ts` (`handleIfEffects` — TSRX031), `server/tsrx/compiler.ts` + `server/tsrx/infer-type.ts` (`isOptionalBinding` — TSRX032). Tests: `server/tests/tsrx/diagnostics.test.ts` (+12 tests across 3 new `describe` blocks).
  **How:** Implemented 3 of the 6 CHECKLIST §10 gotchas as compiler diagnostics, following the existing `TSRXnnn` pattern exactly: **TSRX030** — `<textarea value={…}>` (static/server-rendered form only; a reactive host-mirror thunk is exempted since `bindProperty` corrects it live and pairing it with static text content is sound — see form-textbox.tsrx). **TSRX031** — a client-construct attribute (reactive/event/class-map/style-map) present on one `@if`/`@else` branch root but not the other; generalizes the `aria-describedby`-parity gotcha into the actual underlying compiler bug: `handleIfEffects` only ever emits constructs from the first branch root carrying any construct at all (`primary`), so an attribute unique to the OTHER branch was previously dropped from the generated client with **zero diagnostic** — found live while doing LT-060 (form-textbox.tsrx), logged to NOTES.md, now resolved by this diagnostic (option (a) of the three logged there). **TSRX032** — a destructured prop default (`foo = 'x'`) paired with a non-optional type (`foo: string`, not `foo?:`), making the default unreachable for external callers; reuses `infer-type.ts`'s existing `typeAnnotationForBinding` traversal shape for a new `isOptionalBinding`.
  **Deliberately deferred (not implemented as diagnostics):** the unreferenced-alert-`id` gotcha, `name`-derived id collisions across instances, and binding to a nullable derived cell instead of the exposed property — all three need a soundness heuristic with real false-positive risk (e.g. the derived-cell one doesn't even apply to form-textbox.tsrx itself, see LT-060's handoff) that wants design discussion before landing as a hard compiler error, unlike the other three which are structurally unambiguous. NOTES.md's option (c) (broaden `handleIfEffects` to union constructs across ALL branches, instead of just diagnosing the asymmetry) is also deferred — TSRX031 diagnoses the bug rather than fixing the underlying single-primary-root limitation; worth revisiting under LT-071 if the diagnostic proves to fire often in practice.
  **Corpus impact:** `bun test server/tests/tsrx`: 300 pass, 0 fail (291 baseline + 12 new diagnostics tests across 3 `describe` blocks). `bun test src/tests`: 426 pass, 0 fail. `bun run check:tsrx`: whole corpus still compiles clean with the new diagnostics active (pre-existing unrelated form-radiogroup TS2345 unaffected).
  **Check:** Confirm TSRX030's reactive-thunk exemption is the right call rather than flagging it too (it does leave a harmless invalid attribute in the SSR'd markup, just not the CLS-causing empty-content failure mode the rule targets) — the alternative is a genuine lowering fix (NOTES option (b)) that routes textarea `value` through text-content emission automatically, making the exemption unnecessary; not attempted here as it touches emit-server.ts's attribute-vs-child dispatch more invasively than a diagnostic.

- [x] LT-067: Re-run the agent evals after the grammar fixes; point codegen at `https://tsrx.dev/llms.txt` — reviewed ✓ (docs half only; eval re-run is a user action, not a TODO task)
  **Review:** Approved for what a compiler-dev task can actually do. Correct call not to fabricate an eval run or overstate the corpus-compiles-clean evidence as a substitute for it — those are genuinely different claims (guardrails exist vs. agent struggle measurably dropped). Re-running the eval isn't compiler dev work, so it doesn't belong in TODO.md as a follow-up task; it's a manual action for whoever wants the actual before/after comparison — noted for the user in the conversation summary instead.
  **Skill:** le-truc-dev
  **Changed:** `server/tsrx/LE_TRUC_COMPILER.md` §1 — new paragraph pointing an agent authoring/migrating `.tsrx` at https://tsrx.dev/llms.txt over general JSX/React training, naming the two host-specific gaps not covered by the upstream docs (the `@tsrx/core` 0.1.60 pin lagging the docs; `&{}`/`&[]` lazy destructuring retired outright, LT-052).
  **Check:** The "point codegen at llms.txt" half is done as a documentation change — there's no in-repo agent-prompt/codegen-config file to redirect (TSRX authoring guidance doesn't yet have its own `.agents/skills` entry; `LE_TRUC_COMPILER.md` is the closest authoritative home). The "re-run the agent evals" half is **not done**: this repo has no automated eval harness (searched for `eval`/`evals` scripts — none exist; the original evals were a manual/human-observed agent-migration exercise, which is how LT-037/LT-038/etc. in NOTES.md's history were originally found). Re-running it means giving a fresh agent session a real migration task and observing whether `&{}`/`ref={}` confusion recurs post-LT-052/055 — that needs a live agent session outside this one, not something this task can execute standalone. Indirect corroborating evidence in the meantime: the full 21-component corpus (`bun run check:tsrx`) compiles clean with zero `&{}`/`ref={}`-related diagnostics, and the near-miss guards (TSRX018/020 for `&{}`, TSRX021/022/024 for React JSX idioms, `REACT_ATTR_RENAMES` for `className`/`htmlFor`) are all exercised and passing in `diagnostics.test.ts` — but that's evidence the guardrails work, not a direct measurement of agent struggle reduction. Recommend the user run one real fresh-agent migration task (e.g. hand a coding agent a new, never-migrated component and the reconciled CHECKLIST.md) to get the actual comparison the checklist item asks for.

- [x] LT-068: Document the Le Truc TSRX host profile (CHECKLIST §1, §12) — done ✓
  **Skill:** tech-writer
  **Changed:** New root-level `TSRX-HOST-PROFILE.md` — states the five host-owned decisions (unscoped tag-name-scoped styles, `truc:pass`, `first()`-based refs, no lazy destructuring, legal statements-before-output) as a standalone, citable doc for upstream engagement; cross-linked from `AGENTS.md`, `ARCHITECTURE.md`, and a new `.tsrx` callout in `docs-src/pages/styling.md`. CSS-scoping conformance stays documentation-only (no lint/build check) — see NOTES.md-adjacent deliberation with the user: CSS is emitted verbatim (no selector parsing in the compiler today), legitimate exceptions (resets, `@keyframes`, `:root`) make a naive check false-positive-prone, and all current examples already conform. Revisit only if real drift shows up in practice.

### Component-shape coverage — no example stays hand-written (NOTES triage, 2026-08-26)

Policy decision: every component in `examples/` must be expressible in `.tsrx`. Where a component doesn't fit, the compiler is what changes — not the component, and not the corpus's coverage. "Leave it hand-written permanently" is ruled out as an outcome.

- [ ] LT-069: Widen the client-setup free-name gate to element locals and effect helpers (resolves NOTES LT-039)
  **Skill:** le-truc-dev
  **Context:** Depends on LT-055 (`first()`). `compiler.ts:463`'s client-only setup-statement gate accepts JS globals, context members, signals, and `expose()` ambients — but **not** element locals, so any bare statement touching a queried element fails TSRX005. That single restriction is what blocks the imperative-effect shapes: `on(host, 'keydown', …)`, `new ResizeObserver(…)` with a disconnect cleanup, `canvas.getContext('2d')` redrawn on signal change, and `pointerdown`→`pointermove`/`pointerup` capture sequences that attach and detach listeners for the duration of one drag. Widen the gate to element locals bound by `first()`, and admit the library's own effect helpers (`watch`, `on`, and the effect-with-cleanup form) as recognized ambients. No new construct is needed: le-truc's existing idiom for "run once at connect, clean up on unmount" is an effect returning a cleanup function, and the `@{}` setup block already *is* the factory body — making it accept the same names the hand-written factory accepts is both the smaller change and the easier one to explain. Rejected alternative: a bespoke `onConnect={() => { …; return cleanup }}` pseudo-event, which invents a second effect concept alongside the one authors already know. Statements referencing element locals are client-only by construction and must never be emitted into the server module.
  **Validating cases (three, not one):** `form-colorgraph` (ResizeObserver + canvas + pointer capture), `form-radiogroup` (whose doc comment at `form-radiogroup.tsrx:10-11` records the `on(host, …)` → `onKeydown` JSX-attribute workaround), and `form-inplace-edit` (same restriction). The NOTES entry's claim of "no concrete second use case yet" was inaccurate.

- [ ] LT-070: Migrate `form-colorgraph` to `.tsrx`; retire the `on(host, …)` workarounds (acceptance case for LT-069)
  **Skill:** le-truc-dev
  **Context:** Depends on LT-069. The composition and per-step pieces are already expressible — three `<FormSpinbutton>` instances via `pass={{ … }}`, eight `<li>` steps as plain non-loop `style={() => stepStyle(…)}` reactive attributes (no `@for`, so LT-037's loop machinery isn't involved). LT-069 unblocks the rest. While here, drop the `onKeydown`/`onKeyup` JSX-attribute workarounds in `form-radiogroup.tsrx` and `form-inplace-edit.tsrx` in favour of direct `on(host, …)` setup statements, and delete the deviation note in `form-radiogroup.tsrx`'s doc comment. This is the acceptance gate for "the compiler fits every example component."

- [ ] LT-071: Revisit the deferred component-shape questions after the conformance refactoring (NOTES LT-033, LT-035, LT-036)
  **Skill:** le-truc-dev
  **Context:** Gated on LT-051…LT-060 and LT-069 landing. Three NOTES entries are deferred, not closed, and each is annotated with this gate. Re-evaluate them against the refactored compiler *before* designing anything new — several of these walls may move or disappear once `&{}`, the lift rule, `first()`, and the widened setup gate are in: **LT-036** (signals rendered only through a style/class map still hit TSRX004 with a misleading "never rendered" message — at minimum make the diagnostic distinguish "never rendered" from "rendered but initializer not client-portable"); **LT-035** (`card-mediaqueries` reshaped from light-DOM enhancer to template owner, leaving `card-mediaqueries.html` and the `'multiple components receive same context values'` e2e spec stale); **LT-033** (`card-blogmeta` reformats arbitrary author-supplied light DOM at connect and owns no template — the hardest of the three, and the one most likely to need a genuine `{children}`-adjacent addressing mechanism). Per the policy above, all three must end in a migration, so the question for each is *which compiler capability* is missing, not whether to migrate.

## 2.5.1 form-reset release branch (shipped 2026-08-27)

Commit `761700fe` mixed a library bug fix (`src/extensions/form.ts`, LT-056/LT-057) with v3-only TSRX compiler work (LT-058/LT-059, `server/tsrx/*`). LT-072–074 ported the library side onto `bugfix/form-reset-baseline` (off `next`), merged via PR #119 (`ed1f7ab9`), then `next` merged into `main` via PR #120 (`48b63ed2`) and was tagged `v2.5.1` on 2026-08-27 — confirmed `34c0f37c` (the combined form-fix + docs + changelog commit) is an ancestor of the `v2.5.1` tag. 2.5.1 is live; `next`/`package.json` are already at `2.6.0` for the bind-helpers/TSRX-preview cycle.

- [x] LT-072: Port the form reset fix onto a bugfix branch off `next` for 2.5.1 — reviewed ✓
  **Skill:** le-truc-dev
  **Changed:** `src/extensions/form.ts`, `src/tests/form.test.ts`, `index.ts`, `src/component.ts`, `types/index.d.ts`, `types/src/component.d.ts` (commit `34c0f37c`, PR #119).
  **Review:** Approved. Ported without the v3-only TSRX diagnostics or the `.tsrx` example edit, as scoped; landed on `next` and rode the branch to `v2.5.1`.

- [x] LT-073: Form-docs consistency pass on the release branch — reviewed ✓
  **Skill:** tech-writer
  **Changed:** `docs-src/pages/components.md`, `.agents/skills/le-truc/references/component-model.md`, `.agents/skills/le-truc-dev/references/non-obvious.md` (commit `34c0f37c`).
  **Review:** Approved. Verified the other listed candidates (`README.md`, `ARCHITECTURE.md`, `AGENTS.md`, `source-map.md`, the three dated blog posts) don't reference reset timing/`defaultChecked`/`formResetCallback` semantics — grepped clean, correctly left untouched.

- [x] LT-074: Fold the form-reset fix into the unreleased 2.5.1 changelog entry — reviewed ✓
  **Skill:** changelog-keeper
  **Changed:** `CHANGELOG.md`'s `## 2.5.1` section (commit `34c0f37c`) — Fixed/Added/Changed entries as scoped, referencing `761700fe`.
  **Review:** Approved. Shipped as part of the `v2.5.1` tag; no separate version bump needed.

## Authored-import policy: real exports explicit, FactoryContext ambient (ADR 0024 sub-design 16, re-amended 2026-08-27)

The 2026-08-27 amendment made FactoryContext helpers explicit imports (LT-079, implemented and committed); reversed the same day on two findings: (1) the highlighting rationale failed empirically — Zed's TSRX language service cannot resolve even explicit imports, so the editor-experience fix belongs upstream, out of scope for this library; (2) the context names are not package exports and have no authored binding site (the compiler generates the factory signature), making the import line a false declaration. Settled policy (sub-design 16): real package exports referenced from `@{ }` setup (`createCell`, `deriveCell`, parsers, `defineMethod`, `relayValidity`, …) require explicit authored imports; the FactoryContext vocabulary authors actually write (`expose`, `first`, `all`, `host`, `internals`, `requestContext`, `provideContexts`) is ambient via `globals.d.ts`; `watch`/`on`/`pass`/`each` and every `bind*` are never authored at all — template syntax lowers to them in generated code (corpus-verified: zero authored `on(`/`watch(`/`pass(`/`each(` calls across the 21 components).

- [x] LT-079: Compiler — require explicit `import { ... } from '@zeix/le-truc'` for FactoryContext helpers in `.tsrx` sources (ADR 0024 sub-design 4) — reviewed ✓
  **Skill:** le-truc-dev
  **Changed:** `server/tsrx/ast-utils.ts` (new `AMBIENT_IMPORT_NAMES`), `server/tsrx/imports.ts` (new `parseFactoryImports`; `parsePlainImports` now excludes `'@zeix/le-truc'`), `server/tsrx/diagnostics.ts` (TSRX036 error / TSRX037 warning), `server/tsrx/compiler.ts` (new `reportFactoryImportMismatch` whole-module scan, wired into `compileSource`). Migrated all 21 real `.tsrx` example components to add the required imports.
  **How:** `reportFactoryImportMismatch` walks the whole parsed module once (same shape as `reportLazyPatterns`), scope-aware like `freeIdentifiers` (skips param/declarator bindings, non-computed property keys/member properties) but also skips `ImportDeclaration` nodes entirely so an import specifier doesn't count as its own "usage" — then diffs the resulting usage set against `parseFactoryImports`'s explicit-import set. `internals` (a `CONTEXT_NAMES` member of the same shape as `host`) was NOT named in the ADR amendment's list of 9 and was deliberately left out of `AMBIENT_IMPORT_NAMES` rather than silently widened — flagged for the architect, not resolved here.
  **Check:** (1) Whether `internals` should join the 9 required-import names for consistency with `host` — currently stays purely ambient. (2) `bun run scripts/check-tsrx.ts` — whole corpus compiles clean (zero TSRX036/037; one pre-existing unrelated `tsc` error on `form-radiogroup.tsrx` confirmed present before this change too, via `git stash`). (3) `bun test server/tests/tsrx` — 90 of ~316 tests fail: real-corpus/golden tests pass, but ~90 tests across 24 files have inline source fixtures predating this rule that were NOT migrated (see NOTES.md LT-079 entry) — deliberately scoped out and handed off as LT-081, not silently left broken.
  **Review (2026-08-27):** Approved. Mechanism verified sound end-to-end: the authored `@zeix/le-truc` import is purely declarative (claimed by `parseFactoryImports`, excluded from `parsePlainImports`, never re-emitted), the generated client synthesizes its own imports and destructures context members — so requiring `host` in the authored line is a pure authoring-convention decision, correctly implemented. Diagnostics are bidirectional (036 missing / 037 unused) with actionable fix-its. Splitting the fixture migration into LT-081 rather than blocking review: accepted — the product surface (real corpus) is migrated and verified, the 90 failures are contained and documented. Two follow-ups from review: **LT-082** — the usage scan's scope tracking diverges from `freeIdentifiers` (declarator/function-declaration names never bound, no sequential statement scoping), so a local shadowing one of the required names (`const on = createCell(true)` … `on.get()`) false-positives TSRX036; live case in `class-map.test.ts` fixtures. **LT-083** — `internals` joins the required names; it is a base `FactoryContext` member like `host` (`src/component.ts`), used as a real value in the corpus (`form-spinbutton.tsrx`'s `relayValidity(internals, input)`), and invisible to syntax highlighters — the exact problem the amendment reverses; sub-design 15 already treats `host`/`internals` as a pair, so the amendment's 9-name list omitting it was an oversight, not a boundary. Both must land before LT-081. **Superseded (same day):** the 2026-08-27 re-amendment (ADR 0024 sub-design 16) reverses the context-helper import requirement — the highlighting rationale failed empirically (Zed's TSRX language service can't resolve explicit imports either) and the context names aren't package exports, making the authored import line a false declaration. LT-082 implements the reversal. The scope-tracking defect noted above survives inside LT-082: TSRX036 persists, repurposed for real package exports, and needs the same scope-aware usage scan.

- [x] LT-081: Migrate `server/tests/tsrx/*.test.ts` inline source fixtures to the real-export import rule (sub-design 16, LT-082 follow-up) — run AFTER LT-082 — done ✓
  **Skill:** le-truc-dev
  **Changed:** 18 test files: 53 inline full-source fixtures gained an appended exact-import line (imports hoist, so every existing statement keeps its line number and source offset — diagnostics and span assertions needed no adjustment); helper-composed fixtures got the import in their wrapper template (`reactivity.test.ts` `childOf`, `compiler.test.ts` `configSource`, `features.test.ts` `asyncComponent`, `dirty-flag-harvest.test.ts` `harvestFrom`, `diagnostics.test.ts` `listSource`, `pass.test.ts`/`plain-imports.test.ts`/`lazy-watch-source.test.ts`/`server.golden.test.ts` fixtures the scanner's shape guard couldn't see). `le-truc-imports.test.ts` untouched — its fixtures deliberately exercise TSRX036.
  **How:** The bulk pass used a TypeScript-AST walk over each test file for non-substituted template literals (a hand-rolled backtick scanner mis-pairs on comment backticks — two aborted attempts corrupted files and were restored from the staged baseline), compiled each fixture in-process, collected the TSRX036 names, and appended one exact import line per fixture. Stragglers (fixtures starting with type/plain-import declarations, interpolated helper factories, escaped-backtick interiors) were patched individually.
  **Check:** `bun test server/tests`: **1062 pass, 0 fail** — fully green for the first time since LT-079 (was 60 fail; the +8 count over the old 1054 baseline is LT-082's new `le-truc-imports.test.ts`). `bun run scripts/check-tsrx.ts` exits 0 with zero diagnostics — the long-standing form-radiogroup tsc error also cleared once the corpus re-import regenerated its modules. `bun run build:cem` verified (64 declarations, no duplicate tags); `bun test src/tests` 426/426.

- [x] LT-080: Fix `TSRX-HOST-PROFILE.md` — correct host-profile roster, document the explicit-import requirement (LT-068 follow-up) — done ✓
  **Skill:** tech-writer
  **Changed:** `TSRX-HOST-PROFILE.md` — corrected the intro (six named host profiles, not just Ripple; Le Truc's distinction is server-once/light-DOM-by-default, not profile count), narrowed the styles section's Ripple comparison to the specific hash-scoping contrast, added a new "FactoryContext helpers require an explicit import" section (flagged not-yet-implemented, pointing to LT-079 and ADR 0024 sub-design 4), and reworded "Open questions for upstream" to drop the "only Ripple" claim.

- [x] LT-082: Implement the sub-design 16 authored-import policy — real-export imports required, FactoryContext ambient again (LT-079 reversal) — must land BEFORE LT-081 — done, pending review ⏳
  **Skill:** le-truc-dev
  **Changed:** `server/tsrx/ast-utils.ts` (`AMBIENT_IMPORT_NAMES` → `REAL_EXPORT_NAMES`, the package barrel's value exports), `server/tsrx/imports.ts` (`parseFactoryImports` → statement-level `parseLeTrucImports`; new `placeLeTrucImports` with the `RUNTIME_HARNESS_EXPORTS` per-name server filter; `serverUsageNames` extracted and shared with `placePlainImports`), `server/tsrx/compiler.ts` (`reportFactoryImportMismatch` → scope-aware `reportLeTrucImportMismatch` incl. `JSXCodeBlock` sequential scoping; placement merge carrying `clientLeTrucNames`), `server/tsrx/diagnostics.ts` (TSRX036/037 repurposed: missing real-export import / context name inside an import line), `server/tsrx/ir.ts` (`imports.clientLeTrucNames`), `server/tsrx/emit-client.ts` (synthesized line subtracts authored-provided names), `server/tsrx/globals.d.ts` (ambient contract shrunk to the FactoryContext vocabulary; `first`/`all`/`on`/`pass`/`watch` declarations added), `server/tests/tsrx/globals.test.ts` (parity lists + probe rewritten for the split), `server/tests/tsrx/le-truc-imports.test.ts` (new, 8 tests), `server/tests/tsrx/client.golden.test.ts` + 5 snapshots (two-line import shape). Corpus: 17 context-helper import lines stripped, real-export imports added across 17 files, the `'../../../index'` warts (`relayValidity`, `DuplicateKeyError`) converted to `'@zeix/le-truc'`.
  **How:** Authored `'@zeix/le-truc'` imports stay out of the plain-import verbatim path — placement is per name: the client re-emits the line's used names, the server emits only names the runtime harness cannot provide (the harness keeps its plain-value shims for signal constructors/parsers/`defineMethod` — no duplicate bindings on either side). TSRX036 fires via a scope-aware whole-module scan (declarator/function bindings + sequential `Program`/`BlockStatement`/`JSXCodeBlock` scoping — the LT-079 review defect class; a local `const` shadowing an export never fires). TSRX037 rejects context names inside the import line and `placeLeTrucImports` never re-emits them.
  **Check:** (1) The `JSXCodeBlock` case in the scan's sequential-scoping group — `freeIdentifiers` never needed it (it analyzes per-statement), the whole-AST walk does; regression-tested via the `match` shadow test. (2) `bun run scripts/check-tsrx.ts`: zero TSRX036/037/014 across all 21 components; only the known pre-existing form-radiogroup tsc error. (3) `bun test server/tests`: 60 failures remain, all TSRX036 on fixtures using real exports (`createCell`/`deriveCell`/parsers) without imports — LT-079's 90 context-name failures are gone; the 60 are LT-081's scope. (4) `bun run build:cem` passes (64 declarations, no duplicate tags). (5) `src/tests` 426/426; globals parity + probe pass. (6) `REAL_EXPORT_NAMES` and `RUNTIME_HARNESS_EXPORTS` are hand-maintained duplications of the barrel and the runtime harness — a barrel growth needs a list update or newly authored exports fire TSRX036 until listed.

- [x] LT-084: Rewrite `TSRX-HOST-PROFILE.md`'s explicit-import section for the sub-design 16 policy (LT-080 follow-up) — done ✓
  **Skill:** tech-writer
  **Changed:** `TSRX-HOST-PROFILE.md` — the section retitled "Imports: real exports explicit, FactoryContext vocabulary ambient": real exports imported explicitly, the ambient-7 FactoryContext vocabulary via `globals.d.ts`, effect machinery (`watch`/`on`/`pass`/`each`/`bind*`) never authored with the template-syntax lowerings spelled out, TSRX036/037 named, and the same-day reversal recorded with the Zed language-service limitation marked upstream/out of scope. Plus one factual fix in `server/tsrx/LE_TRUC_COMPILER.md` ("the `.tsrx` source itself imports nothing" → the sub-design 16 split), which the old text directly contradicted.
