# TODO

Proof-of-concept milestone for [ADR 0026](adr/0026-aria-reflection-via-elementinternals-and-bindaria.md) (🔄 Proposed — stays Proposed until this PoC proves the ship shape). Scope rule: prove each planned channel against the **hardest** real case only; features of similar or simpler shape, and the migration of the example components, are deferred until after the decision gate (LT-006). PoC code lives under `test/poc/` (not `examples/` — that is the published demo surface) with its own Playwright project and a served PoC page; PoC components are throwaway and must not enter the public API.

- [x] LT-001: Computed-accessibility ground-truth test harness — done ✓
  **Skill:** le-truc-dev
  **Changed:** `test/poc/` (new): `serve.ts` (Bun server on :3100, on-demand `Bun.build` bundling with `DEV_MODE="true"`), `pages/basic.html` + `poc-basic.ts` (raw-custom-element probe: internals-only / attribute-only / both), `fixtures/aria.ts` (CDP `Accessibility.getFullAXTree` ground-truth reader, Playwright ariaSnapshot tier, axe-core 4.13 in-page runner over the declaration registry), `poc.spec.ts` (pinned observations), `README.md` (findings matrix); `playwright.config.ts` (poc-chromium/firefox/webkit projects on their own testDir + second webServer); `package.json` (axe-core devDependency).
  **How:** `page.goto('/basic')` probes distinguish the three semantic channels in every configured engine; Chromium-only CDP tests assert the engine's computed tree, all-engine tests assert the tooling and axe tiers. Full suite green: 402 unit, 12 PoC, 771 examples.
  **Check:** Harness distinguishes attribute-set from internals-set semantics in all engines ✓ — findings in `test/poc/README.md`.
  **Correction (2026-08-31, during LT-002):** the original baseline wrote `internals.ariaValuenow` (lowercase `n`) instead of the real IDL attribute `ariaValueNow` — a silent no-op data-property assignment, not a setter call. This produced a false "internals-set `aria-valuenow` never reaches the AX tree" finding. Fixed in `poc-basic.ts`/`poc.spec.ts`/`README.md`: internals-set `role`, `aria-label`, AND `aria-valuenow` all reach the Chromium AX tree, same as their attribute-set equivalents. Playwright's tooling tier still ignores internals on all engines; host-attribute override + reassert semantics confirmed for both name and value; axe 4.13 produces zero internals-related false positives via the registry.
  **Review:** Approved (pre-correction) and stands post-correction — the harness itself was sound; only one probe property was mis-cased. No public API touched.

- [x] LT-002: Host default-semantics channel PoC — hardest case: colorgraph-style hue slider — done ✓
  **Skill:** le-truc-dev
  **Changed:** `test/poc/`: `poc-hue-slider.ts` (new, raw custom element, `internals.role='slider'` + reactive `ariaValueNow`/`ariaValueText` via `throttle()`), `poc-stale-expanded.ts` (new, no-mixing wrinkle probe), `pages/hue-slider.html` (new), `hue-slider.spec.ts` (new, pinned observations), `README.md` (LT-002 findings + LT-001 correction); `serve.ts` (fixed 2 pre-existing TS2345 regex-capture-group errors, unrelated to LT-002 but blocking `tsc --noEmit`).
  **How:** `setHue(deg)` is the deterministic test entry point (no simulated pointer events); Chromium-only CDP tests assert the computed tree, all-engine test asserts throttle dedup via a `flushCount` counter.
  **Check:** Computed tree shows slider role + live `valuenow`/`valuetext` in Chromium ✓ (WebKit ground truth remains unobservable via Playwright — standing limitation, documented in LT-001). Override/restore behaviors match ADR 0026's claims ✓. No glitching under high-frequency updates: 50 synchronous `setHue()` calls collapse to exactly 1 throttled flush ✓. No-mixing wrinkle confirmed real (stale `aria-expanded` permanently shadows internals) with a working mitigation (drop the stale attribute at connect, before first reflecting via internals) — open question for LT-006: should this become part of `bindAria()`'s contract or stay an author responsibility?

- [x] LT-003: Element-reference channel PoC — hardest case: combobox pattern — done ✓
  **Skill:** le-truc-dev
  **Changed:** `test/poc/`: `poc-combobox.ts` (new, real `defineComponent` — not a raw custom element, since `all()`'s reactive Cell is a library concern), `poc-late-ref.ts` (new, `:not(:defined)` edge-case probe), `pages/combobox.html` (new), `combobox.spec.ts` (new, pinned observations), `fixtures/aria.ts` (extended: `propValue()` reads CDP `relatedNodes` for element-reference properties, which carry no plain string value; `AriaNodeSnapshot` gained a `description` field for role-dependent description exposure), `README.md` (LT-003 findings).
  **How:** `poc-combobox.ts` wires `ariaControlsElements`/`ariaDescribedByElements`/`ariaLabelledByElements`/`ariaErrorMessageElements` (static) and `ariaActiveDescendantElement` (reactive, retargeted via ArrowUp/ArrowDown against an `all('[role="option"]')` Cell). Chromium-only CDP tests assert the computed tree; the attribute-clearing side-effect test runs on all engines.
  **Check:** Computed tree reflects each relationship in Chromium ✓ (WebKit/Firefox ground truth remains unobservable via Playwright's CDP access — standing LT-001 limitation). `:not(:defined)` targets confirmed immune to the `first()`/`all()` `DEPENDENCY_TIMEOUT` mechanism (`src/helpers/dom.ts`) — an element reference only needs the target's node identity, nothing to wait for. Removed/re-added options under `all()`'s `MutationObserver` retarget correctly ✓. Attribute-clearing side effect confirmed, with a nuance: the attribute is emptied (`''`), not removed. Two fixture-level false negatives caught and fixed before being pinned: (1) element-reference properties report via CDP's `relatedNodes`, not the flat string `computedAriaTree()` originally read — same failure shape as LT-001's `ariaValuenow` typo; (2) `errormessage` only appears once `aria-invalid` is also set. One real bug in the probe itself, not a platform finding: `watch()` wraps its handler in `untrack()`, so reading `all()`'s Cell only inside the handler body (rather than passing it as a `watch()` source) left the option list frozen at its first snapshot — fixed by `watch([activeIndex, options], ...)`; flagged in README.md as a trap for LT-004's `bindAria()` prototype too.

- [x] LT-004: `bindAria()` contract prototype — done ✓
  **Skill:** le-truc-dev
  **Changed:** `test/poc/`: `poc-bind-aria.ts` (new — `bindAria()` prototype, mirrors `src/bindings.ts`'s map-form/`SingleMatchHandlers`/DEV_MODE debug-attribution shape from ADR 0023), `bind-aria.test.ts` (new, bun:test — every mapping row, null-target no-op, debug attribution, `@ts-expect-error` pins), `poc-hue-slider.ts` (LT-002's reactive `ariaValueNow`/`ariaValueText` writes rewired through `bindAria()`, invoked imperatively — no signal graph in this raw custom element), `poc-combobox.ts` (LT-003's `ariaActiveDescendantElement` watch rewired through `bindAria()` + a tracked thunk `watch()` source, replacing the array-source form), `README.md` (LT-004 findings). Merged `next` into this branch first (brings in ADR 0023's now-accepted map-form `bind*` overloads, targets v2.6).
  **How:** `bindAria(target: ARIAMixin | null | undefined, name: keyof ARIAMixin & string)` returns `SingleMatchHandlers<AriaValue>` (`AriaValue = boolean | number | string | Element | readonly Element[]`, deliberately excluding `null`/`undefined` from the generic parameter — required by cause-effect's `SingleMatchHandlers<T extends {}>` constraint — while still guarding `value == null` at runtime, the same "typed optimistically, guarded defensively" split `bindAttribute`'s/`bindStyle`'s map-form `ok(map)` already has). `ok()` coerces boolean→`'true'`/`'false'`, number→decimal string, passes string/`Element`/`Element[]` through, and null-clears; `nil` also null-clears; a nullish target makes every handler a no-op.
  **Check:** Every mapping row exercised by `bind-aria.test.ts` (12 tests, bun:test, no browser) ✓. `@ts-expect-error` pins confirmed load-bearing by temporarily removing one and observing the expected `TS2345` before restoring ✓. Ergonomics verified against the thunk source form (combobox activedescendant — the only single-source form compatible with a `SingleMatchHandlers`-returning helper; the array-source form cannot carry one, a finding in its own right) and imperative standalone invocation (hue-slider, no `watch()` at all); prop-key and bare-Signal source forms were not separately exercised beyond the bun-test level, since no PoC component's ARIA property is driven by either — noted here rather than fabricating a probe for it. Full regression pass green: 422 unit (`bun test src/tests`), 12 bind-aria unit, 0 `tsc --noEmit` errors, biome clean, 26 poc Playwright (stable across 3 repeated runs), 771 examples Playwright.
  **Review:** Approved at the LT-006 gate — reviewed ✓. The signature survives the hard cases; refinements folded into ADR 0026 rather than back into the PoC: (a) the `null`/`undefined` mapping row is documented as a runtime guard under a `T extends {}`-optimistic type, (b) a map form is added for ADR 0023 parity (not prototyped here — the implementation task must prototype it), (c) stale-attribute removal joins the helper's contract (§1). The LT-004 finding that `watch()`'s array-source form cannot carry `SingleMatchHandlers` is deliberately *not* in the ADR — it is a `watch()` question, filed separately.

- [x] LT-005: Element-internals-declaration registration + axe-core gate PoC — done ✓
  **Skill:** le-truc-dev
  **Changed:** `test/poc/`: `poc-truc-registration.ts` (new — prototypes the `Truc` constructor's proposed registration addition: unconditional `globalThis._elementInternals ??= new WeakMap(); registry.set(this, internals)` after `attachInternals()`), `pages/registration.html` (new), `registration.spec.ts` (new, pinned observations), `README.md` (LT-005 findings).
  **How:** `<poc-reg-button-trap>` (`internals.role='button'`, no native button, plus an invalid `aria-checked` content attribute) vs. `<poc-unreg-button-trap>` (byte-for-byte identical, deliberately never registered) on the same page, same `axe.run()` pass — the actual "silent-pass → flagged" comparison. `<poc-reg-button-ok>` is the negative control (registered, no invalid attribute). The connect/disconnect/reconnect identity test moves one instance in and out of the DOM twice via JS.
  **Check:** axe report confirms the trap flips from silent-pass (unregistered) to flagged with `aria-allowed-attr` (registered) ✓; the control stays clean ✓ (registration alone doesn't blanket-flag). One WeakMap entry per instance confirmed: `attachCount === 1` and identical `ElementInternals` object across two remove/re-append cycles ✓ — not from cleanup logic, but because `attachInternals()` lives in the constructor, which only ever runs once per instance. **Finding for LT-006**: of axe 4.13's four named ElementInternals-aware rules (`aria-allowed-attr`, `aria-prohibited-attr`, `aria-required-parent`/`children`, list/listitem), only `aria-allowed-attr`/`aria-prohibited-attr` can actually see an internals-only role in practice — `aria-required-parent`/`aria-required-children` declare `selector: '[role]'` (an attribute-presence CSS selector, confirmed by reading `node_modules/axe-core/axe.js`'s compiled rule metadata) that excludes internals-only elements before role computation ever runs. The originally-planned `list`/`listitem` trap (from that same subset) was tried first and stayed silent regardless of registration for exactly this reason — dropped in favor of the verified `aria-allowed-attr` button case. Full regression pass green: 422 unit, 12 bind-aria unit, 0 `tsc --noEmit` errors, biome clean, 34 poc Playwright (stable across 3 repeated runs), 771 examples Playwright.

- [x] LT-006: PoC findings → ADR 0026 decision gate — done ✓
  **Skill:** architect
  **Decision: GO**, with a narrowed claim about tooling coverage. ADR 0026 moves 🔄 Proposed → ✅ Accepted (2026-08-31), targeting v3.0.
  **Changed:** `adr/0026-*.md` (Status; §1 stale-attribute rule; §2 amendments (a)–(b); §3 axe-reach qualification; §5 testability bound; new **PoC validation** section with per-channel verdicts; corrected/added Consequences; one new rejected alternative; Related), `adr/0016-*.md` (amendment banner on "ARIA reflection: available but not promoted"), `.agents/skills/adr-keeper/references/adr-index.md` (status), `TODO.md` (LT-004 review resolved; LT-007–LT-011 queued).
  **Verdicts:** host default semantics ✅ viable; consumer override/reassert ✅ viable; element references ✅ viable with caveats (`aria-invalid` gate, attribute emptied-not-removed); `bindAria()` contract ✅ viable with a refined signature; registry + axe gate ⚠️ viable but with a materially narrower reach than axe's docs imply; Firefox/WebKit engine trees ❌ unverifiable in CI.
  **Falsified claim:** ADR 0026's Consequences previously asserted the nested-element safety net was restored. It is not — `aria-required-parent`/`aria-required-children` are gated by a `[role]` *attribute* selector, so exactly the nesting case ADR 0016's advisory named stays invisible. Rewritten as a Bad/trade-off with guidance: structural/composite roles stay on the attribute channel; internals reflection is for host-level widget and state semantics.
  **Open question resolved (from LT-002):** stale-attribute removal becomes **part of `bindAria()`'s contract**, not an author responsibility. The gate first argued the opposite (the platform cannot tell a stale SSR echo from a consumer override); that was overturned in review — under ADR 0003/M3 the distinction is *temporal*, so a one-time removal at binding activation clears only the server-rendered echo and leaves post-connect overrides intact. Contract: the server-rendered attribute is the initial value; the component then owns the property reactively via internals. Scoped to `ElementInternals` targets only, and ordered after parsers. Two prerequisites, both specified in §1 and required by LT-007.
  **Out of scope, filed separately:** `watch()`'s array-source form cannot carry `SingleMatchHandlers` (LT-004 finding 2). That is a `watch()` API question, not an ARIA one — kept out of ADR 0026 entirely; a feature request goes to the team.
  **Check:** ADR accepted with evidence ✓; PoC distilled into the ADR's PoC validation table (raw findings stay in `test/poc/README.md`, referenced from Related) ✓; deferred scope enumerated as LT-007–LT-011 ✓.

- [ ] LT-007: Implement `bindAria()` in `src/bindings.ts`
  **Skill:** le-truc-dev
  **Context:** Land ADR 0026 §2. Start from `test/poc/poc-bind-aria.ts` — the prototype deliberately mirrors the real `bind*` shape, so this should be closer to a move than a redesign. Single form `bindAria(target: ARIAMixin | null | undefined, name: keyof ARIAMixin & string): SingleMatchHandlers<AriaValue>`, plus the **map form** `bindAria(target, names: readonly N[])` per ADR 0023's precedent — the map form was *not* prototyped in the PoC, so prototype it before finalising (motivating case: the hue slider's `['ariaValueNow', 'ariaValueText']` from one source). Keep the "typed optimistically, guarded defensively" split for `null`/`undefined` (§2a) and the debug-attribution branch (register `Element` targets, skip `ElementInternals`). Export from the barrel; add JSDoc with `@since 3.0`.
  **Also implements §1's stale-attribute rule** — `bindAria()` removes the shadowing content attribute once, at binding activation, for `ElementInternals` targets only. Two prerequisites, neither of which exists today: (1) a module-private `WeakMap<ElementInternals, HTMLElement>` reverse lookup in `src/internal.ts`, populated on the same `Truc` constructor line as LT-008's registry set — `internalsMap` is keyed host → internals and `ElementInternals` carries no host reference, so the helper otherwise cannot reach the element; (2) an IDL-property → attribute-name mapping that is **not** kebab-case (strip trailing `Element`/`Elements`, strip the `aria` prefix, lowercase, prefix `aria-`; `role` maps to itself). A naive kebab-case transform yields `aria-labelled-by` and silently removes nothing — test that case explicitly.
  **Check:** Every mapping-table row unit-tested (port `test/poc/bind-aria.test.ts`, including the `@ts-expect-error` pins); map form covered for present/absent/nullish entries and `nil`; attribute-name mapping tested across all four shapes (`ariaValueNow`, `ariaDescribedByElements`, `ariaActiveDescendantElement`, `role`). Playwright-tier test for the stale-attribute rule: a server-rendered `aria-expanded="false"` is adopted as initial state by the parser, removed at activation, and subsequent reflection updates reach the computed tree — while an attribute set *after* connect still overrides (both behaviors, or the rule is half-implemented). No removal for `Element` targets. `test/regression-bundle.test.ts` still under the minimal-entry ceiling and ADR 0019's tiers intact.

- [ ] LT-008: Register `ElementInternals` in the element-internals-declaration registry
  **Skill:** le-truc-dev
  **Context:** Land ADR 0026 §3. In `src/component.ts`'s `Truc` constructor, alongside the existing `internalsMap.set(this, this.attachInternals())`, also `(globalThis._elementInternals ??= new WeakMap()).set(this, internals)` when attach succeeded — unconditional, **no `DEV_MODE` gate** (audits run against production builds). Prototyped in `test/poc/poc-truc-registration.ts`; no cleanup logic is needed or wanted (the constructor runs once per instance — see LT-005 finding 5). Do not expose internals as a public host property.
  **Check:** An axe run over a component with an internals-set role and an invalid `aria-*` attribute flags `aria-allowed-attr` (the LT-005 trap, ported to the examples Playwright tier); a correct component stays clean; bundle ceiling holds.

- [ ] LT-009: Cross-engine AT verification for internals-set semantics
  **Skill:** le-truc-dev
  **Context:** The PoC's engine-level ground truth is Chromium-only — Playwright exposes no computed-tree API for Firefox or WebKit, so their support for internals-set `role`/`aria*`/`aria*Elements` is asserted from documentation, never observed (ADR 0026 Consequences). Verify manually with VoiceOver (Safari) and NVDA (Firefox) against the existing `test/poc/` pages: host role + live `valuenow`/`valuetext` on the hue slider, and the combobox's `activedescendant`/`describedby`/`labelledby` relationships. Record results in `test/poc/README.md`.
  **Check:** Each internals-set property confirmed announced or confirmed missing per engine, with the AT and version noted. **Gates LT-010** — a property that does not survive an engine must not migrate there.

- [ ] LT-010: Migrate examples where reflection is strictly better
  **Skill:** le-truc-dev
  **Context:** Land ADR 0026 §5, after LT-007–LT-009. `form-combobox`: replace hand-rolled `aria-describedby` ID plumbing with `ariaDescribedByElements`, and move `aria-expanded` to `watch(isExpanded, bindAria(textbox, 'ariaExpanded'))`. `form-colorgraph`: the hue slider's reactive `valuenow`/`valuetext`, via the map form. **Bounded by testability** (§5): a host property that moves to the internals channel loses its Playwright-visible assertion on every engine and its static nesting validation — migrate only where the reflection is strictly better *and* still assertable. Keep structural/composite roles and anything CSS or attribute selectors consume (`form-listbox`'s `[aria-selected="true"]`) on the attribute channel. §1's stale-attribute rule is handled by `bindAria()` itself (LT-007) — no per-example `removeAttribute()` calls; if one seems necessary, that is a bug report against LT-007, not a workaround to write here.
  **Check:** Existing example specs still pass or are consciously re-pointed at the CDP tier (not silently deleted); no example regresses in the AT verification from LT-009; no component writes both channels for the same property on the same element.

- [ ] LT-011: Rewrite the ARIA docs advisory around the two-channel policy
  **Skill:** tech-writer
  **Context:** `docs-src/pages/components.md` currently carries ADR 0016's "keep explicit `aria-*` attributes" advisory; replace it with ADR 0026 §1's policy table, the stale-attribute rule (as helper behavior authors get for free, *not* as a manual step — state plainly that the server-rendered attribute is the initial value and post-connect overrides still win), `bindAria()` usage including the map form, and the withholding of `ariaOwnsElements`. Where the axe-core supported-rule subset is cited, carry §3's qualification precisely: `aria-required-parent`/`aria-required-children` cannot act on an internals-only role, so structural roles keep no static safety net. The July 2026 blog post stands as a historical record and is not edited. Also check whether the Le Truc CEM plugin needs anything for `bindAria()` (expected: no — unlike `formAssociated`, this adds no class-level metadata) and record the answer either way.
  **Check:** No surviving text tells authors to avoid ARIA reflection; the axe qualification appears wherever the subset is named; CEM question answered in writing.
