# TODO

- [x] LT-001: Inlined TSRX compiler — emitter core (ADR 0023 milestone 1) — reviewed ✓
  **Skill:** le-truc-dev
  **Context:** Build the in-repo compiler module (`server/tsrx/`) on pinned `@tsrx/core` 0.1.60: parse `.tsrx`, emit server HTML render functions (dependency-provable thunk evaluation), extract verbatim tag-scoped CSS, and maintain the component registry; wire as a build effect in `server/build.ts`. Golden-test against the existing hand-written `.html`/`.css` artifacts. See [ADR 0023](adr/0023-adopt-tsrx-as-unified-component-format.md) sub-designs 2–3 and `spike/tsrx-phase0/REPORT-unified.md` §Recommendation.
  **Changed:** `server/tsrx/` (compiler, analyze, emit-server, css, runtime, registry, index) + `server/effects/tsrx.ts` wired into `build.ts`; `examples/**/*.tsrx` signal in `file-signals.ts`.
  **Review:** Approved. Emitted CSS byte-equal to the hand-written artifacts; every golden-page demo variant represented by `render(args)` goldens; reactive `@for` correctly gated as TSRX001. ADR acceptance deliberately deferred by owner — see LT-005/LT-006.

- [x] LT-002: Inlined TSRX compiler — client codegen, sanctioned subset (ADR 0023 milestone 2) — reviewed ✓
  **Skill:** le-truc-dev
  **Context:** Generate `defineComponent()` factories from the same sources: text/attribute/class bindings, event attributes, refs, server-data `@for` + `each()`, hoisted-const rebinding, harvest rules (first-by-document-order). Imports solely from `@zeix/le-truc`; golden-test generated clients against the existing hand-written trio. See ADR 0023 sub-designs 1, 4–5 and `spike/tsrx-phase0/expected/unified-lowerings.md`.
  **Changed:** `server/tsrx/analyze.ts` (addressing, harvest plans incl. membership form, rebinding) + `emit-client.ts`; snapshots in `server/tests/tsrx/snapshots/` (regenerate via `UPDATE_SNAPSHOTS=1 bun test server/tests/tsrx`).
  **Review:** Approved. Generated clients are statement-for-statement the hand-written trio's patterns and typecheck against the real `@zeix/le-truc` types (emit-then-check, the CI half of ADR 0023 sub-design 6). `module-list.tsrx` awaits milestone 3 (LT-003).

- [x] LT-005: Unified format for form components — extensions, attribute-driven props, `internals` (ADR 0023 acceptance gate) — reviewed ✓
  **Skill:** le-truc-dev
  **Context:** The sanctioned subset cannot activate `defineComponent`'s third argument — `formAssociated()` / `formAssociatedCheckbox()` (which must lead the array and widen the factory context to `FormFactoryContext`, ADR 0019) and `observedAttributes([...])` — nor use `internals`/`relayValidity`, Parser-backed attribute-driven `expose()` props, or `defineMethod()` members. Form components (`form-textbox`) cannot be authored in `.tsrx` today. Spike first, record the format decision as ADR 0023 sub-design 8, then implement in `server/tsrx/`.
  **Decision (owner, sub-design 8):** `export const config = { formAssociated: true, observedAttributes: [...] }` — chosen over a `useExtensions([...])` setup hook (a directive that never executes; hands the formAssociated-first rule back to the author) and a module-level `export const extensions = [...]` verbatim slice (viable, but wider validation surface for no gain over the closed key space).
  **Changed:** `examples/form/textbox/form-textbox.tsrx` (fixture: config, Parser-expose `value`, `defineMethod` `clear`, `value={() => host.value}` mirror, `&{'validationMessage'}` managed lazy child); `server/tsrx/` — compiler (config validation TSRX009, Parser-expose/defineMethod ambients, lazy-`&` sigil with leading whitespace), analyze (host/internals as ambient context names + `ambientContext` plan, host-prop mirror → `bindProperty` dispatch, managed lazy source), emit-server (mirror renders from the root attribute expression, managed child renders empty, ambient runtime imports), emit-client (third argument with form variant leading, `FormAssociatedElement` type import, ambient destructure, re-anchored multi-line reindent), runtime (`as*`/`defineMethod` shims); tests: 4 server goldens + CSS byte-golden, client snapshot + convergence + emit-then-check, 5 compiler units, 6 TSRX009 cases. The generated client typechecks ONLY under the widened `FormFactoryContext` overload — `watch('validationMessage', …)` and `host.setCustomValidity` exist nowhere else.
  **Open for the full form migration:** `length`-style signals need an arg→DOM-site substitution harvest rule (fixture omits `length`); setup side-effect statements (`internals?.states.add('clearable')`) are still outside the setup subset; `@if` is unsupported (input/textarea variants unrepresentable); `asClampedInteger`/`asJSON` not yet recognized parser ambients; no `formAssociatedCheckbox` fixture yet.
  **Review:** Approved (2026-08-22, with independent reviewer pass). Config validation is order-independent, `leadingDocComment`'s guard rejects the realistic traps, ambient collection has no gaps, the mirror's prop-name/attr-name equivalence holds by construction. Deferred items ticketed as LT-008. Accepted leniencies: a non-exported module-level `const config` also activates (harmless superset of the documented `export const config`); `internals` without `formAssociated` destructures fine but fails tsc — emit-then-check catches it.

- [x] LT-006: Custom Element Manifest generation for `.tsrx` components (ADR 0023 acceptance gate) — reviewed ✓
  **Skill:** le-truc-dev
  **Context:** `custom-elements.json` is generated by `cem analyze` with `@zeix/cem-plugin-le-truc` over `examples/**/*.ts` (the hand-written trio). The plugin extracts the tag from `defineComponent(...)`, the component description and `@slot`/`@fires`/`@csspart`/`@cssprop`/`@demo`/`@attribute` tags from the JSDoc above `export default`, members from the `Props` type via the TypeScript checker, attributes from Parser-call `expose()` initializers, and extension-derived members from the third argument. Generated clients break three of those inputs today: the component JSDoc is dropped (the compiler slices the function declaration without its leading doc comment), Parser-expose does not exist yet (LT-005), and no extensions array is emitted (LT-005).
  **Decision (owner, sub-design 9 — staged):** plugin over generated clients now; compiler-emitted CEM fragments replace the analyzer+plugin at v3, when the last hand-written component is deleted. Direct emission as the bridge was rejected: two extractors in parity while the format still moves, plus a merge step the analyzer serves anyway.
  **Changed:** `server/tsrx/compiler.ts` (`componentDoc` — the doc comment immediately above the component function, sliced verbatim; whitespace-only guard keeps module docs out) + `emit-client.ts` (emitted above `export default defineComponent(`); corpus `.tsrx` sources carry the hand-written component JSDocs verbatim (counter/tabgroup byte-equal inputs, form-textbox with `@demo`); `custom-elements-manifest.config.mjs` — globs read `server/generated/tsrx/*.client.ts`, the trio's hand-written `.ts` excluded, and the module restriction now compares resolved paths (fixes a latent bug that silently dropped root files nothing imports — `examples/main.ts`'s six declarations vanished from every previous manifest); `scripts/build-tsrx.ts` + `build:cem` chain (compile before analyze); `scripts/verify-cem.ts` fails when a migrated tag is missing; `server/tests/tsrx/cem.golden.test.ts` — shadow-manifest equivalence: counter/tabgroup byte-equal, form-textbox structural (Parser-exposed `value` attribute with `'""'` default + formAssociated member set from the third argument).
  **Note:** `custom-elements.json` is a build artifact (untracked); fresh `bun run build:cem` → 64 declarations (58 prior − 3 trio + 3 generated + 6 recovered from main.ts).
  **Review:** Approved after one fix applied during review (2026-08-22, independent reviewer pass): the shadow-manifest test spawned `cem analyze` with the CLI's default `packagejson: true`, rewriting the root package.json `customElements` pointer to the gitignored shadow outdir on every run — fixed with `packagejson: false` in the shadow config, timeouts added to all three tests, and the corrupted pointer restored. Remaining follow-up ticketed as LT-009 (duplicate-tag guard).

- [x] LT-003: Reactive lists — `@for` over a `List` → template extraction + slot holes + `reconcile()` (ADR 0023 milestone 3) — reviewed ✓
  **Context:** The module-list lowering in `spike/tsrx-phase0/expected/unified-lowerings.md` §3 is the spec: server renders initial keyed items in place plus an extracted `<template>` whose `&{ }` holes become `<slot>` markers; client lowers to `reconcile()` with a generated `bindItem` (ADR 0017 contracts). Un-gates `examples/module/list/module-list.tsrx` (TSRX001). Golden-test against the hand-written `module-list.ts`/`.html`.
  **Skill:** le-truc-dev
  **Changed:** `server/tsrx/compiler.ts` (ForIR gains `listSignal`/`keyName`; `lowerListFor` + `validateListBody` — statics + events + exactly one bare `&{item}` hole; key clause must be a bare identifier; index bindings rejected), `runtime.ts` (`createList` shim now mirrors cause-effect's `getKeyGenerator` exactly — string prefix/function/positional — so server `data-key` values match client keys), `emit-server.ts` (keyed in-place render via `list.entries()` with a synthetic `data-key` attr; extracted `<template>` with the hole as `<slot></slot>`, flushed after the container's close tag through a pending-queue stack), `analyze.ts` (ReconcilePlan pass 1b: container/template queries, bindItem-scoped per-item event queries, hole-parent selector; `@for`-in-list body rules — the key binding is a bindItem param, the item is Signal-only), `emit-client.ts` (`reconcile()` block with slot fill + per-item `on`), `effects/tsrx.ts` (registry seeded from hand-written example tags via `handwrittenExampleModules()`), CEM: module-list migrated (exclude + REQUIRED_TAGS).
  **How:** The child-module type-flow mechanism landed here too: the plan records addressed registry tags (`childTags`) and the generated client side-effect-imports their modules (generated clients for migrated tags, hand-written sources otherwise) so `declare global` tag-map entries resolve in the client's own scope — module-list's `textbox.length`/`pass(submit, …)` typecheck in emit-then-check.
  **Check:** Harvest duality — static seeds pass through verbatim (`createList<string>([], {…})`, DOM agrees by construction), arg-seeded seeds substitute `[...container].children].map(el => el.querySelector(hole)?.textContent ?? '')` with keys regenerating identically. The spec's `.html` golden differs from the .tsrx-authored markup (demo-state `disabled`, `span.label`) — goldens assert the compiler's render of the source truth; the fixture's style block is the Phase-0 variant, not the current hand-written CSS (sync on docs migration).
  **Open:** per-item reactive attributes and hoisted consts in list bodies are gated (milestone-3 subset is the module-list shape); object-item lists unharvestable (single string hole only).
  **Review:** Approved (2026-08-22, with independent reviewer pass — 8 findings fixed in place, regression-tested). Key hardenings: the bindItem fill is now reactive (`watch(item, bindText(hole))` — bindItem runs once per entering element, a one-shot read went stale on in-place value updates) and idempotent for adopted items; the harvest filters to `data-key` children (authored static siblings were phantom items); the runtime key shim matches cause-effect exactly (`||` fallback, not `??`); a second reactive list per component is diagnosed (shared `first('template')` selector would clone the wrong shape).

- [x] LT-004: Type-flow wiring — Volar projection + editor tooling (ADR 0023 milestone 4) — reviewed ✓ (approach reconsidered → LT-011)
  **Context:** `createVolarMappingsResult` over the generated client module as primary projection; `globals.d.ts` for ambient identifiers so raw `.tsrx` sources type-check in editors — the ambient set widened in LT-005: signal constructors plus `expose`/`host`/`internals` (client code positions), the `as*` parser factories and `defineMethod` (expose initializers), and `FormAssociatedElement` (referenced without import in `declare global`). Emit-then-check is already in CI via `server/tests/tsrx/client.golden.test.ts`, including the form fixture that proves the widened `FormFactoryContext` overload.
  **Skill:** le-truc-dev
  **Changed:** `server/tsrx/globals.d.ts` (the ambient vocabulary, typed against `@zeix/le-truc` exports) + `server/tests/tsrx/globals.test.ts` (parity with SIGNAL_CONSTRUCTORS ∪ CONTEXT_NAMES ∪ PARSER_FACTORIES ∪ {expose, defineMethod, FormAssociatedElement} — nothing declared outside the vocabulary — plus a tsc probe using every ambient); child-module side-effect imports (see LT-003 — the projection's type-flow half); emit-then-check now passes `--types node` (child imports pull src/ into the program).
  **How:** The Volar projection probe ran against `createVolarMappingsResult`: it THROWS (`No source map entry for position "…:…"`) unless the source map covers every token its source-AST visitor walks — the API is coupled to Ripple's own transforms (esrap token-complete maps). Our split compiler cannot satisfy it through the client module alone (template markup maps to the SERVER half). Follow-up ticketed as LT-011; globals.d.ts + child imports + emit-then-check are the deliverable half of milestone 4.
  **Check:** globals parity test fails CI if the compiler vocabulary grows without the declarations; the raw-source caveat (raw `.tsrx` is not TS syntax — `@{ }` blocks) stands, documented in globals.d.ts.
  **Review:** Approved (2026-08-22) for the delivered half (globals contract + child-module imports + emit-then-check). The projection half is RECONSIDERED per owner direction: `createVolarMappingsResult` is transform-coupled (demands token-complete esrap-shaped maps), and the client-only projection suffices anyway since TS diagnostics only arise in code positions, which all lower into the client module. LT-011 reshaped accordingly: CLI-first span-table diagnostics mapping, Volar later over the same spans. ADR 0023 sub-design 6 amended at acceptance.

- [x] LT-008: Form-corpus migration enablers — arg→DOM harvest substitution, `@if`, setup side-effects (parallel with LT-003/LT-004) — reviewed ✓
  **Context:** Blockers surfaced by the LT-005 fixture for migrating the real form components (ADR 0023 sub-designs 3/8): (1) `length`-style signals — initializers over server args (`createCell(value.length)`) whose client seed must come from the DOM need an arg→DOM-site substitution harvest rule (the arg's rendered attribute site → element-derived read); (2) `@if` conditional templates — form-textbox renders `<input>` OR `<textarea>`; (3) setup side-effect statements (`internals?.states.add('clearable')`) are outside the const+expose setup subset — decide a format (reactive `state={…}` attribute vs. relaxed setup); (4) a `formAssociatedCheckbox` corpus fixture (compiler support exists, nothing exercises it); (5) recognize `asClampedInteger`/`asJSON` as parser ambients; (6) diagnostic for a managed-prop lazy child (`&{'validationMessage'}`) authored without `formAssociated`. Parser gotcha to document: void elements must self-close (`<input />`) — the parser rejects HTML-style bare voids. Validation corpus: full-parity `form-textbox` migration against the hand-written trio.
  **Skill:** le-truc-dev
  **Changed:** (1) substitution harvest in `analyze.ts` — a pure-arg initializer with no rendered site rewrites each param identifier to its DOM read (host-prop mirror route wins: `createCell(value.length)` → `createCell(input.value.length)`, the hand-written shape; plain attribute and root `host.getAttribute` fallbacks, parenthesized); (2) `@if` end to end — `@else` spelling, server-known conditions (signal conditions rejected: the DOM keeps the rendered branch), client constructs on branch ROOTS with union selectors (`first('textarea, input', …)`) and identical-text validation; countForSelector treats branches as mutually exclusive (max, not sum); (3) client-only setup side effects — expression statements over context names/signals/ambients emit into the factory after expose() and never run server-side; (4) `examples/form/checkbox/form-checkbox.tsrx` (formAssociatedCheckbox fixture, both mirrors, return-update handler — CEM migrated: exclude + REQUIRED_TAGS + structural golden); (5) `asClampedInteger`/`asJSON` ambients + runtime shims; (6) TSRX010 managed-prop-without-formAssociated. `form-textbox.tsrx` advanced: `@if` input/textarea (`multiline` arg), `length` signal, reactive `hidden` on the clear button, `internals?.states.add('clearable')`.
  **Check:** The fixture is NOT full hand-written parity yet: description memo (`createMemo` + dataset remaining template) and `aria-describedby` remain — createMemo is not a recognized signal constructor, and per-markup conditional effects (`if (clearBtn)`) have no .tsrx form. Void self-close gotcha is exercised by the corpus (`<input />`).
  **Review:** Approved (2026-08-22, with independent reviewer pass). One fix applied in review: `createState` added to the recognized signal constructors + runtime shim + globals (the hand-written form components use it — a real migration blocker); `createMemo` deferred to LT-012 (derived-over-host reads need their own client lowering). Reviewer findings in the @if/branch machinery were fixed under LT-003/LT-013 notes (shared branch-body lowerer, html not a client construct).
  **Open:** exposing a prop harvested from DOM textContent (hand-written `label`) has no form; `@if` branches render text only through elements.

- [x] LT-009: `verify:cem` duplicate-tag guard (small, before the wholesale migration) — done ✓
  **Changed:** `scripts/verify-cem.ts` — duplicate-`tagName` check grouping declarations by tag with owning module paths, failing with the exclude hint. Verified against a synthetic duplicate manifest and live: module-list and form-checkbox migrations produced exactly the guarded failure mode before their hand-written twins were excluded.
  **Review:** Confirmed (2026-08-22). Guard fired correctly live during both migrations this session; no gaps.

- [x] LT-010: Template-literal-safe reindentation in codegen (small, before the wholesale migration) — done ✓
  **Changed:** `server/tsrx/indent.ts` (`lineStartsInTemplate` — single-pass lexer over strings, template literals with nested `${}` interpolation, comments, escapes; regex literals documented as the accepted limitation) consumed by `pushStatement` (emit-client), `reindent` (emit-server), and the new setup-statement anchor `pushSetup` (multi-line `createList(…, {…})` configs now re-anchor correctly). Interior lines pass through byte-identical; the common-indent computation excludes them.
  **Review:** Confirmed (2026-08-22, with independent reviewer pass — the reviewer hunted for a wrong-mask input over nested interpolation, strings containing `}`, and block comments inside `${}` and found none).

- [x] LT-013: tsrx.dev feature parity — `@switch`, `@try` error boundaries, `html={expr}` dynamic rendering, parse-error hints — reviewed ✓
  **Skill:** le-truc-dev
  **Context:** Verification-pass request: every language feature listed at tsrx.dev/features must be handled deliberately — supported or gated, never silently dropped. Probed against the pinned @tsrx/core 0.1.60: `@switch (disc) { @case expr: { … } @default: { … } }` and `@try { … } @catch (e) { … }` parse; statement-form switch, the `{html}`/`{text}`/`{ref}` keywords, setup `await`, and `component` declarations do not (newer grammar — parse errors, now with hints naming the construct and the pin policy).
  **Changed:** `server/tsrx/compiler.ts` (TemplateNode `switch`/`try` variants + `lowerSwitch`/`lowerTry` with shared `validateCondition` — server-known, signal-free discriminants; `html` attribute IR — data references only; `newerGrammarHint` on parse failures), `emit-server.ts` (real `switch` with per-arm `break`; `try`/`catch` rendering into an isolated `__arm` buffer so a mid-arm throw cannot leak partial markup — found by executing the render; `sanitizeHtml(String(expr))` children for `html={dataRef}`), `analyze.ts` (switch arms and try bodies/catch arms must be construct-free — exclusive/conditional rendering means the element is not guaranteed; @for-in gated), `runtime.ts` (`sanitizeHtml`: strips script blocks, `on*` attributes, javascript:/vbscript:/data: URLs — defense-in-depth matching `setAttribute`'s posture), `server/tests/tsrx/features.test.ts` (13 tests: arm rendering per args, no-leak error path, sanitizer against hostile markup, all gates, all three parse hints).
  **Check:** The `{name}` prop shorthand was verified already-supported (parser emits the identical JSXAttribute shape as `name={name}`; both halves byte-identical) and pinned by a compiler.test equivalence test. Reactive `html={() => …}` is rejected (no client-side sanitizer contract) — gated residue lives in LT-012 with `@pending` and try-body constructs.
  **Review:** Approved (2026-08-22, with independent reviewer pass — 4 of the 8 session findings were in this task's surface, all fixed in place). Key hardenings: nested control-flow directives in branch bodies now lower through a shared branch-body statement handler (they were silently dropped); nested `@try` joins into the enclosing arm buffer with uniquely named arm buffers (shadowing lost content); the sanitizer's unquoted-handler regex typo fixed (`<img src=x onerror=alert(1)>` now stripped).

- [x] LT-011: Type-flow diagnostics — CLI-first span-table mapping (ADR 0023 milestone 4, reconsidered) — done ✓
  **Skill:** le-truc-dev
  **Context:** Reconsideration after the LT-004 probe (owner direction): `createVolarMappingsResult` is transform-coupled — it walks every source-AST token and throws unless the map covers each one, an invariant only Ripple's own esrap-shaped transforms satisfy. Key insight dissolving the deeper blocker: TS diagnostics only ever arise in CODE positions (setup, thunks, handlers), and every code position lowers into the CLIENT module — so mappings only need to cover code spans; template markup maps to the server half and never produces TS diagnostics. Both emitters record a span table (`[generatedOffset, sourceStart, sourceLen]` per verbatim slice — the nodes already carry offsets); a `check:tsrx` CLI runs emit-then-check and maps tsc's line:col diagnostics back onto `.tsrx` line:col, giving authors type errors at their source with zero editor tooling. This is sufficient for CI and for AI agents working on `.tsrx` sources. Editor-level (LSP) diagnostics are scoped separately as LT-014, scheduled after the `examples/` wholesale migration to TSRX — see that task for why. globals.d.ts (raw-source ambients) and the child-module imports (type flow by projection) stand independently.
  **Changed:** `server/tsrx/spans.ts` (new — `appendWithSpans` records one `SourceSpan` per generated LINE a verbatim slice touches, mirroring the existing reindent rule exactly so it stays byte-for-byte compatible with the prior `pushStatement`/`reindent`/`pushSetup` output; `fileOffsetToLineCol`/`fileLineColToOffset`/`findSpanForGeneratedOffset` for the CLI's diagnostic remap). `compiler.ts` (`SourceRange`/`SetupStmt` types; `setup`/`clientSetup` now carry source ranges alongside their text; `exposeRange`; class-map `AttributeIR` gained a `thunk` node — `object` alone didn't cover the arrow's own range). `analyze.ts` (`LoopEffectPlan`/`TopEffectPlan`/`ReconcileItemEvents` variants carry optional `sourceStart`/`sourceEnd`, populated from the same `attr.thunk`/`attr.handler` nodes already in scope at each construction site). `emit-client.ts` (`pushStatement` replaced by `appendWithSpans`-based `push`; `emitEachBlock`/`emitReconcileBlock` now push directly into the shared `lines`/`spans`/`cursor` at their final depth instead of building a local array at depth+1 and blanket-stripping one leading tab — same output, and incidentally removes a latent bug where that blanket strip could have corrupted a masked template-literal line starting with a literal tab). `emit-server.ts` (`pushSetup` replaced by the same `appendWithSpans` call for `component.setup`, recording spans for parity though the server module isn't tsc-checked today). `index.ts` (`CompiledComponent.clientSpans`). `server/effects/tsrx.ts` (`compileTsrxCorpus` now returns `CompiledSpanInfo[]`). `scripts/check-tsrx.ts` (new — the `check:tsrx` CLI: compiles the corpus, runs the same `tsc --noEmit` invocation as `client.golden.test.ts` with `--pretty false`, parses each `file(line,col): severity TSxxxx: message` line, and remaps it through the component's span table). `package.json` (`check:tsrx` script). `server/tests/tsrx/spans.test.ts` (new — unit coverage of `appendWithSpans` incl. multi-line/template-literal-interior cases, the offset⇄line:col helpers, and an integration test compiling a fixture and confirming a handler's generated offset maps back to its exact `.tsrx` source position).
  **Check:** Verified live by temporarily injecting a type error into `examples/basic/counter/basic-counter.tsrx` (both a handler-body error and a setup-statement error) and confirming `bun run check:tsrx` reported the diagnostic at the exact authored line:col — column-checked against the source text, not just eyeballed. All 124 `server/tests/tsrx` tests pass unchanged (the golden snapshots are byte-identical — the span-recording refactor of `pushStatement`/`reindent`/`pushSetup` preserves their exact output). No public API surface changed (compiler-internal + a new dev-only CLI), so no review gate.

- [ ] LT-014: Type-flow diagnostics — Volar language-core plugin over the LT-011 span table (ADR 0023 milestone 4, stage 2)
  **Skill:** le-truc-dev
  **Context:** Scheduled after the `examples/` wholesale migration to `.tsrx` completes. CLI-first (LT-011) covers CI and AI-agent workflows, but human component authors need in-editor squiggles — LSP support is central to that authoring experience, not optional polish. Deferring until the migration completes rather than building it alongside LT-011 avoids building editor tooling against a corpus that's still shifting shape (harvest rules, `@if`/`@switch`/`@try` lowering, async boundaries) and lets the span-table format settle under real migration mileage first. A `@volar/language-core` plugin projects the generated client module as the virtual file, reusing the same `[generatedOffset, sourceStart, sourceLen]` span table LT-011 produces — no new mapping mechanism, no reuse of `createVolarMappingsResult` (rejected, ADR 0023 sub-design 6 amendment: transform-coupled, token-complete-map invariant we can't satisfy). Trigger: last example in `examples/` ported to `.tsrx`.

- [ ] LT-012: Async boundaries via `isPending` routing + the gated dynamic-HTML/error-boundary residue
  **Skill:** le-truc-dev
  **Context:** From the tsrx.dev feature-parity sweep (LT-013) and the owner's design direction (2026-08-22): async data needs NO grammar change — `await` is only blocked bare in the `@{ }` body (a sync function body, standard JS coloring); `deriveCell(async (_prev, abort) => …, { initial })` is ordinary JS today and returns a colorless `Cell<T>` backed by a Task (cause-effect: "deriveCell() replaces createTask() and can be async, but composes colorless sync"). **Decided lowering for `@try/@pending/@catch`: route on the signal via `isPending(signal)`** (re-exported by `@zeix/le-truc` since 2.5.1). Server (sync render): an async-derived signal is pending at render time unless `{ initial }` seeds a retained value — render the matching arm (pending arm by default; the initial value renders the ok arm — the no-JS state follows the seed). Client: all three arms ship server-rendered with non-matching arms `hidden`, and `watch(signal, { ok, err, stale, nil })` routing (AGENTS.md precedence nil > err > stale > ok) toggles visibility — pure enhance, no client DOM creation; the stale case is exactly pending-with-retained-value (default: keep the ok markup in place while re-fetching). Also here: `html={() => …}` (needs a client-side sanitizer contract), client constructs inside `@try` bodies (optional addressing), and `createMemo` initializers (derived-over-host reads need their own client lowering; `createState` landed in the LT-008 review). Note: `@tsrx/core` 0.1.60 is the LATEST published npm version — the newer grammar on tsrx.dev (statement-form switch, `{html}`/`{text}`/`{ref}` keywords, `component`) is upstream-unreleased; the parse-error hints should keep naming constructs without promising a pin upgrade unlocks them yet.
